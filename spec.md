# Product Page Rebuild — Engineering Spec

Project: modern e-commerce product page templates for Roof Racks Galore.
Source of truth for business requirements: `product-page-requirements.md` (same project folder). This spec translates that into a build plan.

## 0. Goal & Design Mandate

This is **not a re-skin of the existing site.** The current product pages are the feature/content baseline (what needs to be covered), not the layout or UX baseline (how it's presented). The mandate is a ground-up rebuild focused on conversion, benchmarked against 2026 ecommerce UX best practice — even if that means the four product types end up with meaningfully different layouts from each other and from the current site, so long as each is the best-converting approach for that product type. Brand colors/voice stay consistent; layout, structure, and interaction patterns do not need to.

This is a **prototyping/scaffolding phase**, not a production Magento theme build. Output should be static or lightly-templated pages (framework TBD — plain HTML/CSS/JS, or a component framework, whichever is fastest to iterate on visually) that can be reviewed, iterated on, and eventually used as the reference for the real Magento 2 template work.

**Direction validated by client, 2026-09-10 (Scott Childs & Jake/RRG Web Products):** the client's separate in-house Figma redesign was independently reviewed by external UI/UX contacts and informal usability testing (non-industry spouse test) and found to be *worse* than the current live site on usability grounds — "too busy," no single clear primary action. In the same meeting, this project's config-variant prototype (built from this spec) was shown instead and explicitly endorsed by both stakeholders as the direction to build toward going forward ("100%", "I definitely think we should be going with what you've done"), superseding the client's Figma work. The operating principle going forward: **above the fold = hero image + title + price + one unmistakable primary CTA (Add to Cart) + fitment status where applicable, nothing else competing for attention; every other feature (trust icons, on-display/showroom info, gallery, video, tabs, related products) gets pushed below the fold or into the accordion tabs.** This doesn't remove any required feature from Section 5 — it's a placement/prominence principle, not a scope cut. See Section 9 for the full list of confirmed follow-up changes from this meeting.

### 0.1 Conversion research findings to design against

Grounded in 2026 ecommerce UX research (Baymard Institute benchmarks, Nielsen Norman Group, Spiegel Research Center, and fitment-vertical CRO case studies). Use these as design constraints, not just inspiration:

1. **The bar is genuinely low industry-wide.** Baymard's 2026 benchmark found 52% of desktop sites, 62% of mobile sites, and 64% of apps deliver "mediocre or worse" product page UX. Getting the fundamentals right is itself a competitive advantage.

2. **Imagery and video are the highest-leverage investment, not copy.** 93% of consumers cite visual appearance as the key purchase-decision factor (Nielsen Norman Group). High-quality photography across multiple angles plus video is the single highest-impact conversion lever for most categories. This validates and elevates the priority of the Fitment Gallery and product video requirements — they are core conversion infrastructure, not supplementary content.

3. **Reviews carry outsized weight on higher-ticket items.** Products with 5+ reviews convert 270% better than products with zero reviews (Spiegel Research Center); for items over $100, five reviews increases purchase likelihood by 380%. Reviews/star rating need real visual prominence near the top of the page, not a link buried below the fold.

4. **Total cost and return-policy transparency are widely neglected — an easy structural win.** 67% of ecommerce sites fail to show total cost (incl. shipping/tax) on the product page; 44% fail to surface return policy. Both are named top abandonment causes. RRG already has strong underlying content here (Delivery/Click & Collect widget, Gold Guarantee) — the design task is surfacing it early and clearly, not tabbing it away.

5. **Fitment uncertainty is the single biggest conversion killer in this specific vertical — this is the most important finding for this project.** CRO case studies in fitment-driven verticals (ignition parts, auto parts generally) consistently identify "fitment uncertainty close to purchase" as the top structural conversion blocker, and identify precise vehicle compatibility + clear specs + fast confirmation as what actually converts browsers into buyers. **Implication: the fitment status message (Fits / Confirm your vehicle / Doesn't fit) must sit directly beside price and Add to Cart — not as a badge elsewhere on the page, not tabbed, not below the fold.** This is the single highest-priority placement decision in the whole spec for vehicle-specific templates.

6. **Mobile-first is mandatory, not a checkbox.** Mobile is ~73% of ecommerce traffic. 2026 best practice specifically calls for sticky Add-to-Cart CTAs, thumb-friendly variant selectors (not tiny dropdowns), and compressed/collapsible content sections on mobile — design mobile layout first, not as a responsive afterthought of desktop.

7. **Replace "above the fold" thinking with the "3-second confidence rule."** A visitor should get an instant, clear answer to "should I even consider this product?" near the title — a short value-prop/differentiator line, not just a title and price. Relevant especially for vehicle-specific and config-variant templates where the product name alone is dense/technical.

8. **Structured data and AI-assisted shopping discovery are now a live design input, not just SEO.** 2026 guidance includes Product/variant schema markup and natural-language-friendly content so AI shopping assistants can accurately compare and recommend. Worth a lightweight pass (schema.org Product/Offer markup) even at prototype stage so the pattern is established.

Full source list is in Section 8.

## 1. Product Type Taxonomy

Four page templates are required. A given real product may combine more than one dimension (see Section 4, open question). **Per the design mandate above, each template's layout should be optimized independently for its own conversion challenge rather than forced into one shared skeleton** — Section 5 gives a components list, not a fixed layout order, for this reason.

| Type | Variant mechanism | Reference example(s) | Primary conversion challenge |
|---|---|---|---|
| **Simple** | None | Front Runner Pro Water Tank 42L (`FRWTAN063`) | Fast, low-friction path to cart — minimal hesitation expected, don't over-build the page |
| **Config-variant** | Dropdown/toggle (non-visual options, e.g. Assembled/Unassembled) | Rhino Rack 6 Series Pioneer Platform 900×1430 — `RH62112` (assembled) vs `RH62112F` (flat pack) | Making the trade-off (price vs. convenience/labor) clear and easy to compare |
| **Sibling/color** | Swatch picker | MaxTrax MKII — full family of **13 colors** scraped (`data/raw/MAXTRAX_MKII_family.json`), each currently its own listing on the live site | Visual browsing/selection — swatches need to be fast and satisfying to click through |
| **Vehicle-specific kit** | Config-variant (assembled/unassembled etc.), layered on top of fitment logic — confirmed combinable, not either/or (see Section 4, item 1) | Rhino Rack JC-01773 Pioneer 6 Platform for Toyota Hilux N80 — `GP01M1TZZ` | **Fitment confidence** — this is the highest-stakes conversion problem of the four (see 0.1.5) |
| **Grouped/bundle kit (non-vehicle)** ✅ *confirmed 2026-09-10, built 2026-09-10* | None (fixed bundle) | Yakima RoadShower 15L Complete Shower & Hose Bundle — `8004109PROMO` (real, live; client's own promised example SKU never arrived, so this stands in — flagged for confirmation) | Same challenge as Simple (fast, low-friction) — the only difference from Simple is a "What's Included" component-breakdown box (de-emphasized styling, same as vehicle-specific's) since the bundle has multiple physical parts. Layout is otherwise Simple's, not a distinct template. Built at `prototypes/grouped-bundle/index.html`. |

### 1.1 Critical constraint: no variant UI exists on the live site, anywhere

Every variant (color, assembled/unassembled, etc.) is currently a fully separate product listing — its own URL, SKU, page. There is nothing to lift or adapt for on-page variant selection; it's being built from scratch. Practically, this means:

- **Scraping is per-listing, not per-product-family.** To build one config-variant or sibling/color page, scrape each separate live listing that belongs to the family, then merge.
- **A merge step is required** before a variant template can render. See Section 3.

## 2. Data Model

Suggested shape for a merged product record (JSON), used to drive all four templates:

```json
{
  "product_family_id": "rhino-pioneer-6-900x1430",
  "product_type": "config-variant",
  "shared": {
    "brand": "Rhino Rack",
    "brand_logo_url": "...",
    "title_base": "Rhino Rack 6 Series Pioneer Platform 900 x 1430mm",
    "description_long": "...",
    "trust_icons": [
      {"label": "Trusted Since 1989", "sub": "Now with over 30 locations Australia wide"},
      {"label": "Trained Professionals", "sub": "Over 200,000 roof racks fitted"},
      {"label": "Australia's Largest", "sub": "We're the only nationwide roof rack specialists"},
      {"label": "Need Help", "sub": "Contact us on: 1300 071 264"}
    ],
    "gold_guarantee_content": "...",
    "shipping_info_content": "...",
    "breadcrumbs": ["Home", "Platforms & Trays", "..."],
    "related_products": [ { "sku": "...", "name": "...", "price": "...", "image": "...", "url": "..." } ]
  },
  "variants": [
    {
      "variant_label": "Assembled",
      "sku": "RH62112",
      "price_was": 1059.00,
      "price_now": 900.15,
      "discount_pct": 15,
      "images": ["..."],
      "video_url": "https://www.youtube.com/embed/cWOku7VJcjM",
      "fitting_instructions_pdf": "https://rackit.s3.amazonaws.com/manuals/...",
      "specifications": { "Platform Style": "Flat", "Platform Size (L x W x H)": "900 x 1430mm", "Number of Platform Planks": 4, "Platform Weight": "12.5kg", "..." : "..." },
      "stock_status": "in_stock"
    },
    {
      "variant_label": "Flat Pack (Unassembled)",
      "sku": "RH62112F",
      "price_was": 979.00,
      "price_now": 780.00,
      "discount_pct": 20,
      "images": ["..."],
      "video_url": null,
      "fitting_instructions_pdf": "https://assets.rhinorack.com/Instructions/...",
      "specifications": { "Number of Platform Planks": 5, "...": "..." },
      "stock_status": "in_stock"
    }
  ]
}
```

Notes:
- `shared` holds anything identical across variants (Details prose, Gold Guarantee, Shipping Info, trust icons, breadcrumbs, related products, brand).
- `variants[]` holds anything that differs (price, SKU, images, video, fitting instructions PDF, and any specification key that differs — see plank-count discrepancy below).
- For **simple** products, `variants` has exactly one entry and the variant picker UI is omitted.
- For **vehicle-specific kits**, add a `fitment` object (see Section 2.1) instead of a variants array, or alongside it if a kit can also carry a config-variant (open question, Section 4).
- Specification fields that differ between variants (e.g. plank count 4 vs 5 on the assembled/flat-pack pair) should live in each variant's `specifications`, not `shared` — even if most spec fields are identical, don't assume the whole spec table is shared without checking field-by-field during scraping.

### 2.1 Fitment object (vehicle-specific kits only)

```json
{
  "fitment": {
    "vehicle_make": "Toyota",
    "vehicle_model": "Hilux",
    "vehicle_generation": "N80",
    "vehicle_years": "2015-2026",
    "body_style": "4dr Ute",
    "roof_type": "Bare Roof",
    "fitment_gallery": {
      "count": 283,
      "images": [ { "thumb_url": "...", "detail_url": "https://www.roofracksgalore.com.au/rackit/fitment/display/item/rackit/product_id/component?parent_sku=GP01M1TZZ&product_id=166874" } ]
    }
  }
}
```

The three fitment-messaging states (Fits / Unknown / Doesn't fit) are a **runtime/session concern**, not scraped data — they depend on comparing this product's fitment data against whatever vehicle is set in the user's session at view time. Build this as a small pure function: `getFitmentStatus(productFitment, sessionVehicle) -> "fits" | "unknown" | "no_fit"`, with the three states driving which message/CTA renders near the top of the page.

## 3. Variant Merge Strategy (scraping → page model)

For config-variant and sibling/color templates:

1. Identify all separate live listings belonging to one product family (manual list for now — provided per-family by the client; no automated "family detection" exists on the live site).
2. Scrape each listing individually. Use targeted extraction, not a single full-page dump — see Section 3.1 for why.
3. Diff the scraped records field-by-field. Anything identical across all variants → `shared`. Anything that differs → stays in that variant's object.
4. Flag any field that differs unexpectedly (e.g., plank count 4 vs 5) for a human sanity check rather than silently treating it as either shared or correct — some differences may be genuine spec differences, others may be data inconsistencies on the live site.
5. Assemble into the JSON shape in Section 2 and hand to the template.

### 3.1 Scraping notes

- Vehicle-specific product pages can have enormous fitment galleries (283+ images seen on one SKU) that dominate the page's HTML. A naive full-page text scrape will truncate before reaching price/description/tabs content. Scrape targeted sections separately:
  - Price + discount badge
  - Title + breadcrumbs
  - Description (short + long)
  - Specifications table
  - Tab contents (Gold Guarantee, Shipping Info, Fitting Instructions link)
  - Related products carousel
  - Fitment gallery (paginate or cap; don't pull all images inline into the same extraction pass as everything else)
- Info tabs are **conditionally present** — not every product has Fitting Instructions or a video. Scrape what's there; don't assume a fixed tab count.
- Pricing pattern to expect: `$was $now` strikethrough pair, plus a discount badge (e.g. "Save 20%" or "$243 off RRP" depending on product type).

## 4. Open Questions (flag to client before finalizing templates, don't block scaffolding on these)

1. ✅ **Resolved (client meeting, 2026-09-10 — Scott Childs & Jake/RRG Web Products):** Yes — a vehicle-specific kit *can and should* also carry a config-variant (e.g. assembled/unassembled) simultaneously. Confirmed via the Pioneer 6 Platform × Hilux N80 example: a customer searching their Hilux still needs to pick between flat-pack and assembled. The data model needs both `fitment` and `variants[]` on the same record, and the page needs both a variant picker and fitment messaging, adjacent to each other near price/Add to Cart. **Action: add a variant picker to the vehicle-specific template** (see Section 9).
2. Is vehicle fitment matched exactly (this exact model/year), or does it span a range/generation? Affects how `getFitmentStatus` compares session vehicle to product fitment. Still open.
3. Do sibling/color products (e.g. MaxTrax) ever also need vehicle-fitment logic, or are "variant type" and "fitment type" independent, freely-combinable dimensions? Still open.

Scaffold assuming these are all independent/combinable (most flexible), and note in code comments where a decision on the above would simplify the model.

## 5. Page Composition

**This is a components list, not a mandated layout order.** Per the design mandate (Section 0), each template should sequence and weight these differently based on its own conversion challenge. The one hard placement rule, driven directly by the research in 0.1.5: **for the vehicle-specific template, fitment status must be adjacent to price/Add to Cart — treat this as non-negotiable, not a design preference.**

Components to build (all templates draw from this pool, not all use all of them). **Status key:** ✅ built and live on the relevant template(s) · ⬜ not started.

- ✅ **Breadcrumbs — status corrected again 2026-09-10 (second correction, same day).** The previous correction above was itself wrong: it was based on `grep -r breadcrumb prototypes/` returning zero matches, but the actual markup uses the class `.rrg-crumbs` (documented correctly in `PAGE-GLOSSARY.md` the whole time) — the literal word "breadcrumb" never appears in the code, so the grep was a false negative. Breadcrumbs have existed on all 5 templates since the first commit. The real gap, found once the actual markup was inspected: Vehicle-specific was missing the "Vehicle Landing Page" level Graham's meeting asked for (had Home → Category → Product, not Home → Vehicle → Category → Product), Simple was missing a category level entirely (Home → Product only), and Vehicle-specific had an unescaped `&` bug in "Platforms & Trays". Both fixed 2026-09-10: Vehicle-specific now reads Home → Toyota Hilux N80 → Platforms & Trays → [product]; Simple now reads Home → Camping & Offroad → [product]. Config-variant, Sibling-color, and Grouped-bundle already had a correct Home → Category → Product trail and were left untouched. **Lesson for future sessions: don't grep for the word "breadcrumb" — check for `.rrg-crumbs` instead.**
- ✅ **Brand logo — now a hard requirement on every template, not just Vehicle-specific** (client instruction, 2026-09-10). Real logo files for all three brands in the current catalogue (Rhino Rack, Front Runner, MAXTRAX) are on hand — see Section 9.
- ✅ Title + micro value-prop line (the "3-second confidence rule" answer — a short differentiator, not just the product name)
- ✅ SKU (with copy-icon) + reviews/star rating — reviews need real visual weight, not a small link, given the 270–380% conversion lift they carry on higher-ticket items. The copy-icon actually copies the SKU to the clipboard now (was decorative at first build) — instore-staff-facing utility, confirmed working.
- ✅ **Done 2026-09-10 (Graham Sowerby meeting):** Ex-demo/factory-seconds — the modal itself (2–3 condition/price options, each with its own price/fulfillment note/image) stays, but the entry point changed from a separate `.exdemo-cta` button to inline text appended to the stock-status line itself — `In Stock — Ex-Demo/Factory Seconds from $1,495`, with the price portion a clickable blue link (`.exdemo-inline-link`) that opens the existing modal (`renderStockLine()` in shared.js). Supersedes the earlier "try 2–3 button-weight variations" idea from the same meeting. Still demo-panel-toggled, still needs a real inventory data source before production (unchanged gap).
- ✅ Price (was/now, discount badge) + total-cost/shipping indicator nearby (don't make the visitor go find shipping cost separately — this is a named top abandonment cause)
- ✅ **New:** Sale tag graphic (corner ribbon on the decision panel) — shown/hidden by the same on-sale state as the price, not a separate toggle. Client-supplied asset (`sale-tag.png`, a "Spring Sale" design specifically — a generic/seasonless version is worth getting for when the promotion changes). **Fixed 2026-09-10:** was only vertically centered against `.price-row` (the price/badge line), reading as sitting too high against the full `.price-block` (which also includes the "✓ In Stock" line beneath). Now absolutely positioned against `.price-block` itself (`position:relative` on the block, `top:50%;transform:translateY(-50%)` on the tag) so it centers against the whole block on all 5 templates.
- ✅ **New:** Special order messaging — a banner near Add to Cart ("Special order: contact us and we'll order this in for you, approx. 5–7 business days") for products that aren't on-hand but are orderable. Add to Cart stays enabled (client's explicit call — informational, not a purchase blocker). Demo-panel-toggled preview only; **flag to client: needs a real "is this a special-order SKU" data field before production.**
- **Vehicle-specific template: fitment status message (Fits / Confirm your vehicle / Doesn't fit) — placed directly beside price/Add to Cart, not elsewhere** ✅ — ✅ **lightened 2026-09-10** per client feedback: `.fitment` changed from a full solid-tinted box to a white/near-white card with a 3px colored left accent, label+dot keeping the state color, body copy neutral gray. Applies automatically to both the decision panel and the persistent/sticky decision bar's fitment indicator (same shared class), which keeps its structure/behavior as-is per client confirmation.
- ✅ **Done 2026-09-10 (Graham Sowerby meeting), extended to Grouped-bundle same day per client follow-up.** Package contents list ("What's Included") for multi-part kits — **Vehicle-specific and Grouped-bundle both** — each component as its own row, quantity leading (`.pi-qty`, e.g. "1x Rhino-Rack 6 Series Pioneer Platform"), SKU text itself as the click-to-copy target (`.sku-copy`), no separate copy-icon button. Same change applied to the main SKU row above the product title, all 5 templates. Vehicle-specific's quantities (all ×1) are a placeholder default — no real per-component quantity data exists, flagged in Section 7. Grouped-bundle's quantities (all ×1) are **real, confirmed data** — they match the page's own existing "Bundle Includes" list in the Details tab verbatim, not a guess. First pass had missed Grouped-bundle entirely (its rows still used the old `.copy-btn` icon button with no quantity) — corrected same day per client catch.
- **Config-variant / sibling-color templates:** variant picker (dropdown/toggle or swatches) — thumb-friendly sizing on mobile, not a cramped native dropdown ✅
- ✅ Image carousel (master + secondary images; embed video where present) — highest-leverage visual investment per research, don't undersize this on mobile. Thumbnails now scroll as a single-row carousel (prev/next nav) rather than wrapping to a second row once there are 5+ images.
- ✅ Add to Cart — sticky on mobile scroll. **Colour locked in 2026-09-10 (Graham Sowerby meeting):** gold is now the real shipped default, not a preview toggle — Graham saw it live on the gold state and said to lock it in ("I think I'm happy to actually just lock in the gold"). The red-vs-gold Demo State Panel checkbox was removed (no longer an open question); `[data-cta-label].btn-primary` in shared.css is gold (`#FFCA48`/black text) by default across all 5 templates, while other `.btn-primary` buttons (e.g. Find Nearest Showroom) stay red.
- ✅ **New (client meeting, 2026-09-10), done:** Payment-plan badges (Afterpay/Zip/PayPal Pay in 4) directly under Add to Cart, on all five templates (including the new grouped/bundle one) — new `.payment-badges` component in shared.css, CSS-drawn wordmarks since no logo assets exist for these providers. **Confirmed 2026-09-10 (Graham meeting) this isn't just nice-to-have — it's a contractual display requirement under RRG's Afterpay agreement**, on top of the conversion benefit.
- ✅ **Done 2026-09-10 (Graham Sowerby meeting rework):** Delivery / Click & Collect widget (tabbed: Delivery | Click & Collect) — single postcode field shared across both tabs (was one input per tab), results visible by default (no click required), default tab flipped to **Click & Collect** (was Delivery), small icons on the tab labels, and a "View all stores" link (not button) under the Click & Collect results opening a new store slide-out drawer (`.store-slideout`, `buildStoreSlideout()` in shared.js — no such slide-out previously existed anywhere in the prototype, despite earlier docs implying one did; confirmed by full-repo search before building it). The existing "both unavailable" message state (`.dc-unavailable-note`) needed no changes — still correct with the new default tab.
- ✅ **Done 2026-09-10 (Graham Sowerby meeting rework):** Showroom Finder widget — on all 5 templates, with a Demo State Panel toggle to preview a SKU that isn't on display anywhere. Two changes: (1) a small **"On Display" pill** (`.stock-chip.display`, own gold/bronze colour, distinct from the green "In Stock" pill) next to the stock-status line near price/CTA, visible on load without needing to interact with anything, wired to the same toggle as the full block; (2) an **experimental interactive map** (Leaflet + OpenStreetMap, free, no API key) piloted on **Vehicle-Specific only**, toggled from its own Demo State Panel section, swapping in place of the block's default content — real map tiles, pins for the demo store network. Explicitly a "test and scrap if it doesn't work" pilot per the meeting, not yet rolled out to the other 4 templates or made a committed feature.
- ⚠️ Get It Installed CTA — built as an image+text panel (installer photo, REVIEWS.io badge, "More Information + Bookings" button), paired side-by-side with the product video under the gallery where both exist, or with a video placeholder where no real video was scraped. **Placement rule confirmed 2026-09-10: this component must sit directly under the secondary image thumbnails, constrained to the width of the image column — never full page-width.** ✅ **Fixed 2026-09-10:** vehicle-specific's `.install-media-row` moved inside the gallery column div (was a separate full-width `<section>` after `.hero` closed) — now matches config-variant's placement exactly. **Rolled out to Simple and Sibling-Color 2026-09-10 (client ask)** — previously they only had a bare, admin-toggleable video-thumbnail placeholder with no Get It Installed panel; now all 5 templates carry the full `.install-media-row` pairing and respond to the same Demo State Panel "Has video" toggle. ✅ **Done 2026-09-10 (Graham Sowerby meeting item):** button copy now has two states — vehicle-specific's default markup reads "See 283 real fitments" (count sourced from `#fitGallerySection`'s new `data-count` attribute) and links to `#fitGallerySection` (smooth-scrolls via `shared.css`'s existing `html{scroll-behavior:smooth}`); falls back to "More Information + Bookings" / `#` when no Fitment Gallery exists. New "Fitment Gallery exists for this product" Demo State Panel toggle (`applyFitGalleryFlag()` in shared.js) previews both states by hiding/showing `#fitGallerySection` and swapping the CTA together, so the two never go out of sync. Other 4 templates unaffected (none have a Fitment Gallery).
- ✅ **New (client meeting, 2026-09-10), demo-preview only, done:** Paid "Fitted" option as a purchasable upsell preview — generic `reapplyFittedOption()`/`setFittedOptionFlag()`/`setFittedOptionMode()` in shared.js, gated on `.variant-picker` presence (config-variant + vehicle-specific). Master on/off switch, plus a sub-toggle between **Mode 1** ("Fitted" as a full-width third card appended to the variant picker, gold-tinted, "Preview only" label, survives real variant-switch re-renders) and **Mode 2** ("Fitted" as a separate upsell checkbox injected directly above Add to Cart). Purely visual — no real price/cart wiring. Real feature still needs store-ops/state-manager sign-off before going live (fitting costs aren't standardized across stores).
- ✅ Trust icon row (4 icons, same content site-wide)
- ✅ **Done 2026-09-10 (Graham Sowerby meeting).** Short description: `.value-prop` (the short line under the title) now clamps to 2 lines (`-webkit-line-clamp`) with a "Read more" link that checks the Details tab's radio input directly and scrolls to it (`initTabJumpLinks()` in shared.js — the tabs accordion is pure CSS, so a plain anchor jump wouldn't have switched the tab).
- ✅ **Done 2026-09-10 (Graham Sowerby meeting).** Info tabs: Specifications now renders as a real 2-column `<table>` (attribute `<th>` | value `<td>`) on all 5 templates — the two JS-rendered templates (config-variant, vehicle-specific) and the hardcoded one (sibling-color's `renderSpecs()`) now emit `<tr>` rows instead of `<li>`; Simple and Grouped-bundle's static bullet lists were converted directly (Grouped-bundle's prose bullets split into label/value pairs by hand, e.g. "Water capacity: 15L" → Water Capacity | 15L). Gold Guarantee tab condensed to 2 USP lines (90-day exchanges, nationwide support) + a "Learn more" link — no dedicated guarantee page exists yet to link to, so it's an `href="#"` placeholder like other not-yet-built destinations in this prototype (flag before this leaves prototype stage). **Superseded 2026-09-10 (tab-content fidelity pass):** the condensed 2-line version was replaced with the full real Gold Guarantee copy on all 5 templates — see the "Tab-content fidelity pass" entry in Section 9. The "Learn more" `href="#"` gap noted above no longer applies (there's nothing left to link out to — the full content is inline).
- ✅ **Done 2026-09-10 (Graham Sowerby meeting item):** Important Vehicle Fit Notes ("Important Vehicle Footnotes" on the live site) — vehicle-specific only, `.vehicle-fit-notes`/`#vehicleFitNotes` block sitting directly below the fitment status card, above the variant picker. Warm amber accent, distinct from the fitment card's own state colors. Starts `hidden` (variable length, not present on every vehicle, no real per-vehicle content yet — current copy is placeholder demo text flagging a Hilux N80 sunroof/tow-bar clearance caveat, needs real client content before production). Toggled via a new Demo State Panel checkbox (`applyVehicleFitNotesFlag()` in shared.js).
- ✅ **Done 2026-09-10 (Graham Sowerby meeting item, scope expanded + refined per client direction same day):** Rack Fit Guarantee — real client-supplied asset (`fitgaurantee.svg`, copied into `prototypes/_shared/`), not a hand-drawn grayscale icon as originally scoped. Appears on **all 3 fitment instances** (client ask): icon in the top-right corner of the main decision-panel fitment card (`.fitment-wrap`/`.rack-fit-badge`), and icon-only on the two condensed fitment strips (sticky mobile bar, persistent desktop bar — `.rack-fit-badge-icon`). **Icon-only everywhere, no visible caption text** ("Peace of mind with our FREE Nationwide Rack Guarantee" is a `title` tooltip on all 3, not on-page text — first version had it as visible text on the main panel, client felt it was too much). **Only shown when the session vehicle is a confirmed match** ("Fits your vehicle") — hidden for "Confirm your vehicle" and "Doesn't fit your vehicle" states, and when no vehicle is set. `applyFitmentState()` in shared.js now hides/shows each badge (found via `el.nextElementSibling` off the `[data-fitment-slot]` div) alongside its normal fitment-copy re-render, so this works generically across all 3 instances with one code path. All three badges are markup siblings to `.fitment` rather than injected content, so they're untouched by the innerHTML rewrite itself. Fitment-relevant-only is automatic since only Vehicle-specific has `.fitment` at all. No demo toggle — permanent, always-on (visibility is driven entirely by fitment state). **Fixed 2026-09-10:** the main-panel icon (`.rack-fit-badge`) was pinned `top:10px`, sitting noticeably above center against the fitment card's actual height — changed to `top:50%;transform:translateY(-50%)` so it centers correctly regardless of how tall the fitment card's copy makes it.
- **Vehicle-specific only:** Fitment Gallery ("Fitment Gallery" panel: icon badge + heading, right-aligned "View All In-store Fitments (N)" link, landscape-photo carousel with nav arrows + page dots) — high priority given research on imagery's conversion weight ✅. Redesigned 2026-09-10 to match a live-site/Figma reference — see Section 9's "Fitted Photos Gallery" follow-up entry for full detail. **Placement/colour finalized 2026-09-12 (Section 12 item 5):** full-width above the Trust Row, non-red background, permanently — the placement/red-background Demo State Panel toggles that existed to help decide this have been removed. "View All In-store Fitments" now opens a real photo-grid slide-out (Section 12 item 26).
- ⚠️ **Superseded 2026-09-10 (corrected same day):** the "Gallery Placement" toggle originally built here moved the *Main Product Gallery* on both vehicle-specific and config-variant — this was a misunderstanding. It's been corrected to move the **Fitment Gallery** (not the Main Product Gallery, which no longer moves at all) between full-width-above-the-tabs and nested-under-the-main-gallery. Vehicle-specific only — config-variant's copy was removed since it has no Fitment Gallery to place. **Retired 2026-09-12** once the final full-width placement was locked in (Section 12 item 5) — see Section 9.
- ✅ Related / recently viewed / upsell / cross-sell carousels
- ✅ **Done 2026-09-12.** Lightweight schema.org Product/Offer structured data now on all 5 templates — added static JSON-LD blocks to Config-variant (RH62112), Sibling-color (default colour Black, MTX02BK), and Vehicle-specific (default variant GP01M1TZZ), matching Simple/Grouped-bundle's existing pattern. Each reflects the page's default/first variant only, not live-synced to the JS variant/colour switcher — since each variant/colour is really its own separate page/URL in production (already documented, item 2), a static per-page block is actually the more accurate model, not a shortcut. Verified with Playwright: all 3 new blocks parse as valid JSON with correct name/sku/price/image/url for their default variant, zero console errors.

## 6. Suggested Build Order in Claude Code

**Status key used below:** ✅ done · ⚠️ done but deviated from the suggestion · ⬜ not started. See Section 9 for the full current-status log.

1. ✅ **Design exploration first, per template — produce 2–3 distinct layout directions, not one, and justify every one of them.** Done in full for **vehicle-specific only** (the highest-stakes template — see Section 9). ⚠️ For Simple/Config-variant/Sibling-color, built directly as one design each rather than 2–3 options — an explicit, deliberate choice the client made at build time given these are the lower-stakes templates per the Section 1 table and the strong shared-component direction already validated on vehicle-specific. Original suggestion: For each of the four templates, before committing to a single build, generate 2–3 meaningfully different layout options (not just color/spacing tweaks — different structural approaches to sequencing and weighting the components in Section 5). Each option should be a standalone, reviewable page (real scraped data, not lorem ipsum) so it can be judged on how it actually reads, not just described.

   Every option needs a written justification alongside it, not just a label. For each option, document:
   - **What specific design decisions were made** (e.g. "fitment status placed above the fold beside price, reviews moved into a compact strip under the title rather than a separate tab").
   - **What finding or principle each decision is grounded in**, citing the specific research point from Section 0.1 (or additional research pulled in during the exploration step) — not a vague "best practice" reference. If a decision isn't backed by a specific finding, say so plainly rather than dressing it up as evidence-based.
   - **What it trades off against.** No layout decision is free — leading with fitment status may compress visual/gallery space above the fold; leading with imagery may push fitment confirmation further down. Name the trade-off honestly for every option so the choice between options is a real comparison of costs and benefits, not just a comparison of upsides.

   This turns option review into "which set of trade-offs fits us best," not "which one looks nicest" — the goal is finding the right balance visually and usability-wise for this business specifically, not picking a winner by instinct.
   - Vehicle-specific template: all options must still satisfy the non-negotiable fitment-placement rule (Section 5) — the options should differ in everything else (gallery prominence, tab structure, review placement, etc.), not in whether that rule is respected.
   - Simple/config-variant/sibling-color templates: freer to explore, since their conversion challenges are lower-stakes per the Section 1 table.
2. ✅ **Review checkpoint:** done for vehicle-specific (Option A selected; B and C were later deleted outright rather than kept archived — see Section 9). ✅ **Closed 2026-09-12 for Simple/Config-variant/Sibling-color** — these were built directly rather than as reviewable options, and never got a single formal "review this design" moment the way vehicle-specific did, but Brenton's explicit call (rather than running a fresh critique pass) was that the substantial iterative correction these three have been through since — Section 10's USP rows, pricing/price-block redesign, region selector + UK brand skin, sticky/persistent bar fixes, and Section 12's heading-hierarchy audit, status-pill unification, short-description expand/collapse, and more, all applied and verified across all 5 templates — already functions as that feedback pass. See Section 9 for the dated log entry.
3. ✅ Scaffold the shared component library — done, and went well beyond "mock/placeholder data" over several build rounds: lives at `prototypes/_shared/shared.css` + `shared.js`, used by all four templates with real content. As of 2026-09-10 it includes:
   - Trust icons, breadcrumbs, a tabs container (horizontal tab bar on desktop ≥901px, vertical accordion on mobile — both built on radio-button inputs so exactly one panel is ever open).
   - A full tabbed Delivery/Click & Collect widget (postcode entry, live Standard/Express or nearest-store results) and a Showroom Finder CTA block.
   - An image gallery: main image (3:2, was 4:3) + thumbnails as a single-row scroll carousel with prev/next nav (was a wrapping 4-column grid — changed because 5+ images pushed page content down and created height mismatches against the shorter sidebar column).
   - Product video + Get It Installed CTA, paired side-by-side under the gallery (`.install-media-row`/`.install-cta-panel`) with a full-width fallback layout when there's no video to pair with.
   - Brand logo, sale-tag graphic, and working copy-to-clipboard SKU buttons (see Section 5) on every template.
   - Ex-demo/factory-seconds CTA + modal, and a special-order banner (see Section 5).
   - A product-card grid for related products.
   - A sticky mobile Add-to-Cart bar, and (vehicle-specific only) a persistent desktop decision bar that appears once the initial price/CTA panel scrolls out of view.
   - **The Demo State Panel** — a floating reviewer tool (not a production component) for previewing how any template looks in different product states (has video / on sale / in stock / shipping available / click & collect available / special order / has ex-demo stock), plus the vehicle-fitment simulator on Vehicle-specific. Full detail in Section 9 — this is the main thing to understand before doing further work on these prototypes, since most "does this look right if X" questions should be answered by toggling the panel rather than editing markup.

   Real site header (utility bar + main nav + search + cart) and real logo (`headerlogo.png`) are wired in across all pages, matching the client's "Navbar - Logged In - Final.png" reference.
4. ⚠️ Build the data model + merge script — **not** built as the standalone, independently-testable utility this step suggests. Each template instead embeds its own merged product data as an inline JS object in its page (see `MERGED_PRODUCT` in `config-variant/index.html` and `MAXTRAX_FAMILY` in `sibling-color/index.html`), driving live variant/swatch switching (price, gallery, video, specs, fitting-instructions link all update correctly). Fine for the prototyping phase; **a real standalone merge utility is still owed** before this becomes reference material for the Magento build.
5. ✅ Simple template wired — `prototypes/simple/index.html`, real Water Tank data, deliberately the leanest template (no fitment, no variant picker, no persistent bar, no Showroom Finder).
6. ✅ Config-variant template wired — `prototypes/config-variant/index.html`. Comparison-card toggle (not a dropdown) for Assembled vs Flat Pack; confirmed the real plank-count discrepancy (4 vs 5) live.
7. ✅ Vehicle-specific template wired and iterated — `prototypes/vehicle-specific/`. See Section 9 for the full history (3 options explored, Option A selected, several rounds of client feedback applied).
8. ✅ Sibling/color template wired — `prototypes/sibling-color/index.html`. The "fresh scraping" this step flagged as a prerequisite is done: all 13 MaxTrax MKII colors scraped (`data/raw/MAXTRAX_MKII_family.json`), swatch picker live-swaps gallery/price/stock/SKU/Colour spec per color.

## 7. Known Data Gaps

- ✅ **Resolved:** Sibling/color family fully scraped — all 13 MaxTrax MKII colors, not just Black (`data/raw/MAXTRAX_MKII_family.json`).
- Plank-count discrepancy (4 vs 5) on the Pioneer 6 Platform pair is unconfirmed — treat as variant-specific data until the client confirms which is correct/intentional. Still open.
- **New:** Titanium Grey (MaxTrax) has no scraped discount ($319 flat vs. $319→$299 on all 12 other colors) — needs a client check: genuine pricing difference or a missed sale flag on that one listing.
- ✅ **Resolved (2026-09-09):** Config-variant (RH62112) and Vehicle-specific (GP01M1TZZ) cross-sell carousels are now populated. Neither live PDP carries an on-page "related products" module (confirmed live), so these four-item grids were curated by searching the live catalogue for accessories that explicitly list Rhino Rack Pioneer Platform / this kit's compatibility (accessory bar, tie-down kits, light-bar bracket, awning brackets, MAXTRAX recovery boards) — real products, real prices/images, but sourced by curation rather than lifted from an existing on-page module. Flag to the client: this is a different data-provenance than the Simple/Sibling-color related-products grids, which were scraped directly from a real on-page carousel.
- **New:** The MaxTrax Black listing's extra thumbnail images are real scraped URLs (filenamed per-SKU) but show generic lifestyle/action photography of an orange board — the live site appears to reuse the same action shots across color listings regardless of filename. Worth flagging to the client.
- ✅ **Resolved (2026-09-10):** Brand logo assets — client supplied real logo files for all three brands currently in the reference catalogue: `prototypes/_shared/brand-rhino-rack.webp`, `brand-front-runner.webp`, `brand-maxtrax.webp`. No gap remains for these three; a new brand entering the catalogue would need its logo added the same way.
- **New:** Vehicle-specific package-item SKUs (Platform `RH62100`, Backbone `RTHB1`, Tracks `RTS556`, shown in the new "What's Included" list) were reconstructed from the free-text long description in `GP01M1TZZ.json`, not independently scraped/confirmed against their own listings — worth a client sanity check before this is treated as authoritative, same caution as the plank-count discrepancy above.
- **New:** Ex-demo/factory-seconds pricing and special-order messaging are currently **demo-preview-only** — toggled from the admin panel with placeholder prices/stores, not backed by any real data source. Before either leaves prototype stage, the client needs to define: which SKUs currently carry ex-demo/factory-second stock, at what price/condition/store, and which SKUs should show as special-order vs. simply out-of-stock.
- **New (2026-09-10):** Vehicle-specific's new Flat Pack variant (`GP01M1TZZF`, $1,552.00) is fabricated demo data — no real Flat Pack listing exists yet for this exact Hilux kit. Modelled on the real RH62112/RH62112F price ratio. Needs a real SKU and price from the client before the config-variant picker leaves prototype stage.
- **New (2026-09-10):** The new grouped/bundle template uses a real live product (Yakima RoadShower 15L Complete Shower & Hose Bundle, `8004109PROMO`) found via the site's own search, since the client's promised example SKU never arrived before this was built. Needs client confirmation that this stand-in is acceptable, or their intended example to rebuild against. It also has no real Yakima brand-logo file (the other three brands were client-supplied) — currently a text wordmark placeholder.
- ✅ **Resolved (2026-09-10):** This SKU had no `data/raw/*.json` backing it at all — unlike the other four templates, its content had been hand-authored directly into the HTML rather than scraped. Created `data/raw/8004109PROMO.json` from a fresh live crawl. See the "Tab-content fidelity pass" entry in Section 9 for the full story — this surfaced alongside a much larger content-accuracy issue across all 5 products.
- ✅ **Resolved (2026-09-10, Graham Sowerby meeting):** MAXTRAX's brand logo rendered noticeably smaller than the other brands' due to `.brand-logo`'s fixed-height/auto-width sizing interacting badly with its squatter source aspect ratio — fixed via CSS (bounding-box sizing) without needing a new asset. See Section 5/9.
- **New (2026-09-10):** Vehicle-specific's What's Included row quantities (×1 for all three components) are a placeholder default, not confirmed real per-component counts — needs a client check before this leaves prototype stage, same caution as the package-item SKUs already flagged above. (Grouped-bundle's What's Included quantities are **not** a gap — they're real, taken directly from that page's own existing "Bundle Includes" list.)
- **New (2026-09-10):** The Gold Guarantee tab's "Learn more" link has no real destination yet (`href="#"`) — needs a real guarantee page/URL before this leaves prototype stage.
- ✅ **Resolved (2026-09-11, same day as the item above).** The Reviews tab (Section 10, item 19) now runs a real REVIEWS.io Polaris embed against RRG's actual `roof-racks-galore` account — no longer placeholder content. Remaining gap: none of the demo SKUs in this prototype have real reviews on the account yet, so every template's Reviews tab currently shows the widget's genuine "No reviews collected for this product yet" state rather than populated reviews — that's real REVIEWS.io behaviour, not a bug, and will resolve itself once the account accumulates reviews against these SKUs. ✅ **Also resolved (2026-09-11, same day):** the Decision Panel's star-rating badge is no longer a static placeholder either — no suitable off-the-shelf REVIEWS.io widget exists for that compact above-the-fold spot, so it's custom-built (`initReviewSummary()` in shared.js) against the same real `api.reviews.io/timeline/data` endpoint the Polaris widget itself calls. **Corrected same day (client feedback):** an initial version fell back to the store-wide rating when a SKU had zero product reviews — reverted, since a company-wide figure next to one specific product reads as a per-product rating and would mislead a customer on an individual PDP. It now simply hides the whole badge when there are no product reviews for that SKU yet, same as every demo SKU in this prototype today — same underlying data gap as the Reviews tab above, not a separate one. The full REVIEWS.io integration (both the Reviews tab and this badge) is now documented for handover in `DEVELOPER-BRIEF.md` (Section 11) — keep that doc in sync whenever either changes.
- **New (2026-09-11):** The new FAQ section (Section 10, item 20) is placeholder copy — plausible per-product questions, but not client-authored content. Needs real FAQ copy per product before this leaves prototype stage.
- **New (2026-09-11):** What's Included product-name links (Section 10, item 3) are dud `href="#"` placeholders in this prototype — no real destination on either template. When `DEVELOPER-BRIEF.md` documents this widget, it must specify that in the real Magento build each link goes to that component's own PDP (e.g. the Backbone/Tracks rows link to their own product pages), not stay as dead links. Flag for whenever the Developer Brief write-up for this widget is done — held along with everything else per the standing screenshot/final-pass rule (see the process note at the end of Section 10).
- **New (2026-09-12, Section 12 item 26):** the Fitted Photos Gallery's real fitment-photo count for GP01M1TZZ is 283, but only 16 real photo URLs were ever scraped (`data/raw/GP01M1TZZ.json`) — confirmed as a deliberate layout-work stub, per that file's own note, not a scrape failure. The "View All In-store Fitments" slide-out built this session (`FIT_GALLERY_PHOTOS` in `vehicle-specific/index.html`) cycles/repeats those 16 real photos to visually fill out the real count rather than showing a short 16-photo grid (Brenton's explicit choice). Extended same day once the slide-out grew a per-fitment detail view (matching the live site's own "Browse Fitment" popup): each detail view's 3 "other angle" thumbnails reuse other real photos from the same 16-photo set rather than genuine multi-angle shots of that specific fitment (no such data was ever scraped), and every fitment shows the same real vehicle/component data (Toyota Hilux N80, this page's own What's Included list) rather than the different vehicles/products the live reference's cross-product browser shows (Brenton's explicit choice — honest to what was actually scraped for this one SKU, not fabricated variety). Needs a real bulk fitment-photo pull, with genuine per-fitment vehicle/component/multi-angle data, from the client before this leaves prototype stage.

## 8. Research Sources (Section 0.1)

- Baymard Institute — Product Page UX 2026 benchmark (52%/62%/64% desktop/mobile/app "mediocre or worse" finding)
- Nielsen Norman Group — visual appearance as primary purchase-decision factor (93%)
- Spiegel Research Center — review-count impact on conversion (270% / 380% lift figures)
- Baymard Institute — total-cost and return-policy visibility gaps (67% / 44%)
- Blend Commerce — PerTronix CRO case study (fitment uncertainty as top structural conversion blocker in a fitment-driven vertical)
- PC Fitment / Standard Parts Toolkit — browsing-vs-buying analysis in automotive aftermarket ecommerce
- Various 2026 CRO/PDP guides (VWO, VNTANA, DigitalApplied, ConvertPolo) — mobile traffic share, sticky CTA and thumb-friendly variant selector guidance, "3-second confidence rule" framing

## 9. Build Status Log — read this first if picking up in a new session

**Last updated: 2026-09-10.** Framework decision made: plain HTML/CSS/JS (not a component framework) — fastest to iterate on visually, no build step.

### Version control
This project is a git repo pushed to **https://github.com/TonnieGit/RRG-Design-Staging.git** (`main` branch). Commits to this repo are authored as `brenton.cooley@gmail.com` (set as this repo's local `user.email`/`user.name`, not the machine's global git identity — don't change the global config to match).

**Workflow: push once a round of changes has been reviewed and approved, not after every individual edit.** Batch a session's changes into one commit (or a few logically-grouped commits) covering everything just signed off, with a message describing what changed and why, then push to `origin main`. Don't push mid-task or speculatively — only once the user is happy with the current state.

### Folder structure
```
PAGE-GLOSSARY.md                   — naming reference: every page section + widget by name
                                      (built 2026-09-10 after a "gallery" naming mix-up — see Section 9)
data/raw/                          — real scraped JSON per SKU/family (see below)
prototypes/index.html              — master index, links all four templates
prototypes/_shared/
  shared.css, shared.js            — the component library every template imports
  headerlogo.png                   — real site logo, used in the header
  installer.png                    — Get It Installed CTA photo
  sale-tag.png                     — "Spring Sale" corner-ribbon graphic
  brand-rhino-rack.webp            — brand logos, used on every template's decision panel
  brand-front-runner.webp
  brand-maxtrax.webp
prototypes/vehicle-specific/       — index.html (single template — see below, options B/C removed)
prototypes/config-variant/         — index.html
prototypes/sibling-color/          — index.html
prototypes/simple/                 — index.html
prototypes/grouped-bundle/         — index.html (new 2026-09-10 — Yakima RoadShower bundle)
```

### Scraped data on hand (`data/raw/`)
`FRWTAN063.json` (Water Tank), `RH62112.json` + `RH62112F.json` (Pioneer 6 Platform pair), `MAXTRAX_MKII_family.json` (all 13 colors), `GP01M1TZZ.json` (Hilux N80 kit, fitment gallery stubbed to 16 of 283 real images).

### Vehicle-specific — now a single template, not an options comparison
Three layout options were originally built and screenshotted for client review: **A — Fitment Confidence Command Center** (sidebar decision panel beside gallery), **B — Imagery-Led + Persistent Decision Bar** (full-width hero image), **C — Compact Confidence Bar**. Client feedback: **Option A selected**; B and C were kept archived for a while, then on 2026-09-10 the client asked to remove them entirely rather than keep them on disk — `option-b.html`, `option-c.html`, and the old options-comparison `index.html` were deleted, and `option-a.html` was renamed straight to `index.html` (same folder-per-template pattern as the other three). "Option A" framing/labels were scrubbed from the page since there's nothing left to disambiguate from. **This is now simply *the* vehicle-specific template — don't recreate an options-comparison structure unless the client explicitly asks to explore alternatives again.**

Feedback rounds applied directly to it (chronological):
- Delivery/Click & Collect rebuilt as a real tabbed widget (was a small button), placed in the sidebar directly under Add to Cart.
- Showroom Finder rebuilt as its own high-contrast "on display at N stores near you" CTA block, not a passive link — and its previous redundant sidebar link ("🏬 On display at 14 stores — find yours") was later removed since it duplicated the full-width block directly below it.
- Gallery thumbnails changed to flex/fill the full width of the main image, then later became the shared single-row scroll carousel (see below).
- Real site header + logo swapped in.
- Info tabs converted to a horizontal tab bar on desktop, vertical accordion kept on mobile.
- Get It Installed CTA added, initially as its own full-width banner (no real video existed for this SKU to pair it with) — client didn't like that as the *default* look, so it was changed to always show side-by-side with a video placeholder by default, matching Config-variant's layout. The full-width banner look is still available by toggling video off in the Demo State Panel.
- Package contents list ("What's Included") added — each kit component with its own SKU + copy button.

### Simple / Config-variant / Sibling-color — built directly, still not through a dedicated review round
These three were each built as a single design (not 2–3 options) — the client's explicit call, since the spec already treats them as lower-stakes than vehicle-specific. **They have not been through a template-specific feedback round the way vehicle-specific was**, though they've received every cross-cutting fix below (gallery carousel, brand logo, sale tag, copy buttons, demo panel, etc.). Each has its own "Design Rationale" section at the bottom of its page documenting decisions, spec grounding, and honest trade-offs — read those first.

### The Demo State Panel — the main new capability this session, understand this before further work
A floating "Demo State" button (bottom-right, every template) expands into a panel for **previewing how a page looks in different product states**, instead of hand-editing markup to check. Built entirely in `shared.js`/`shared.css` (`buildAdminPanel()`, `.admin-fab`/`.admin-panel`), driven generically off existing markup/classes so it works the same way on all four templates without per-page wiring. Toggles, all in one "Product State" section:
- **Has video** — pairs the product video with the Get It Installed CTA (`.install-media-row`); off collapses to a full-width panel. Shows a generic `.video-placeholder` on templates/variants with no real scraped `video_url`.
- **On sale** — swaps the displayed price between sale and regular, hides the discount badge, and shows/hides the sale-tag corner graphic in sync.
- **In stock** — disables Add to Cart with an "Out of stock" banner; restores correctly (respecting the active fitment-demo vehicle on Vehicle-specific, else defaulting to "Add To Cart").
- **Shipping available** / **Click & Collect available** — hide tabs/panels in the Delivery/Click & Collect widget, fall back to whichever stays on, show a note if both are off.
- **Special order item** — banner above Add to Cart; Add to Cart itself stays enabled (client's explicit call).
- **B-Stock / Ex-Demo available** — shows a CTA ("🏷 Ex Demo / Factory Seconds — from $X") that opens a modal with 3 placeholder options (Ex-Demo/Sellable Return/Factory Second, each with the product's main image, its own price, and a "View Item" button — currently inert, no link), priced live off whatever the page's current price is.

On **Vehicle-specific only**, the panel also has a "Session Vehicle" section — this absorbed the old standalone "REVIEWER DEMO" top bar that used to simulate the fitment-matching session vehicle (`initFitmentDemo`, now called from inside `buildAdminPanel` instead of auto-running on load).

Every "Product State" toggle **defaults to whatever the page's real data shows** (auto-detected at load via `detectInitial*State()` helpers) — e.g. "Has video" starts unchecked on the three templates with no real scraped video, checked on Config-variant's Assembled variant. Special order and B-Stock always default off, since no template has real data representing either state.

**A real bug was caught and fixed while building this**, worth knowing about since it's a pattern that could recur: Config-variant's `renderPrice()` didn't reset the was-price/badge's `hidden` state on every render, so toggling "on sale" off then switching variants left the wrong price showing. Fixed by having `renderPrice()` explicitly reset `hidden = false` on both before the panel's own `reapplySaleFlag()` (called at the end of `renderAll()`) can correctly re-hide them if the toggle is off. **Any future per-page render function must reset visibility of its real-data-driven elements on every call, not assume they start unhidden.**

### Cross-cutting fixes/additions (now affect all four templates via the shared library)
- Real header (utility bar: nearest store / vehicle / call us / country / logged-in user; main nav: logo, Products/Store Finder/Fit My Vehicle/Catalogue/Services, search, cart) matching the client-provided "Navbar - Logged In - Final.png", plus the real `headerlogo.png` in place of the text wordmark.
- Gallery: main image now 3:2 (was 4:3); thumbnails now a single-row horizontal-scroll carousel with prev/next nav (`initGalleryCarousels()`) instead of wrapping to a second row once there are 5+ images — this was causing a visible height mismatch against the shorter sidebar column.
- Related-product cards use real scraped images (Simple and Sibling-color only — Config-variant and Vehicle-specific still need this, see Section 7).
- Info tabs: fixed a real bug where the original CSS accordion could show multiple tab panels open simultaneously (a general sibling selector over-matched); now radio-button-driven so exactly one panel is ever open, on both the desktop tab bar and the mobile accordion.
- Brand logo (now mandatory, client instruction) on every decision panel.
- Sale-tag corner graphic, wired to the same on-sale state as price.
- Working copy-to-clipboard on every SKU copy button (`initCopyButtons()` — was decorative before).
- Get It Installed CTA image+text panel (see Section 5), Special order banner, and Ex-Demo/Factory Seconds CTA + modal — all Demo State Panel-toggled previews.

### Client review meeting, 2026-09-10 (Scott Childs & Jake/RRG Web Products) — outcome and confirmed action items
Brenton demoed this project's config-variant prototype live; both stakeholders endorsed this direction over the client's separate in-house Figma redesign (see Section 0 note — that Figma work is now superseded, not a design target anymore). Confirmed action items from this meeting, in build-priority order:

**All 7 items below were completed 2026-09-10, same day as the meeting.**

1. ✅ **Fix Get It Installed/video placement on vehicle-specific** — moved `.install-media-row` inside the gallery column div (was a separate full-width `<section>` after `.hero` closed), so it now sits directly under the secondary image thumbnails, constrained to the image-column width, matching config-variant exactly.
2. ✅ **Add payment-plan badges** (Afterpay/Zip/PayPal Pay in 4) under Add to Cart — new `.payment-badges` component in shared.css (CSS-drawn wordmarks, no logo assets on hand), on all five templates including the new grouped/bundle one.
3. ✅ **Lighten the fitment status badge styling** — `.fitment` in shared.css changed from a full tinted-fill + full-strength-border box to a white/near-white card with a 3px colored left accent only; label+dot keep the state color, body copy is neutral gray. Applies automatically to both the decision-panel fitment box and the persistent buying bar (same shared class). Persistent buying bar's structure/behavior otherwise unchanged.
4. ✅ **Add a config-variant picker to the vehicle-specific template** (Assembled/Flat Pack) — resolves Section 4 item 1. New `VEHICLE_PRODUCT` data object combines the existing `PRODUCT_FITMENT` with a `variants[]` array; picker drives SKU, price (decision panel + persistent bar), specifications list and the Fitting Instructions link live. **Flat Pack SKU (`GP01M1TZZF`) and pricing ($1,552/$1,753) are fabricated demo data**, modelled on the real RH62112/RH62112F price ratio — flagged in the page's own Design Rationale, needs a real SKU/price from the client before this leaves prototype stage.
5. ✅ **Build the paid "Fitted" option as a demo-panel-toggled preview** on config-variant and vehicle-specific — generic `reapplyFittedOption()`/`setFittedOptionFlag()`/`setFittedOptionMode()` in shared.js, gated on `.variant-picker` presence. Master on/off + **Mode 1** (full-width gold "Get It Fitted" card appended to the variant picker, survives real variant-switch re-renders) / **Mode 2** (upsell checkbox above Add to Cart) sub-toggle, defaulting to Mode 1. Demo-only, not wired to real checkout/booking — store-ops/state-manager sign-off is still pending.
6. ⚠️ **Prototype gallery placement A/B** — generic `applyGalleryPlacement()` in shared.js moved the real gallery DOM node (`#galleryColumn`) between its normal spot and a slot further down the page (`#galleryPushedSlot`), toggled via a new Demo State Panel "Gallery Placement" radio. Built on vehicle-specific (pushed slot = below Showroom Finder, as named) and config-variant (pushed slot = below trust icons, since it has no Showroom Finder section). Hero collapses to a single centered decision-panel column (max-width 560px) in the pushed state; fitment status stays correctly adjacent to price/Add to Cart in both layouts. **⚠️ Corrected 2026-09-10 (same day, later session):** this was built on a misreading — the client meant the *Fitment Gallery* ("See it fitted to a [vehicle]" installation photos), not the Main Product Gallery. See the "Fitted Photos Gallery" follow-up entry below for the fix; the Main Product Gallery no longer moves at all, and the (now-inapplicable) toggle was removed from config-variant entirely.
7. ✅ **New grouped/bundle template** — built at `prototypes/grouped-bundle/index.html`: Simple's layout + vehicle-specific's de-emphasized "What's Included" box. The client's promised example SKU never arrived, so rather than block on it, used a real live product matching their own "Roadshower bundle" example — **Yakima RoadShower 15L Complete Shower & Hose Bundle (`8004109PROMO`)**, found via the site's own search: real price ($846→$449), real 4-component breakdown with real SKUs (prices sum exactly to $449), real description/specs, curated related products. **Flagged in the page's Design Rationale:** confirm this SKU is acceptable or supply the intended one; no real Yakima logo file exists yet (text wordmark fallback, new `.brand-logo-text` class). Added as a 5th card on `prototypes/index.html`.
8. Open, not yet scoped by the client: whether Get It Installed should roll out to Simple/Sibling-color as brand advertising even where literal fitting doesn't strictly apply (water tank, MaxTrax) — no firm answer reached, revisit later.

**Explicitly confirmed as already correct, no change needed:** current on-fold decluttering (already well ahead of the old Figma baseline — no further stripping needed), the What's Included box's current de-emphasized styling, the Ex-Demo/B-Stock CTA's current subtle-button styling (the client's original complaint was about the old Figma's overbearing red button, not this build), and the Assembled/Flat Pack wording (client is aware of the "does this mean fitted?" ambiguity risk but doesn't want it touched yet).

**Out of this project's scope, but flagged as next up:** the PLP (category grid/list view) needs the same simplification pass — the client's own in-house PLP attempt was called out as cluttered next to the new PDP direction. Client plans to pick this up "early next week" with the same external UX reviewers who validated this PDP direction. No action here until that project starts.

**Staging deployment:** client wants this pushed to a Vercel staging environment once items 1–7 above are done, and has asked for help with the deployment itself — flagged for later the same day (2026-09-10), not yet done.

### Client review meeting, 2026-09-10 (Graham Sowerby) — reopened decisions + new action items

Same day as the Scott Childs/Jake meeting above, separate call. Brenton walked Graham through the same live prototype. Outcome was broadly a strong endorsement of the direction ("it's very much the right direction," "well done, mate"), but this meeting **reopened a few of the Scott/Jake meeting's "confirmed correct" items** and surfaced a genuine gap (breadcrumbs) that hadn't been caught yet. Where the two meetings' guidance conflicts, **build to this meeting's resolution — it came later the same day.**

**Reversed from the Scott/Jake meeting:**
1. ✅ **Done 2026-09-10 (same day, follow-up session). Add to Cart colour: gold, not red.** Scott/Jake's meeting said "red stays for now, revisit later." Graham asked to see the existing red/gold demo toggle live, then said to lock in gold. Gold is now the shipped default, not a preview state — the demo toggle was removed entirely. See Section 5.
2. ✅ **Done 2026-09-10 (same day, follow-up session). B-stock/Ex-demo CTA: inline text on the stock-status line, not a button at all.** Scott/Jake's meeting called the current subtle-button treatment "fine, no change needed." Graham's meeting reopened it (Tim's likely reaction: "people are going to miss that and I want to sell my B-stock") and landed on a different pattern after iterating past a "3 button-weight variations" idea: appended to the existing in-stock line — `In Stock — Ex-Demo/Factory Seconds from $1,495` — with the price portion a clickable blue link opening the existing modal, no separate button element. See Section 5.
3. ✅ **Done 2026-09-10 (follow-up session). Showroom Finder: the full-width black block is not settled.** Scott/Jake confirmed the "See It In Person" full-width black CTA as correct. Graham pushed back live — "it doesn't scream to me that this is a showroom... doesn't shout local location, come and visit, get in a car." Landed on two changes, both built: a small "On Display" pill next to the stock-status line (always visible, no interaction required) as the primary fix, plus an experimental interactive map (Leaflet + OpenStreetMap, postcode-area pins) piloted on Vehicle-Specific only, toggled in place of the block. See Section 5 and the Click & Collect rework below.

**New build items from this meeting:**

4. ✅ **Breadcrumbs — turned out to already exist; fixed to match Graham's structure, 2026-09-10.** At the time of this meeting, a `grep -r breadcrumb prototypes/` had (wrongly) confirmed no breadcrumb markup existed anywhere, since the real class name is `.rrg-crumbs`, not the literal word "breadcrumb" — a false negative caught later the same day. Graham's proposed structure, walking the real live site's pattern: **Vehicle Landing Page (e.g. "Ford Ranger, Bare Roof") → Category for that vehicle (e.g. Roof Racks) → current product.** Vehicle-specific's existing trail was missing the vehicle level (had Home → Category → Product) — fixed to Home → Toyota Hilux N80 → Platforms & Trays → [product], plus a pre-existing `&`-escaping bug on the same line. See Section 5. A related but explicitly **not-committed** idea from the same discussion: if a shopper without a saved vehicle arrives at a rack-set PDP having just browsed/searched a specific vehicle, ideally the session should recognize that implied vehicle context and offer to confirm it — Graham's response was "that's kind of fine... we can always implement that at any point," i.e. noted, not scheduled.
5. ✅ **Done 2026-09-10 (follow-up session). Click & Collect widget, fuller rework than the Scott/Jake meeting's "add a View All Stock button" ask** (that ask is still in here, just expanded):
   - ✅ Single postcode field synced across the Delivery and Click & Collect tabs (was two separate inputs).
   - ✅ **Results show by default, no click required.** (Note: "pre-filled from the customer's known postcode" is simulated with a static demo postcode, since there's no real customer session to read from at prototype stage — same caution as the rest of this widget's data.) Top stores shown, capped ~100km radius per the copy line under the results (demo distances already fall well under that cap, so no filtering logic was needed to enforce it).
   - ✅ Each store row (both the inline view and the new full slide-out) has a status pill: In Stock / Order In — Xd. (On Display is used by the Showroom Finder's own rows, not Click & Collect's — the two widgets style similarly but list different things.)
   - ✅ "View all stores" is link text under the results, opening a **newly built** slide-out drawer — turned out no such component existed anywhere in the prototype despite earlier docs referencing an "existing" one (that description was of the real live site, confirmed by full-repo search before building).
   - ✅ Default active tab flips to **Click & Collect** (reasoning, both meetings: stores benefit more from foot traffic/GP than 3rd-party shipping, and web-to-store conversion is harder to track but not a bad trade).
   - Explicit "both currently unavailable for this item" message state — already existed (`.dc-unavailable-note`) and needed no changes; verified it still reads correctly with the new default tab.
   - ✅ Small subtle icons next to the "Delivery" / "Click & Collect" tab labels (same restraint as the in-stock checkmark — "shouldn't stand out too much on the product page, more on the cart").
6. ✅ **Done 2026-09-10.** Get It Installed CTA now has a Fitment Gallery on/off Demo State Panel toggle, independent of the existing "Has video" toggle — see Section 5.
7. ✅ **Done 2026-09-10.** Important Vehicle Fit Notes — see Section 5. New content block, vehicle-specific only.
8. ✅ **Done 2026-09-10 (scope expanded to all 3 fitment instances + real asset/copy, per client direction the same day).** Rack Fit Guarantee — see Section 5. Fitment-relevant products only.
9. ✅ **Done 2026-09-10.** Specifications tab as a real table, Gold Guarantee tab condensed — see Section 5.
10. ✅ **Done 2026-09-10 (corrected same day — Grouped-bundle was missed on the first pass).** SKU copy UX simplified — dropped the separate copy-icon buttons, the SKU text itself (`.sku-copy`) is now the clickable-to-copy target, on the main SKU row (all 5 templates) and every What's Included row (**Vehicle-specific and Grouped-bundle both** — first pass wrongly treated Grouped-bundle's list as a different, out-of-scope component; client caught it, both templates now match). `initCopyButtons()` in shared.js was rewritten in the same pass — the old version cached the "restore" text once at page load, which would have reverted a re-rendered SKU (after a variant/colour switch) back to whatever showed on first load; it now captures the current value fresh on every click.
11. ✅ **Done 2026-09-10.** Short description 2-line clamp + "Read more" link to the Details tab — turned out to target `.value-prop` (the short line under the title), not the longer `.short-desc` paragraph near the tabs, per this item's own original wording ("current **value-prop line** has no clamp/read-more logic"). New `initTabJumpLinks()` in shared.js, since the tabs accordion is pure CSS (radio + label + sibling selector, no JS) — a plain anchor jump wouldn't have checked the tab's radio input.
12. ✅ **Done 2026-09-10.** Fitment status microcopy: dropped "kit," now "this product." The "doesn't fit" state's `detail` is now a function of the session vehicle (not a static string) so it can include the customer's full saved vehicle spec (body style, roof type, year) — `DEMO_VEHICLES` in shared.js gained fabricated `body_style`/`roof_type` fields for both demo vehicles to support this (no real per-customer vehicle data source exists yet, same caution as the rest of this file). CTA/action wording ("Find The Right Kit" → "Find The Right Fit") updated to match.
13. ✅ **Done 2026-09-10.** MAXTRAX logo — root cause confirmed: MAXTRAX's source file is a squatter canvas (148×39, ~3.8:1) than Rhino Rack/Yakima's (~6.3:1), so the old fixed-height/auto-width `.brand-logo` rendered it ~40% narrower even though the height matched. Fixed by sizing against a bounding box instead (`max-width:160px;max-height:32px`, width/height auto) so every brand fills a similar visual footprint regardless of its own aspect ratio — no new asset needed. Front Runner shares MAXTRAX's exact aspect ratio and gets the same improvement on Simple.
14. ✅ **Done 2026-09-10.** Mobile layout: decision panel now renders above the video/Get It Installed row. `.gallery-col`'s children become independent `.hero` grid items via `display:contents` (mobile only) so each can be ordered independently — gallery images/thumbs, then decision panel, then the video/install row — without moving any markup. Install row's mobile top margin bumped from 12px to 22px per the "~10px more spacing" ask. **Bonus fix found while testing this at narrow widths (2026-09-10, not part of the original ask):** the payment-badges instalment text (`.pb-text`, e.g. "4 payments of $473.27") had `white-space:nowrap`, which couldn't shrink to fit 3 badges side by side at ~390px and forced the whole page to horizontally scroll on all 5 templates — removed, now wraps to 2 lines when tight, no visual change at desktop widths.
15. **Prototype-tooling only, not a template change:** the hamburger menu in the prototype's own header should drop down to jump directly between the 5 product templates, for review convenience — currently the only way to switch templates is back to the index page.

**Process/naming notes, not build items:**
- This bundle product type (the grouped/bundle template) is internally called a **"Vehicle Product Set,"** not "rack set" — naming correction for docs/glossary going forward.
- Tim's standing rule, reconfirmed: flag any region-specific copy claims (e.g. "Australia's Largest," already in the trust-icon content in Section 2) in scope docs so they don't silently ship if the build is ever reused for NZ/UK — a documentation habit, not a template change.
- Cart-level display must clearly group bundle components under the parent product name/SKU at checkout — flagged risk is a customer adding what looks like one product and being confused when 2–3 items arrive. This is a cart/checkout requirement outside this project's page-level scope, but worth carrying forward to whoever owns that build.
- Magento sibling-group data model needs a new field for per-product-in-group explainer text (e.g. "Assembled"/"Flat Pack"), **as a dropdown of standardized values, not a free-text field** — Jake's ask, to avoid the same concept being worded differently across different sibling groups. Graham/Jake to action on the Magento/Rackit side, not a prototype change.
- Confirmed feasible but not committed: deep-linking the "change vehicle" CTA to the equivalent product for the customer's actual saved vehicle where one exists (via the bar-range data), rather than a generic fit-my-vehicle results page. Graham: "we could definitely do it... whether we do it at this point is another question."
- Recently Viewed reconfirmed **not** to belong in the decision panel — if built at all, it's the very last section at the bottom of the page, separate from Related Products/cross-sell.
- Brenton's target: get all of the above done by lunch 2026-09-11 before handing off to "Mark" for further build/review.

### Follow-up session, 2026-09-10 (same day, after the meeting write-up above) — naming glossary, Fitted Photos Gallery fixes & redesign, Body Tabs cleanup

**Naming glossary (`PAGE-GLOSSARY.md`, project root):** Built after a real mix-up — when the client said "gallery" they meant the Fitment Gallery (real installation photos), not the Main Product Gallery, and the two had gotten confused mid-conversation. The glossary now names every section and widget across all 5 templates so future sessions/conversations can disambiguate on first ask. It documents a specific naming clash worth remembering: **"gallery" can mean the Main Product Gallery, the Fitted Photos Gallery Placement toggle, or the Fitment Gallery itself** — always use the specific name. Keep this file updated whenever a section/widget is added or renamed.

**Fitted Photos Gallery Placement toggle — fixed:** Confirmed the bug described in item 6's correction above. `applyFitGalleryPlacement()` in shared.js now moves `#fitGallerySection` (the Fitment Gallery) between `#fitGalleryHomeSlot` (default, full width above the tabs) and `#fitGalleryNestedSlot` (inside the gallery column, under the main gallery/thumbs/install row) — new `.fit-gallery-section.nested` CSS gives it a compact layout when nested. The Main Product Gallery itself no longer moves under any toggle. Vehicle-specific only (config-variant's copy of the old toggle was removed, since it has no Fitment Gallery).

**Fitment Gallery redesigned as a "Fitment Gallery" panel widget:** Client supplied a live-site/Figma reference (icon badge + heading, "View All In-store Fitments (N)" link, "Explore Fitting Costs" button, photo carousel with nav arrows, page-dot pagination, red background). Rebuilt to match, then iterated per client feedback across several rounds the same day:
- New `.fit-gallery-panel` component in shared.css — all colours driven by `--fg-*` custom properties so the red/non-red toggle (`.red` modifier) and both placements share one rule set.
- New `initFitGalleryCarousel()` in shared.js — real scroll-snap carousel, working prev/next nav (mirrors `initGalleryCarousels()`'s overflow-detection convention), and click-to-jump/scroll-synced page dots.
- **Red background toggle**, new Demo State Panel checkbox under "Fitted Photos Gallery" (`applyFitGalleryRedFlag()`) — **defaults to non-red** (client's new preferred direction); flip it on to preview the live-site red for stakeholder-pushback conversations.
- **Photos per view:** 7 in the full-width placement, 4 when nested (client's choice, favoring showing more photos over just stretching proportions; bumped up from an initial 2 when nested per follow-up feedback).
- Heading copy: "Gallery" → **"Fitment Gallery"** (plain "Gallery" read as ambiguous against the Main Product Gallery).
- Removed the "Explore Fitting Costs" CTA button entirely (duplicated the Get It Installed CTA directly above it); "View All In-store Fitments (283)" is now right-aligned in its place.
- Photo tiles changed from portrait-leaning `aspect-ratio:4/3` to landscape `16/9` (matches the site's product-video aspect-ratio convention) — also usefully shortens the widget's overall height, most noticeable in the nested placement.
- **Section heading typography standardized:** client asked that "Fitment Gallery," "Related Products," and "See It In Person" (Showroom Finder) all share one heading style. Added a shared rule in shared.css (`.related-heading, .showroom-widget h3, .fit-gallery-title h2`) — Barlow Condensed Bold Italic, uppercase — and added the italic font weight to the Google Fonts `@import` (previously only regular weights were loaded, so italic would have silently fallen back to a browser-synthesized oblique). Size was tuned twice: first pass at 22px, then bumped to **37px** after the client clarified they wanted the heading's actual rendered glyph height (cap-height, not the CSS line-box) to visually match the 26px gold icon badge — confirmed via canvas `measureText().actualBoundingBoxAscent` (Barlow Condensed Bold Italic's cap-height is only ~73% of its font-size, so 22px was reading far smaller than the badge). Client confirmed all three headings should scale together rather than sizing Fitment Gallery on its own.

**Body Tabs section (`.body-cols`) widened to full width:** All 5 templates had a small `<aside>` sidebar (one-line cross-sell blurb, wording varied per template — "Related Kits for [Vehicle]" / "Related Products" / "Also Fits Your Setup" / etc.) taking the right third of a 2fr/1fr grid next to the Tabs widget. Client felt it added little and made the tabs look oddly narrow. Removed the `<aside>` entirely on all 5 templates and changed `.body-cols` to a plain block — the Tabs widget (and the Related Products grid below it, unaffected either way) now runs the full page width.

All of the above verified visually/functionally with Playwright (placement × red-toggle combinations, nav-arrow paging, dot sync, heading sizing) on vehicle-specific, plus the Body Tabs fix spot-checked on Simple.

**Bug caught same day, worth knowing about:** the heading-size fix above only visibly took effect in the full-width placement at first — a leftover `.fit-gallery-section.nested h2{font-size:16px}` rule from the pre-redesign version of this widget (never cleaned up when the panel was rebuilt) had higher CSS specificity than the new shared heading rule, so it silently won when nested. Removed that whole dead block (it also referenced `.sub`/`.fit-gallery-grid`/`.fit-gallery-more`, none of which exist in the current markup). **Lesson: when a component gets rebuilt, grep for the old class names afterward** — a stale rule with higher specificity can override new shared styling invisibly rather than erroring.

### Showroom Finder rolled out to all 5 templates (client ask, 2026-09-10)
Previously Vehicle-Specific only. Added the same `#showroom` / `.showroom-widget` section (postcode entry, demo results for "Moorebank, NSW" / "Castle Hill, NSW") to Simple, Config-Variant, Sibling-Color, and Grouped-Bundle, placed directly below the Trust Row and above the Short Description on each — same relative position as Vehicle-Specific's (below the hero, its own section, not inside the Decision Panel). Copy/store-count per template is placeholder demo content, same fabricated-data caveat as Vehicle-Specific's original.

**Corrected same day:** Vehicle-Specific's own Showroom Finder section was left in its pre-existing spot (between Package Contents and the Fitment Gallery, i.e. *above* the Trust Row) instead of being moved to match the other four templates. Client caught this — moved to sit directly below the Trust Row on Vehicle-Specific too, so all 5 templates now share the same relative order: Fitment Gallery (Vehicle-Specific only) → Trust Row → Showroom Finder → Short Description (where present) / Body Tabs.

### Add to Cart colour preview toggle (2026-09-10)
New Demo State Panel checkbox, "Gold Add to Cart button (colour preview)," under Product State on all 5 templates — `applyCtaColorFlag()` in shared.js adds/removes a `.cta-gold` class on every `[data-cta-label]` button on the page (decision panel, sticky mobile bar, and vehicle-specific's persistent bar all update together), swapping the Add to Cart button to `#FFCA48` with black text. Off by default (current red stays the shipped default); demo-preview only, same status as the other colour/placement toggles.

### Get It Installed CTA + video widget rolled out to all 5 templates (client ask, 2026-09-10)
Previously Config-Variant, Vehicle-Specific, and Grouped-Bundle only (Section 6, step 7's build note). Simple and Sibling-Color had only a bare `<img class="video-placeholder" ... data-admin-video hidden>` with no Get It Installed panel next to it — replaced on both with the full `.install-media-row` pairing (video placeholder + `.install-cta-panel` installer photo/REVIEWS.io badge/"More Information + Bookings" button), identical markup to Grouped-Bundle's. No shared.js/CSS changes were needed — `detectInitialVideoState()`/`applyVideoFlag()` and the `.install-media-row`/`.install-media-row.no-video` CSS already worked generically off the `.install-media-row` class, so the Demo State Panel's "Has video" toggle now drives Simple and Sibling-Color the same way it already drove the other three. `PAGE-GLOSSARY.md` updated accordingly.

Added a new Demo State Panel toggle, "On display in-store (Showroom Finder)," under Product State — defaults checked (on) since the section is now standard, unlike the other Product State toggles which default off. `applyShowroomFlag()` in shared.js hides/shows `#showroom`; the panel only renders the toggle when `#showroom` exists (`hasShowroom` check, same conditional-rendering convention as the other feature toggles), so this stays safe if a future template omits it. Stale "no Showroom Finder" claims in Simple's and Grouped-Bundle's Design Rationale sections were corrected to match. `PAGE-GLOSSARY.md` updated accordingly.

### Add to Cart gold lock-in + B-stock inline text (2026-09-10, follow-up session)
Picked up the two quick reversals from the Graham Sowerby meeting's list (Next steps items 1 above superseded by this entry for these two specifically):
- **Add to Cart gold, locked in as the real default.** Removed the "Gold Add to Cart button (colour preview)" Demo State Panel checkbox and `applyCtaColorFlag()`/`.cta-gold` entirely — no longer an open question, so no toggle needed. `shared.css` now styles `[data-cta-label].btn-primary` gold (`#FFCA48` background, black text) by default; other `.btn-primary` buttons that aren't Add to Cart (e.g. vehicle-specific's "Find Nearest Showroom") were unaffected since the rule is scoped to the `[data-cta-label]` attribute, not `.btn-primary` generally.
- **B-stock/Ex-demo moved into the stock-status line.** Replaced the standalone `.exdemo-cta` button (previously inserted after `.price-block`) with inline text appended directly to `.stock-status-line` — e.g. `In Stock — Ex-Demo/Factory Seconds from $1,495` — where the price is an underlined blue `.exdemo-inline-link` that opens the existing ex-demo modal, no separate button element. New `renderStockLine()` in shared.js is now the single source of truth for that line's content (base stock-status text + the exdemo suffix when the flag is on); both `applyStockStatus()` and `applyExdemoFlag()` call it so the two states compose correctly regardless of toggle order. The modal itself, its 3 condition/price options, and its "still needs real inventory data" caveat are all unchanged. **Follow-up same day:** fixed a copy typo ("X-Demo" → "Ex-Demo") and changed `.exdemo-inline-link`'s colour from red to blue (`#1A56DB`), per client feedback.

Not yet verified live in-browser this session (Playwright was mid-use elsewhere and the profile was locked) — verify visually before considering this fully closed out, though `node --check` confirmed shared.js parses cleanly and a full-file review of the touched sections found no leftover references to the removed classes/toggle.

### Showroom Finder + Click & Collect rework (2026-09-10, planned then built in one session)

Backlog item #3 (Graham Sowerby meeting). Planned via EnterPlanMode with Brenton first — three open design calls were resolved before building: (1) build a real store slide-out drawer rather than stub the "View all stores" link, since exploration confirmed no such component existed anywhere in the prototype despite spec.md previously describing one as "existing" (that description was of the real live site); (2) give the "On Display" pill its own colour rather than reusing the green "In Stock" pill; (3) pilot the interactive map on Vehicle-Specific only rather than all 5 templates immediately.

**Click & Collect / Delivery widget** (`shared.css`/`shared.js`, all 5 templates):
- Merged the two separate postcode inputs (`#deliveryPostcode`, `#collectPostcode`) into one `#dcPostcode` field sitting in a new `.dc-postcode-row` above both tab panels, plus a "Update" button and `.dc-postcode-note` that mirrors back whatever postcode is typed (`initDcPostcode()`) — a light demo re-trigger, not real geocoding, consistent with the rest of this widget's data.
- Removed the `hidden` attribute that previously gated both panels' results behind a click — both now show their demo results on load.
- Flipped the default active tab from Delivery to Click & Collect across all 5 templates' markup (no JS logic changes needed — `applyAvailabilityFlags()`'s existing fallback already handled either tab being active).
- Added small inline-SVG icons (truck / pin) to the `.dc-tab` buttons.
- Added a "Showing stores within 100km — View all stores" line under the Click & Collect results (`.dc-viewall-row`, blue `.dc-viewall` link, same blue as the Ex-Demo inline link for visual consistency).
- The existing "both unavailable" message state (`.dc-unavailable-note`) needed no changes.

**Store slide-out** (new component): `buildStoreSlideout()` in shared.js builds a right-edge drawer (`.store-slideout`/`.store-slideout-backdrop`, same modal-backdrop-once-per-page convention as the Ex-Demo modal but sliding rather than centered) listing each template's `window.STORE_NETWORK` array (6 stores per template, reusing store names already seen elsewhere in the project's demo data rather than inventing new ones) — more than the 2 shown inline. Opened by the new "View all stores" link.

**"On Display" pill** (all 5 templates): new `.stock-line-row` wraps `.stock-status-line` plus a new `<span class="stock-chip display" data-display-pill>` sibling — always visible next to price/stock status, not only inside the black Showroom block. New `.stock-chip.display` CSS variant (gold/bronze, `--rrg-display`/`--rrg-display-bg` tokens) distinct from the green "In Stock" pill; the Showroom widget's own internal pill was changed from reusing `.stock-chip.in` to the same `.display` variant (bright gold on its black background) for one consistent colour language. Both pills are wired to the existing `applyShowroomFlag()` toggle, so the Demo State Panel's "On display in-store" checkbox updates both at once.

**Experimental interactive map** (Vehicle-Specific only, pilot): Leaflet 1.9.4 + OpenStreetMap tiles loaded via CDN (`<link>`/`<script>` in `<head>`, free, no API key). New `#showroomMap` container inside the Showroom widget, hidden by default; existing content wrapped in `#showroomDefaultView` so the two can swap. New `applyShowroomMapFlag()` in shared.js initializes the Leaflet map and drops pins from a new `SHOWROOM_MAP_STORES` array (hardcoded approximate lat/lng for the demo store names already in use) on first toggle, then just calls `invalidateSize()` on subsequent toggles (a known Leaflet gotcha — a map initialized inside a `hidden` container renders broken until told to remeasure). New conditional Demo State Panel section ("Showroom Finder (experimental)"), same `hasX`-conditional-rendering convention as the existing Fitted Photos Gallery Placement toggle, so it only appears where `#showroomMap` exists.

**Verified this session** (Playwright, browser lock from the previous session had cleared): gold Add to Cart + On Display pill render correctly on Vehicle-Specific and Simple; Click & Collect defaults to that tab with results visible and the postcode pre-filled; typing a postcode and switching tabs preserves the value; "View all stores" opens the slide-out with the full 6-store list; the interactive map toggle loads real OpenStreetMap tiles with correctly placed pins; toggling "On display in-store" off correctly hides the new pill (`[data-display-pill].hidden === true`); toggling Shipping + Click & Collect both off correctly shows the "both unavailable" note and hides the whole widget (postcode row included). No console errors besides an unrelated missing favicon.

**Not yet done:** map rollout beyond Vehicle-Specific (explicitly Brenton's call once reviewed), and this hasn't been through a client review pass yet — built and self-verified only.

### Showroom Finder + Click & Collect — follow-up correction round (2026-09-10, same day)

First client feedback pass on the rework above surfaced four real corrections, all built:

1. **"On Display" pill moved off the price line, onto individual store rows.** The pill beside `.stock-status-line` was the wrong concept — a store either has this product on display or it doesn't, it's a per-store fact, not a blanket product-level one. Removed `.stock-line-row`/`[data-display-pill]` and the price-line pill entirely (all 5 templates, plus its wiring out of `applyShowroomFlag()`). Store rows in the Click & Collect widget and the slide-out now show it alongside the existing In Stock/Order In pill where it applies (`rrgStorePillsHTML()` in shared.js) — e.g. Moorebank shows both `In Stock` and `On Display`.
2. **Postcode field starts empty**, not pre-filled — a prefilled value read as real customer data it wasn't. The "View all stores" line under Click & Collect's results now carries the message instead, and changes copy depending on state: `In stock and on display in {N} stores — View all stores` by default, `Showing stores within 100km of {postcode} — View all stores` once a postcode is entered and Update is clicked (`initDcPostcode()`, `.dc-viewall-line`). The old separate `.dc-postcode-note` line was dropped as redundant now that this line carries the message.
3. **Store slide-out rebuilt on real data.** The first version's 6 stores per template were fabricated placeholders. Navigated to a real product on the live site (a Yakima roof rack for a Mercedes C-Class), opened its Click & Collect card, clicked "View all Stock," and scraped its real Store Inventory slide-out via Playwright — 35 real stores across NSW/VIC/SA/TAS/QLD/ACT/WA with real names, addresses, phone numbers (`tel:` links) and Google Maps links. This is now `RRG_STORE_NETWORK` in shared.js — one canonical list (store identity doesn't vary by product), grouped by state to match the live site's own presentation, used by both the slide-out and the Click & Collect widget's inline rows on every template. Per-store In Stock/Order In status and which stores are "On Display" (`ON_DISPLAY_STORES`, currently Moorebank + Castle Hill) are still demo/placeholder — no real per-SKU per-store stock feed exists — flagged same as the rest of this widget's data. Each template's old fabricated `window.STORE_NETWORK` block was deleted.
4. **Interactive map: two fixes.**
   - **Z-index bug** — the map was rendering on top of the Demo State Panel. Cause: Leaflet's internal panes/controls default to z-index up to 1000, and neither Leaflet's container nor `.showroom-map` established a stacking context of their own, so those values competed directly against the page's own z-index scale (admin panel is 60/61) instead of being contained locally. Fixed with `.showroom-map{position:relative;z-index:0}`, which gives Leaflet's internals their own local stacking context — they can no longer escape above fixed page chrome regardless of their internal numbers.
   - **Layout corrected from a full swap to a split view.** The first version replaced the black block entirely when the map toggled on, losing the postcode entry and "Find Nearest Showroom" CTA. `applyShowroomMapFlag()` now toggles a `.split-view` class on `.showroom-widget` instead (`display:grid;grid-template-columns:1fr 1fr`, stacks on mobile) — `#showroomDefaultView` and `#showroomMap` render side by side, both visible. Also added a "View all stores" link under Vehicle-Specific's block content, wired to the same slide-out. Real postcode-driven radius search (typing a postcode and having the map itself re-center/filter to stores within it) is **not implemented** — no geocoding service is wired up, so the map shows a fixed demo view of the store cluster regardless of postcode input. Flagged here as the gap to close before this leaves pilot stage; `buildStoreSlideout()`'s trigger-handling was also generalized from a single `document.querySelector` to `document.querySelectorAll`, since there are now two "View all stores" triggers per page on Vehicle-Specific (Click & Collect's and the Showroom widget's).

**Verified this session** (Playwright): pill correctly absent from the price line and present on store rows (both In Stock + On Display shown together on Moorebank); postcode field empty on load; view-all line reads the default copy and correctly switches to the postcode-entered copy after Update; slide-out renders all real stores grouped by state with working `tel:`/map links; Demo State Panel now renders visually on top of the map (previously the reverse); split-view shows the black block and the map together with the "View all stores" link present. Spot-checked Simple in addition to Vehicle-Specific — consistent. No console errors.

### Showroom Finder + Click & Collect — second correction round (2026-09-10, same day)

Two more fixes from the next client review pass:

1. **Click & Collect no longer shows named stores by default.** Showing "Moorebank" / "Smeaton Grange" with no postcode entered implied a real nearest-store match that wasn't real — the store data was static regardless of what (if anything) was typed. `initDcPostcode()`'s `update()` now hides `#collectResults` (the named-store rows) whenever the postcode field is empty, and only reveals them once a postcode is entered and Update/Enter is triggered. The "In stock and on display in {N} stores — View all stores" line stays visible either way — that summary line was always meant to be true regardless of postcode, only the specific named rows implied a match that needed gating. Delivery's results (flat freight rates, not location-matched) are unaffected.
2. **Interactive map: full-bleed, 1:2 ratio.** The map previously sat inside the black block's own padding, reading as a smaller tile with black bars around it. `.showroom-widget.split-view` now sets `padding:0` + `overflow:hidden` on the widget itself, moves padding onto `#showroomDefaultView` only, and lets `.showroom-map` fill its grid cell edge to edge (`margin:0;height:auto`, corners squared off since the widget's own `overflow:hidden` clips to its rounded corners). Grid columns changed from `1fr 1fr` (50/50) to `1fr 2fr` — text column now roughly a third of the width, map two-thirds, per client ask ("show more of the actual map").

**Verified this session** (Playwright): typing nothing into the postcode field and loading the page shows no store rows, only the "In stock and on display in 35 stores — View all stores" line; entering a postcode and clicking Update correctly reveals the two named store rows; the map now renders flush to the widget's edges with no visible padding/border, at roughly a 1:2 text-to-map ratio. No console errors.

### Interactive map — locked in as default and rolled out to all 5 templates (2026-09-10, same day)

Client reviewed the map on Vehicle-Specific and liked it — two changes followed:

1. **Map locked in as the default, no longer a Demo State Panel toggle.** Removed the "Interactive map (instead of block)" checkbox entirely (markup, `adminState` field, switch case). `applyShowroomMapFlag(true)` is now called unconditionally from `buildAdminPanel()` — safe everywhere since the function no-ops if a page has no `#showroomMap` container. Vehicle-Specific's markup was also updated to start in `.split-view` state directly (was toggled in via JS only), so there's no layout jump before JS runs.
2. **Rolled out to all 5 templates** (previously Vehicle-Specific only, per the original pilot decision — extended after client sign-off in review, not silently). Each of Simple, Sibling-Color, Config-Variant, and Grouped-Bundle got the same treatment Vehicle-Specific has: Leaflet CDN tags in `<head>`, the Showroom Finder widget's existing content wrapped in `#showroomDefaultView` inside a `.showroom-widget.split-view`, a `#showroomMap` container, a `window.SHOWROOM_MAP_STORES` pin array (same 4 NSW stores — Moorebank/Castle Hill/Smeaton Grange/Silverwater — reused across all 5 templates, since the Showroom Finder's own static "On Display" demo copy was already identical across all 5), and the "View all stores" link into the store slide-out. `buildStoreSlideout()` already handled multiple triggers per page (from the earlier Click & Collect work), so no shared.js change was needed for the extra trigger.
3. **"View all stores" link coloured white, Showroom Finder widget only** (`.showroom-widget .dc-viewall{color:#fff}`) — the existing blue (`#1A56DB`) read poorly against the widget's black background; the same link in the Click & Collect card (white background) stays blue. Scoped via a descendant selector so it doesn't affect the Click & Collect widget's link.

**Verified this session** (Playwright, all 5 templates): map renders with real OpenStreetMap tiles and pins on Simple and Grouped-Bundle (spot-checked visually), config-variant and sibling-color confirmed via console (zero errors on all 5); "View all stores" now has two working triggers per page (Click & Collect card + Showroom Finder widget) both opening the same slide-out — confirmed by clicking the Showroom Finder's trigger specifically on Simple; computed link colour confirmed white inside `.showroom-widget` and unchanged blue inside the Click & Collect card on the same page.

### Delivery panel — same postcode-gating bug the Collect tab already had fixed (2026-09-10, same day)

Caught on a later pass: the Delivery tab's Standard/Express freight rates were still showing unconditionally regardless of postcode — the postcode-gating fix from the "second correction round" above only ever covered `#collectResults`, not `#deliveryResults`, so Delivery was left exactly where Collect had been before that fix. `initDcPostcode()`'s `update()` now hides `#deliveryResults` the same way (`hidden = !val`), and a new `.dc-postcode-prompt` ("Enter your postcode above to see delivery options.") fills the gap while it's hidden, mirroring the Collect panel always having its "In stock and on display in N stores" line visible — an empty panel with nothing in it would have read as broken. Markup change (`.dc-postcode-prompt` div added to the Delivery panel) applied identically across all 5 templates; the gating logic itself lives once in shared.js, so no other JS changes were needed.

**Verified this session** (Playwright): Delivery tab shows only the prompt with no postcode entered (screenshotted on Vehicle-Specific); entering a postcode and clicking Update hides the prompt and reveals the Standard/Express rates; spot-checked the same hidden-by-default behavior on Grouped-Bundle. No console errors on either.

### Next steps (in order)

**Superseded 2026-09-11:** a new backlog from a solo Brenton Cooley review session (see the dedicated section at the end of this file, "Next backlog — 2026-09-11") now supersedes several items below where they overlap — most notably the Get It Installed CTA (further restyling, REVIEWS.io badge removal) and Special Order (behavior changes from additive to replacing the stock-status label). The numbered list at the end of the file is the current forward backlog; items below stay as a historical record of what was already done.

**Superseded:** the old "7 confirmed build items, all done 2026-09-10" list above was from the Scott Childs/Jake meeting only. The same-day Graham Sowerby meeting reopened three of those items and added a longer list of its own — see that meeting's write-up above for full detail. The list below is the current combined backlog, in priority order, picking up where that write-up left off.

1. ✅ **Done 2026-09-10.** Breadcrumbs turned out to already exist on all 5 templates (`.rrg-crumbs`) — the "build from scratch" framing was based on a grep for the literal word "breadcrumb", which never appears in the code. Fixed the two real gaps once the actual markup was checked: Vehicle-specific was missing the Vehicle Landing Page level (now Home → Toyota Hilux N80 → Platforms & Trays → product) and had an `&`-escaping bug; Simple was missing its category level entirely (now Home → Camping & Offroad → product). Config-variant, Sibling-color, and Grouped-bundle already had a correct trail and weren't touched.
2. ✅ **Done 2026-09-10.** Reversed the two Scott/Jake calls the Graham meeting overturned: Add to Cart is now locked to gold as the real default (preview toggle removed), and the B-stock/Ex-demo button was replaced with the inline `In Stock — Ex-Demo/Factory Seconds from $X` text treatment (blue link).
3. ✅ **Done 2026-09-10.** Reworked Showroom Finder + Click & Collect together — "On Display" pill next to stock status, Click & Collect showing top stores by default with status pills, synced postcode field, default tab flipped to Click & Collect, "View all stores" opening a new store slide-out drawer, "both unavailable" message state (unchanged, already worked), small tab icons, and the experimental interactive map piloted on Vehicle-Specific (Leaflet + OpenStreetMap). Not yet verified against a live client review — built and self-tested (Playwright) only. Map not yet rolled out beyond the pilot template; that's Brenton's call once reviewed.
4. ✅ **Done 2026-09-10.** Get It Installed fitment-count copy + fallback state + its new Demo State Panel toggle; Important Vehicle Fit Notes block; Rack Fit Guarantee (real asset/copy, rolled out to all 3 fitment instances per client direction). See Section 5 and the Graham meeting write-up above for full detail.
5. ✅ **Done 2026-09-10.** The remainder of the Graham-meeting list: Specifications-as-table + condensed Gold Guarantee tab; SKU click-to-copy (drop the icon buttons); What's Included quantity column; short-description 2-line clamp + Read More; fitment microcopy ("this product," full vehicle spec in the no-fit state); MAXTRAX logo sizing fix; mobile decision-panel-above-video reorder. Plus one unrelated bug found and fixed while testing the mobile reorder at narrow widths: the payment-badges instalment text forced a page-wide horizontal scroll on all 5 templates (`.pb-text{white-space:nowrap}` removed). **Every item from the Graham Sowerby meeting backlog (spec.md §9) is now done** — remaining work is client sign-off / data-gap resolution (items below), not new builds.
6. **Get client sign-off on the redesigned Fitment Gallery panel** (red/non-red default, heading size, landscape photos, removed CTA button) — built off a client-supplied reference but not yet through a dedicated confirmation round of its own. "View All In-store Fitments (283)" is currently a dead `href="#"` link — needs a real destination (a lightbox? a full gallery page?) before this leaves prototype stage.
7. **Get client feedback on Simple and Sibling-color specifically** — these still haven't had a dedicated review pass the way vehicle-specific and config-variant have now effectively had. Also get feedback on the grouped/bundle template ("Vehicle Product Set" per the Graham meeting's naming correction), including sign-off on the stand-in Yakima RoadShower SKU used in place of the client's promised example.
8. Resolve the open data flags in Section 7 with the client (plank-count discrepancy, Titanium Grey pricing, MaxTrax Black lifestyle photos, vehicle-specific package-item SKUs, vehicle-specific's fabricated Flat Pack SKU/pricing, What's Included quantities, Gold Guarantee "Learn more" destination, and defining a real data source for ex-demo/special-order state before those leave prototype stage). Also source a real Yakima logo file for the grouped/bundle template (text wordmark stands in for now).
9. Once all templates are signed off, build the real standalone data-model/merge-script utility (Section 6, step 4) — currently each template just embeds its own merged JS object inline, which was fine for prototyping but isn't reference-grade for the Magento build.
10. Resolve the remaining Section 4 open questions (items 2 and 3) with the client before finalizing the data model.
11. Extend schema.org Product/Offer structured data to Config-variant, Sibling-color, and Vehicle-specific (only Simple and the new grouped/bundle template have it today).
12. Deploy to Vercel staging once the above is through another client review pass (client to request this explicitly).
13. **Not scheduled, just noted:** paid "Get It Fitted" needs a dedicated alignment call with Chris Roach, Jake Martin, and Tim before any further work past the current demo-toggle state; session-vehicle inference (recognizing an implied vehicle from browsing context even when unsaved) is a flagged idea, not committed.

### Tab-content fidelity pass (2026-09-10)

Brenton noticed the 5 products' Details/Specifications/Gold Guarantee/Shipping Info/Fitting Instructions tab content read as "wildly different" from each other and asked for a live-site crawl to check. Confirmed real, not a false alarm: **Simple (`FRWTAN063`) was an exact verbatim match to the live site throughout. The other four had Details-tab content that had been rewritten/condensed rather than carried over** — in Config-variant and Vehicle-specific this dropped entire real sections (accessory-range/Zwifloc paragraph and the closing fitment-tool CTA on RH62112; the whole "Key Features and Benefits" / "Typical Applications" bullet lists and an "About Rhino-Rack" company-history section on GP01M1TZZ). Grouped-bundle's Details tab was a 2-paragraph rewrite of what's actually 12+ paragraphs/bullet-lists on the live page. Gold Guarantee and Shipping Info were also condensed on all 5 templates — that one *was* a deliberate, client-approved decision from the Graham Sowerby meeting (see Section 5), not a fidelity bug, but Brenton asked to revert it too once the scope of the other issue became clear.

**Fixed, all 5 templates, by re-crawling each live product page directly** (Playwright, not WebFetch — the site 403s plain HTTP fetches, real content only loads through a real browser) and pulling verbatim text:
- **Details tab** now carries the full real copy for every product, restructured into semantic `<div class="details-sub"><h5>…</h5><ul>…</ul></div>` blocks matching the live page's own section headings (e.g. GP01M1TZZ now has its Platform/Backbone/Tracks/About-Rhino-Rack sections back; RH62112/RH62112F got per-variant `details_html` wired into config-variant's existing `renderAll()` swap mechanism, since the two variants' real copy is genuinely different, not shared, and the template had wrongly been treating it as shared).
- **Gold Guarantee and Shipping Info** confirmed identical site-wide boilerplate across all 5 products (spot-checked byte-for-byte across FRWTAN063/RH62112/GP01M1TZZ/MaxTrax Black/Yakima) — one canonical full-text version now used on all 5 templates, replacing the condensed 2-USP-line/short-summary versions.
- **Specifications** — already accurate on 4 of 5 templates (verified field-by-field against the live spec table). Grouped-bundle's was wrong in a different way: the live page's own Specs tab only shows SKU + Brand, with all the water-capacity/pressure/weight facts actually living inside the Details block's own "Specifications & Features" bullet list — the prototype's specs table had promoted those into fabricated-looking spec-table rows. Fixed to match the real tab structure: Specs tab now just SKU + Brand, full bullet list restored to Details.
- **Fitting Instructions** — GP01M1TZZ was missing 3 of its 4 real manual links (RTHB1, RTS556, Roof Racks Safety); only the main Platform Instructions link was wired up. All 4 now present with real hrefs.
- **CSS fix required:** the tabs' pure-CSS accordion (`prototypes/_shared/shared.css`) capped open-panel height at `max-height:2000px` on mobile (desktop already used `max-height:none`) — too small for the new full-length content, which would have silently clipped via `overflow:hidden`. Bumped to `30000px`. Verified no clipping on the longest tab (Vehicle-specific's Gold Guarantee, 1701px real height) at 390px mobile width via Playwright.
- **New:** `data/raw/8004109PROMO.json` created — this SKU never had a scraped JSON file at all (unlike the other four), its content had been authored straight into the HTML. Backfilled from the same live crawl; also fixed a related data-flag noted below.
- **`data/raw/*.json` updated to match** (the underlying scrape data was itself already condensed for RH62112 and GP01M1TZZ before the templates condensed it further) — these files are meant to be the source of truth, so they needed the same fix, not just the rendered HTML.

**Not changed:** the `.value-prop` micro-differentiator lines and `.short-desc` short paragraphs — those are deliberately-authored copy per the spec's "3-second confidence rule" (Section 0.1.7), not meant to be verbatim scrapes, and weren't part of what Brenton flagged. MaxTrax's `.short-desc` and Simple's short description *were* real scraped fields, so those were restored to verbatim; the others were left as-is since there's no equivalent single "short description" field on those live pages to restore from (config-variant/vehicle-specific/grouped-bundle's real short-form content on the live site is the micro-value-prop bullet list, which was already accurate).

**Verified with Playwright:** config-variant's Details tab correctly swaps between the two variants' real copy on click; no console errors on any of the 5 templates; no content clipping at mobile width on the longest tabs; grouped-bundle screenshotted to confirm layout holds with the much longer content. Not yet pushed — this is a big enough content change to want Brenton's sign-off first, per the usual push-after-approval workflow.

### What's Included product-name links (2026-09-11, backlog item 3)

Made each `.pi-name` in the What's Included widget a placeholder `<a href="#">` link (dud, per Brenton — no real product page to point to yet), on the two templates that have this widget (vehicle-specific, grouped-bundle; Simple/Config-variant/Sibling-color don't bundle products so have no What's Included block). Kept the existing visual look (`color:#333`, no default link blue/underline) and only added an underline on hover as the new affordance. Verified with Playwright on both templates: links present with correct labels, zero console errors, no layout shift.

### What's Included quantity-format re-confirmation (2026-09-11, backlog item 18)

Re-checked the `.pi-qty` ("1x ...") quantity-leading format against item 3's `.pi-name` link change, now that item 3 is done. Confirmed no behaviour change, as spec.md predicted: markup order is unchanged (`.pi-qty` span still precedes `.pi-name` link in every `.package-item` row on both templates) and item 3 only touched `.pi-name`'s own colour/hover rule, not any layout property — `.package-item`'s flex layout and `.pi-qty`'s `flex:0 0 auto` sizing were never part of that change. Verified with Playwright on both templates that have this widget (vehicle-specific, grouped-bundle): rows read "1x [linked name]" in order, hover-underline on the link still works, no layout shift, zero console errors (one unrelated favicon 404 on vehicle-specific, pre-existing and not caused by this or item 3).

### Shipping-tab DC widget duplicate + Store Slide-out postcode grouping (2026-09-11, backlog items 13 & 14)

Both items are covered in full under Section 10.A items 13 and 14 above (kept there rather than duplicated here, since both are already-numbered backlog entries) — see those for the complete detail: a second Delivery/Click & Collect widget on the Shipping Info tab with live two-way postcode sync across every `.dc-widget` on the page (`syncDcPostcode()`), and the Store Slide-out reworked into `<details>/<summary>` accordion groups with a postcode-resolved "Within 100km" group promoted to the top (`postcodeToState()`/`renderStoreSlideoutBody()`, real AU postcode-digit ranges including the ACT carve-out). Not yet pushed — holding for Brenton's sign-off per the usual workflow.

### Region-aware phone number + real UK/NZ single-store data (2026-09-11, backlog items 28, 29 & 30)

Covered in full under Section 10.D items 28–30 above — Brenton caught the UK region still reading "On display at the London Store" (the original placeholder) and asked why, which led to picking up all three data-fix items in one pass since they share the same sourced data (`REGION_SINGLE_STORES`/`REGION_PHONE` in shared.js). Real Trust Row phone numbers per region, real Bolton store data replacing the London placeholder (fixing the Showroom heading, Click & Collect line, map popup, B-Stock/Ex-Demo copy, and one additional stale "London showroom" reference in the Trust Row copy that the original item didn't list), and real Auckland address/phone/coordinates for NZ. Not yet pushed — holding for Brenton's sign-off per the usual workflow.

### Short description consolidated into the Decision Panel (2026-09-11, not a numbered backlog item — caught reviewing Sibling-Color)

Brenton flagged an unexplained paragraph on Sibling-Color sitting between Showroom Finder and the USP row, duplicating the Details tab's opening sentence. Investigation found two separate "short description" elements on every template: `.value-prop` (the short line under the `<h1>` in the Decision Panel, with the 2-line clamp + "Read more" built 2026-09-10 per item 11) and a second, unclamped `.short-desc` paragraph lower on the page — the one Brenton actually spotted. Brenton confirmed: `.value-prop` was never a real Rackit field and shouldn't exist on any product page; `.short-desc` is the real field and belongs in the Decision Panel slot `.value-prop` was occupying.

Fixed by relocating each template's real `.short-desc` content up into the Decision Panel (keeping the existing clamp + "Read more" → Details tab mechanic, `initTabJumpLinks()`, unchanged) and deleting the old lower-page paragraph and every `.value-prop`/`.value-prop-readmore` reference — a rename/consolidation, not a new component. Vehicle-specific (the one template with no `.short-desc` field previously) now carries a real Rackit-sourced short description Brenton supplied: "This Rhino Rack rack package includes everything you need to fit securely to your Toyota Hilux N80 4dr Ute (2015 to 2026) with Bare Roof" — matching that template's own Details-tab opening line, same pattern already used on the other 4 templates. Confirmed region-agnostic with Brenton (static per-SKU copy, no AU/NZ/UK differences). `PAGE-GLOSSARY.md`'s "Short description 'Read more'" entry updated to match. `DEVELOPER-BRIEF.md` line 237's "title/value-prop line" phrasing is now stale but was left untouched (screenshots/wording there are gated on Brenton's separate sign-off) — flagged for a wording fix whenever that document is next opened.

**Same-day follow-up:** Brenton caught the "Read more" link landing on its own row below the 2 clamped lines instead of inline at the end of the visible text. Root cause: CSS `-webkit-line-clamp` can't guarantee an inline child stays visible at the truncation point — it just clips straight through it if it doesn't fit on the clamped line, so the link had to sit outside the clamped box as a separate row instead. Replaced the CSS clamp with a JS measurer, `layoutShortDesc()` in shared.js: the "Read more" link is now the short-desc `<div>`'s own last child (nested in the markup, not a sibling row after it), and on `DOMContentLoaded` (and on resize, debounced) the function measures the real rendered height against a 2-line budget and trims the source text word-by-word — storing the untouched original in `data-full-text` so repeat runs always trim from the real source — until "`<text>`… Read more" fits within exactly 2 lines, keeping the link genuinely inline at the end of whatever text remains. Caught and fixed one bug during that same build: the first version captured `el.textContent` (which already included the link's own "Read more" label, now nested inside) as the "source" text, producing a doubled "Read more Read more" — fixed by measuring off a clone with the link removed first. Verified with Playwright on all 5 templates at 1280px (short-desc always lands as exactly 2 lines, link inline at the end, no leftover blank row) and mobile 390px on Sibling-Color (resize correctly re-triggers the layout at the 16px mobile floor, no horizontal overflow) — zero console errors throughout.

Not yet pushed — holding for Brenton's sign-off per the usual workflow.

### Fitment CTA copy thresholds (2026-09-11, backlog item 17)

`applyFitGalleryFlag()` (shared.js) reworked from a flat "See N real fitments" into a 3-tier branch on the gallery's real count: 5+ → "We've fitted this N times — view the gallery" (links to the gallery); 1–4 → "See real fitments," no number (still links to the gallery, since a small number isn't worth leading with); 0 → "Get It Installed," falling back to the same external fitting-costs link the no-gallery-at-all state already uses, but kept as its own distinct copy (confirmed with Brenton — not a reuse of "See Fitting Options," which stays exactly as-is for the "Fitment Gallery exists" checkbox being off entirely). Confirmed region-agnostic — this widget doesn't appear in the Region Selector's cascade table.

Since the demo count was hardcoded to 283 in vehicle-specific's markup with no way to preview other values, added a "Fitment count" number input to the Demo State Panel (Vehicle-Specific only, next to the existing "Fitment Gallery exists for this product" checkbox) that writes onto `#fitGallerySection`'s `data-count` and re-runs `applyFitGalleryFlag()` live, regardless of the checkbox's current state.

Verified with Playwright on Vehicle-Specific: default (283/checkbox on) shows the "We've fitted this 283 times" copy linking to the gallery anchor; setting the new count input to 3 shows "See real fitments" still linking to the gallery; setting it to 0 shows "Get It Installed" linking to the external URL with `target="_blank"`; unchecking the gallery-exists checkbox shows "See Fitting Options" unchanged; restoring 283/checked returns to the original state — zero console errors across all four. Spot-checked Simple (no fitment gallery at all) to confirm its permanent "See Fitting Options" default is unaffected. `PAGE-GLOSSARY.md`'s "Get It Installed CTA" entry updated to match. Not yet pushed — holding for Brenton's sign-off per the usual workflow.

### Get It Installed region differences (2026-09-11, backlog item 34)

Revisited now item 16's redesign has landed. Confirmed with Brenton: the two trust bullets stay identical across all regions (no copy swap); the CTA link — hardcoded to the full AU domain — becomes a relative path (`/roof-rack-installation-and-fitting-costs`) instead, since it'll be a genuine global URL once each region is its own Magento store domain in production, resolving correctly with no per-region JS branching needed. Changed in the 4 templates with a static href plus `applyFitGalleryFlag()`'s `fallbackLink()` helper in shared.js (drives Vehicle-Specific's "0 fitments"/"gallery off" states). Full detail under Section 10.D item 34 above. Verified with Playwright on all 5 templates — correct href everywhere it applies, Vehicle-Specific's other two fitment states unaffected, region round trip on Simple confirmed no interaction with the cascade, zero console errors. Not yet pushed — holding for Brenton's sign-off per the usual workflow.

### Section 12 bulk batch — 23 items (2026-09-12, dev-brief review backlog)

Brenton asked which of Section 12's open items could be tackled confidently in one batch with minimal back-and-forth. After confirming region scope (none of these need new region handling beyond what items 16/22 already are), the Persistent Bar rollout scope (thumbnail+name included on all 5 templates, not just the fitment box), item 32's scope (documentation only, no real per-product PDP to link to yet), and item 30's scope (build FAQPage schema now against the existing demo FAQ content), 23 items landed this session: 1, 2, 3, 4, 9, 10, 11, 13, 14, 16, 21, 22, 25, 27, 28, 29, 30, 32, 33, 34, 35, 36, 37 (see Section 12 above for each item's own detail). Excluded: 15 (needs Rackit-schema-owner input), 26 (blocked on item 5's undecided placement/colour), 5/8/12/19/23/31 (already flagged 🤔/💬 — open decisions or bigger discussions), and the pure screenshot-capture gaps (17, 20, 24, 38 — no underlying bug, just missing captures, deferred to the standing final mass-capture pass); items 6 and 40 were confirmed in scope but deliberately deferred to a future session (bigger effort than the rest of this batch).

### Section 6 item 2 closed — Simple/Config-variant/Sibling-color review checkpoint (2026-09-12)

Picked up via `/backlog`. Rather than running a fresh design critique against these three templates (an option offered and declined), Brenton's explicit call was to close this checkpoint on the strength of the correction/iteration these three have already been through since their initial direct build — the same Section 10/12 backlogs referenced throughout this log (USP rows, pricing/price-block redesign, heading-hierarchy audit, status-pill unification, short-description expand/collapse, region selector + UK brand skin, sticky/persistent bar fixes, among others), all built and Playwright-verified across all 5 templates, these three included. No new critique artifact exists for this item and none is owed — treat it as closed, not as deferred. Confirmed region-agnostic (general design-quality checkpoint, not part of the region-selector cascade). No prototype/code change, no `DEVELOPER-BRIEF.md` touch, nothing pushed.

**Prototype fixes** (`shared.js`/`shared.css` + all 5 templates' markup): Persistent Bar now ships on Simple/Config-Variant/Sibling-Color/Grouped-Bundle (thumbnail+name+price+Add to Cart, no fitment box/Rack Fit Guarantee badge — those stay Vehicle-Specific-only), with price/thumbnail wired into each template's own `renderPrice()`/`renderGallery()` so they stay in sync with the selected variant/colour, same pattern `#mobilePrice` already used; Rack Fit Guarantee badge (all 3 instances) now jumps to the Gold Guarantee tab on click, reusing `initTabJumpLinks()`; Add to Cart no longer hard-disables on "Doesn't fit" (`applyFitmentState()`); UK's Delivery/Click & Collect now correctly defaults to the Delivery tab (`REGION_DEFAULT_DC_TAB`); NZ/UK's single-store on-display claim is now a real flag check (`REGION_SINGLE_STORES[region].onDisplay`) instead of unconditional, applied consistently to both the Click & Collect summary line and the Showroom map's single-pin popup; Discontinued state no longer shows duplicate text (stock-status-line now hides entirely rather than repeating the banner's message); Discontinued banner text is black, not red-on-red; Ex-Demo/B-Stock pill tags no longer wrap to two lines; each template now carries a `FAQPage` JSON-LD block built from its own existing FAQ content.

Item 3 (Showroom map blank on mobile screenshots) was investigated live with Playwright at 390px rather than assumed — confirmed a capture-timing issue (map renders correctly once scrolled into view with time for tiles to load), not a genuine render bug, so no code change was needed; noted as a capture-process requirement for the eventual final mass-capture pass instead.

**`DEVELOPER-BRIEF.md` text fixes** (approved for direct editing this session, not held for separate sign-off, since these are corrections to the brief itself): reworded "live-swap" language across §6.2/§6.3/§7.5/§7.6 to correctly describe real production behaviour as navigation to each variant/colour's own separate product page (this prototype's in-page swap is a demo simulation only); "kits" → "Vehicle Rack Sets" in §6.4; §7.3's stale badge-location wording fixed to match the actual CSS; §7.4 and §7.18 now carry Rackit data-source notes; §7.17 notes Gold Guarantee is Magento CMS content and cross-references the Reviews tab to Section 2; §7.9 reworded so On Display reads as a general concept, not an AU-only one; §7.7's region cascade line corrected to match the UK DC-tab fix; §7.15's stale "placeholder box" description corrected to match the actual (fully-hidden) behaviour; §7.19/§7.22 now document the intended Related Products/Discontinued-alternates card click-through behaviour; §7.20's Package Contents Links section rewritten to drop the incorrect claim and the unnecessary named reference; and a global pass removed backlog-item-number citations, meeting references, and named confirmations throughout the document (0.1, 4.6, Section 3, §7.20), replacing each with a plain statement of current, correct behaviour. Also caught and fixed in passing: the Grouped-Bundle Package Contents screenshot showing the Demo State Panel in frame (swapped to `screenshot-pending.svg`, per the standing screenshot-hold policy) and Section 0.1's rule 2 now explicitly calls out checking every existing screenshot for the panel being visible before the final mass-capture pass.

Verified with Playwright throughout (persistent bar rendering/variant-sync on all 5 templates, badge click-to-tab-jump on all 3 instances, fitment-state CTA enabled, region round trips, discontinued/ex-demo CSS fixes, FAQPage JSON-LD parses correctly on all 5 templates) — zero console errors. Not pushed; `DEVELOPER-BRIEF.md`'s own build-status framing otherwise untouched beyond the sections described above.

### Fitted Photos Gallery — final design + working "View All" slide-out (2026-09-12, Section 12 items 5 & 26)

Picked up together since item 26 was explicitly blocked on item 5. Brenton's decisions: lock the gallery's placement/colour to full-width above the Trust Row, non-red (the existing default), permanently; build "View All In-store Fitments" as a right-edge slide-out matching the Store Slide-out convention; and, since only 16 of the real 283 fitment photos were ever scraped, cycle/repeat those 16 real photos to fill the expanded grid rather than showing a short 16-photo set (flagged as a data gap in Section 7 for the client).

**Item 5:** removed `applyFitGalleryPlacement()`/`applyFitGalleryRedFlag()`, the `#fitGalleryNestedSlot`/`#fitGalleryHomeSlot` slots, the `.fit-gallery-section.nested`/`.fit-gallery-panel.red` CSS, and both Demo State Panel controls entirely from `shared.js`/`shared.css`/`vehicle-specific/index.html` — the panel's visual defaults were already the locked-in look, so no styling change was needed beyond removing the now-dead toggle mechanism.

**Item 26:** lifted the carousel's photos into a shared `FIT_GALLERY_PHOTOS` array in `vehicle-specific/index.html` (all 16 real scraped URLs from `data/raw/GP01M1TZZ.json`, up from the 11 previously hardcoded — 5 real ones had been sitting unused in the scrape), rendered once via `renderFitGalleryTrack()` so both the small carousel and the new slide-out draw from one source. Built `buildFitGallerySlideout()`/`openFitGallerySlideout()`/`closeFitGallerySlideout()` in `shared.js` plus new `.fit-gallery-slideout*` CSS in `shared.css` — same backdrop/right-edge-drawer/transition convention as `buildStoreSlideout()`, but 600px wide with a responsive photo grid instead of a text list. The "View All In-store Fitments (283)" link now triggers this drawer (`data-fit-gallery-slideout`, same trigger pattern as the store/ex-demo slide-outs) instead of being a dead `href="#"`; the grid cycles the 16 real photos up to the section's real `data-count`, capped at 100 tiles.

`PAGE-GLOSSARY.md` updated (retired the placement-toggle entry, updated the "View All" description to match the new working behaviour). `DEVELOPER-BRIEF.md` §7.16/§6.4 still describe the old toggle-pending/dead-link state — not touched this session, owed whenever Brenton's next separate dev-brief sign-off round happens.

Verified with Playwright on Vehicle-Specific: Demo State Panel no longer shows the placement/red toggles (Fitment Gallery exists/count controls still present and working); gallery still renders full-width/non-red with no visual regression; in-page carousel still shows all 16 photos with working prev/next nav and dots; clicking "View All In-store Fitments (283)" opens the slide-out with a populated grid and matching "In-store Fitments (283)" heading; close button and backdrop-click both close it; zero console errors throughout. Not pushed — holding for Brenton's sign-off per the usual workflow.

**Same-day follow-up — grid-only slide-out reworked into a two-view detail popup.** Brenton shared a screenshot of the live site's own "Browse Fitment" popup (opened by clicking a photo) and asked for that behaviour instead of a flat grid with no click-through. Clarified three genuine data gaps before building, since our 16 real photos have no per-photo vehicle/component/multi-angle data behind them at all: (1) every fitment shows the *same* real vehicle + real components (Toyota Hilux N80, this page's own What's Included list) rather than fabricating variety across different vehicles/products the way the live reference's cross-product browser does; (2) the detail view's 4-thumbnail strip reuses 3 other real photos from the same 16 as stand-in "other angles" rather than inventing new shots; (3) "View Rack & Buy" is replaced with "Back to Product" (closes the drawer, scrolls to and briefly highlights the Decision Panel) since this page already is that exact product.

Built `renderFitGallerySlideoutGrid()` (the original grid, now split out so it can be returned to) and `renderFitGallerySlideoutDetail(index)` in `shared.js`, both writing into the same `#fitGallerySlideoutBody`/swapping the head between the "In-store Fitments (N)" title and a new "‹ Back to Grid View" link (`#fgsBackLink`); a single delegated click listener on the body handles grid-photo clicks, Prev/Next, and Back to Product so neither view needs its own rebinding after each render. `FIT_GALLERY_PHOTOS` changed from plain URL strings to `{thumb, id}` objects — `id` is each photo's own real Rackit `product_id` from the scrape, used as the detail view's "Fit #" so that number is real, not fabricated, even though the photo itself may be a cycled repeat once past the real 16. Rack Components list is read live from the page's own `.package-item` rows (`rackComponentsFromPage()`) rather than duplicated as a second hardcoded list, so the two can never drift apart. New `.fgs-*` CSS in `shared.css` (browse bar, Prev/Next as `.btn-outline` rather than the live reference's bold red pills, per this project's standing secondary-CTA colour rule — Prev/Next aren't purchase actions and red would compete with Add to Cart; main image + thumbnail grid; product title/vehicle line reusing the fitment-badge car icon; component rows with a dark quantity pill matching the reference; a brief gold outline on the Decision Panel when "Back to Product" lands there).

Verified with Playwright: clicking a grid photo opens the detail view with the correct real Fit # and matching browse position; Next/Prev update both correctly and disable at the first/last tile; Back to Grid View returns to the full 100-tile grid; Back to Product closes the drawer and applies the highlight class to the Decision Panel; zero console errors throughout. Not pushed — holding for Brenton's sign-off per the usual workflow.

**Same-day follow-up #2 — in-page carousel photos now open the same fitment's detail directly.** Brenton noticed the small Fitment Gallery carousel on the main page (not the slide-out) had no click behaviour at all. New `openFitGallerySlideoutAt(index)` in `shared.js` opens the slide-out straight to `renderFitGallerySlideoutDetail(index)`, skipping the grid; `renderFitGalleryTrack()` (`vehicle-specific/index.html`) now tags each carousel photo with the same `data-fgs-open-index` used by the grid and wires a click handler to it — index maps 1:1 since the carousel shows all 16 real photos in the same order the grid does before it starts cycling past them. Added a pointer cursor + hover-dim to `.fit-gallery-track img` in `shared.css` so the photos read as clickable. Verified with Playwright: clicking carousel photo index 7 opens the slide-out directly on that fitment's detail view with the matching real Fit # (165847) and correct browse position (8 of 283); zero console errors. Not pushed — holding for Brenton's sign-off per the usual workflow.

## 10. Next backlog — 2026-09-11 (Brenton Cooley solo review session: region selector, widget updates, display tracking)

Source: Brenton's manual task list + a solo Granola-recorded review session, 2026-09-11 ("Product page redesign — region selector, widget updates, and display tracking"). **Planned but not yet built** — numbered here, on Brenton's explicit instruction, so the work can be picked up across multiple future chats rather than built in one marathon session (unlike the Graham-meeting backlog above). Update each item ⬜→✅ in place as it's completed, same convention as the rest of this file.

**Scope decisions confirmed before writing this list (don't re-litigate these when picking an item up):**
- **Region selector** is a header dropdown that behaves like the existing Demo State Panel toggles — live-swaps copy/widgets via JS across all 5 templates, no page reload, no separate per-region page files.
- **Showroom Finder map rework** (35-pin default view, red/grey pins) ships across all 5 templates immediately, not piloted on one template first — this is a refinement of an already-shared, already-locked-in feature (the map itself), not a new pilot needing re-validation.
- **Rackety "showroom" bin-location work** (the real backend data source for a genuine on-display flag) is explicitly **out of scope for this project** — that's Brenton's own follow-up with Chris (phase 1 = Tula products only, dash-display SKUs as a P2). This prototype keeps treating "on display per store" as a given boolean, same as its other demo data.

### A. Mechanical UI fixes
1. ✅ Vehicle fitment bar: green dot → car icon.
2. ✅ Move SKU to the top-right of the decision panel.
3. ✅ **Done 2026-09-11.** What's Included: each product name (`.pi-name`) is now an `<a href="#">` placeholder link (dud, per Brenton — doesn't need to resolve to a real product page yet) on both templates that have this widget (vehicle-specific, grouped-bundle; the other 3 templates don't bundle products so have no What's Included block). Styling unchanged from the existing span look (`color:#333`, no default link blue/underline), with underline added on hover as the only new affordance. Verified with Playwright on both templates — links present, correct labels, no console errors, no layout shift.
4. ✅ **Done 2026-09-11.** B-Stock/Ex-Demo modal → right-edge slide-in, matching the existing store slide-out pattern (`.exdemo-slideout*` CSS, `buildExdemoSlideout()`/`openExdemoSlideout()`/`closeExdemoSlideout()` in shared.js — renamed from the old `*Modal()` functions; the now-unused generic `.modal-*` CSS was removed since nothing else referenced it). Two region follow-ups folded in while touching this code (confirmed with Brenton via AskUserQuestion): **UK calls this "Graded"** instead of "Ex-Demo/Factory Seconds" — UK-only, applies to the drawer heading and the inline stock-line link text only (`exdemoCopy()`); the 3 individual option tags (Ex-Demo/Sellable Return/Factory Second) are unchanged in every region. **Option-card store names are now real**, derived from the canonical data instead of a separate fabricated `EXDEMO_STORES` list: AU cycles through real names flattened from `RRG_STORE_NETWORK` (`exdemoStoreName()`), NZ/UK show their single region store from `REGION_SINGLE_STORES` (will pick up the real UK "Bolton" name for free once item 29 is built, same multi-spot-fix pattern noted there). `applyRegion()` now also re-runs `renderStockLine()` so the wording/store name flips live if the region switches while the trigger link is visible. Verified with Playwright on Simple (full open/close mechanics, backdrop-click-to-close, AU→UK→NZ→AU round trip on wording/heading/store name/currency) and spot-checked on Vehicle-Specific (open/close only, region cascade already covered) — zero console errors on either.
5. ✅ Colour-selection value text moved to sit next to the "Colour" label (currently separate).
6. ✅ **Done 2026-09-11.** USP dot points added directly above the Tabs widget (`.body-cols`) on all 5 templates — new `.usp-row`/`.usp-item` component in shared.css, a vertical stacked list (checkmark icon + full-sentence text per row), not Trust Row's horizontal 4-column grid, since the real content is full marketing sentences of variable count rather than short labels. **Content is real, sourced from Rackit (the backend), pasted verbatim by Brenton — not authored copy:** Config-Variant (6 points), Sibling-Color (4), Vehicle-Specific and Grouped-Bundle (3 each). **Simple (`FRWTAN063`) has no USPs set in Rackit, so it gets no `.usp-row` markup at all** — omitted entirely rather than rendered empty, a genuine "field absent for this SKU" case worth flagging in the eventual `DEVELOPER-BRIEF.md` writeup as an optional per-product field, same convention as other optional-data components. Static markup, no JS/`applyRegion()` hook needed — confirmed region-agnostic with Brenton, and content doesn't vary by variant/colour. Verified with Playwright on all 5 templates: correct point count/text on the 4 templates with content, zero `.usp-row` elements on Simple, sits directly above the Tabs widget on every template, no console errors; mobile (390px) spot-checked on Grouped-Bundle — wraps cleanly, no overflow caused by the new block.
7. ✅ Trust bar phone number becomes a `tel:` link.
8. ✅ Specifications tab: soft row-highlight on hover, desktop only (not mobile).
9. ✅ Fitting Instructions tab: any link renders with a PDF icon.
10. ✅ Sticky header (vehicle-specific's desktop persistent bar + mobile sticky-cta bar, the only template with a "Fits Your Vehicle" box): added a product thumbnail + truncated name block, sharing space with the fitment box so it settles to roughly half its old width. Desktop keeps icon + short label; mobile is icon-only (no room once competing with the name) — same "icon only, tooltip carries the caption" convention as the Rack Fit Guarantee badge. The car icon in both is now a click-to-copy button for a demo-derived vehicle slug (`toyota-hilux-n80`, no real slug field exists yet — flagged in shared.js). Main decision-panel fitment card untouched (still full detail text, plain non-interactive icon).
11. ✅ Rename "Important Vehicle Fit Notes" → "Product Notes" (vehicle-specific, the only template it exists on today — widening it to other product types still needs real non-vehicle copy before it can ship there, see Not build items below).
12. ✅ All yellow/orange warning-box text (headings, body, icons) recoloured black for legibility — applies to the Product Notes block and the Special Order banner.
13. ✅ **Done 2026-09-11.** Shipping Info tab: a second `.dc-widget` (identical Delivery/Click & Collect widget) now sits at the top of the tab on all 5 templates. Postcode is shared live across every `.dc-widget` on the page, not just prefilled once — typing/committing a postcode in either the decision-panel widget or this new one (`syncDcPostcode()` in shared.js) mirrors the value into the other and re-runs both their own `update()`, in both directions. No new `applyRegion()` hooks needed: tab switching, availability flags, and the region cascade (default tab, "View all stores" visibility) already iterated every `.dc-widget` generically before this change, so the new widget inherited all of it for free. Postcode inputs now identified via `[data-dc-postcode]` instead of `id="dcPostcode"` so two widgets can coexist on one page without a duplicate id. **Bug caught by Brenton post-build, fixed same day:** the new postcode field was invisible/untypable inside the Shipping Info tab — root cause was the tabs accordion's own CSS, `.tabs input{display:none;}` (meant only to hide the accordion's own hidden radio-button inputs), matching *any* `<input>` nested anywhere inside `.tabs`, including this new one several levels deep in the tab's `.content`. Narrowed to `.tabs>input[type="radio"]` (shared.css) so it only ever targets the accordion's own direct-child radio controls, same real elements as before, nothing else affected. Re-verified with Playwright on both templates, both mobile (390px, vertical accordion) and desktop (1280px, horizontal tab bar) widths: field now visible and genuinely typeable (real keystrokes, not just JS-set values), decision-panel widget correctly picks up the typed value — zero console errors throughout.
14. ✅ **Done 2026-09-11.** Store slide-out: stores now group into collapsible `<details>/<summary>` accordions (`renderStoreSlideoutBody()` in shared.js, same no-JS convention as the FAQ section) instead of a flat always-visible list. Once a postcode resolves to a state — via a new, genuine AU postcode-digit-range lookup, `postcodeToState()` (not real distance/geocoding, which this prototype has never had; confirmed with Brenton as the intended simulation), including the real 2600–2618/2900–2920 ACT carve-out — that state's stores promote into a "Within 100km" group at the top, open by default and omitted from the list below so they aren't shown twice; every other state stays reachable as a closed group. With no postcode (or a non-AU region, where the store-slideout trigger is already hidden entirely by the existing region cascade — confirmed AU-only in scope with Brenton), every state renders open, matching the previous flat order. Wired to the same shared postcode as item 13 (`syncDcPostcode()` calls `renderStoreSlideoutBody()` on every commit). Verified with Playwright on Simple: NSW postcode (2170) correctly promotes "Within 100km" with 7 stores; ACT postcode (2600) correctly resolves to Australian Capital Territory, not New South Wales; clearing the postcode falls back to all 7 state groups open; zero console errors. Spot-checked on Vehicle-Specific — same WA-postcode grouping behavior, no regressions.
15. ✅ "On Display" store-row pill colour: yellow/orange → blue.
16. ✅ **Done 2026-09-11 (new session).** Get It Installed panel — full redesign, replacing the reverted mechanical placeholder (full-bleed installer photo with a dark gradient text overlay, a static/fake 4-star badge, a small white pill button) with an actual light card, on all 5 templates. Confirmed via AskUserQuestion before building: **region-agnostic** for now (item 34 stays open for later, once there's something concrete to base region differences on); **stays paired with the video** in the gallery column rather than becoming its own full-width section like Showroom Finder (smaller structural change, same position under the gallery images); **blank slate** — none of the reverted pass's specific choices carried over. New structure: a framed/contained installer photo (`.install-photo`, `aspect-ratio:3/2`, no more absolute-positioned full-bleed background) at the top of a white card (`.install-cta-panel`, `border:1px solid var(--rrg-line)`, matching the Fitment Gallery panel's established card language); a header row with a small icon badge (`.install-badge`, reusing the `.fit-gallery-badge` visual pattern) + "Get It Installed" heading; 2 real trust bullets (`.install-usps`, reusing the `.usp-item`/`.usp-icon` checkmark-row component from item 6's USP row) — "Over 200,000 roof racks fitted" and "Nationwide network of trained fitters," both drawn from the Trust Row's existing real copy ("Trained Professionals"/"Australia's Largest"), not fabricated. The static/fake REVIEWS.io-style star badge (`.install-cta-reviews`/`.stars`) is dropped entirely — it was always non-functional and is now genuinely redundant with the real per-SKU star rating already live in the Decision Panel (item 19). **CTA button restyled to `.btn-outline`** (the site's existing white-fill/black-border subdued secondary style, already used for "Select Store" etc.) rather than red or gold — a specific call from Brenton mid-review: even though red isn't the Add to Cart colour, it still pulls the eye and would read as a second prominent CTA competing with the one purchase action, so this needed to be visibly subdued instead. CTA text/href logic is untouched (stays item 17's job) — vehicle-specific still renders its fitment-count-aware "See 283 real fitments" link, the other 4 templates still render "More Information + Bookings." The `.no-video` state (Demo State Panel toggle, or default on templates with no real video) is rebuilt as a horizontal photo-left/content-right split on desktop instead of the old full-bleed-with-left-anchored-overlay look, using the same `.no-video` class shared.js already toggles via `applyVideoFlag()` — no JS changes were needed anywhere; `applyFitGalleryFlag()`'s `.install-cta-panel .btn` selector still resolves correctly since the CTA anchor kept its class. Verified with Playwright on all 5 templates: card renders correctly paired with video, `.no-video` split variant confirmed on Simple, vehicle-specific's fitment-aware CTA text/href and its "Fitment Gallery exists" toggle fallback both still work correctly, mobile (390px) spot-checked on Vehicle-Specific (clean stacking, no horizontal overflow, `.install-cta-reviews`/`.stars` confirmed fully gone) — zero console errors across all 5. Not yet pushed.

**Follow-up same day, real issues caught by Brenton on review:** the framed-photo-plus-white-body-below version (first pass above) had two problems: (1) paired with the video, the card was visibly taller than it — since the video renders at a fixed `aspect-ratio:16/9` and the card had a photo *plus* a separate body section stacked beneath it, the two could never match height while paired side by side; (2) `.install-cta-panel`'s own `border:1px solid var(--rrg-line)` wrapped the whole card flush against the photo's edges, reading as a visible (near-white, `#D8D8D8`) border line around the photo itself. Fixed by reverting to a full-bleed absolutely-positioned photo (`.install-photo{position:absolute;inset:0}`, no border anywhere on `.install-cta-panel`) with the badge/heading/trust-bullets/CTA overlaid directly on top via a dark gradient scrim (`.install-body{position:relative;height:100%;background:linear-gradient(90deg, rgba(0,0,0,.82)...)}`) — same real content (2 trust bullets, `.btn-outline` CTA) kept exactly as-is, only the structural container changed. Because the photo is now the full card again, `.install-cta-panel{aspect-ratio:16/9}` is what controls height when paired with the video, so the two are pixel-identical by construction rather than by coincidence — confirmed with Playwright (`getBoundingClientRect()` on both, `diff:0`). The `.no-video` full-width state keeps its own `min-height:220px` (no video to match, so no fixed-aspect constraint needed there) with the same overlay content, just a wider scrim (`max-width:420px` on the text column so it doesn't stretch edge-to-edge on a much wider card). Text/icon sizes were shrunk slightly (16px heading down from 17px, 12px bullet text down from 13px, tighter gaps) to comfortably fit within the 16/9 envelope at typical gallery-column widths (~170px tall) without feeling cramped. `.btn-outline` needed no changes — its white-fill/black-text look already reads clearly over the dark scrim, same as it did on the white card. Re-verified with Playwright on Simple (paired-state height match, no-video full-width layout) and Vehicle-Specific (mobile 390px: matching height, no border, no overflow, console clean). `PAGE-GLOSSARY.md`'s Get It Installed CTA entry and the project memory files updated to match. Not yet pushed.

**Second follow-up same day, two more real issues plus a copy/link fix:** (1) Brenton reported a visible black border around the photo in *both* states, and the no-video state's content packed into the top-left instead of centring. Investigated both properly rather than guessing: the border turned out to be baked directly into `installer.png` itself — canvas pixel-sampling (`getImageData` scan from each edge inward) measured a genuine ~10–25px black frame in the source file (left 23px, right 25px, top 22px, bottom 10px, out of 740×420), not a CSS bug — `getBoundingClientRect()` had already confirmed the image element's own box was pixel-exact against the panel with no gap. Fixed by scaling the photo up around its own centre (`.install-photo{transform:scale(1.16)}`) so the border gets cropped outside the visible/clipped frame — 1.16 was computed from the worst-case edge (top, ~5.2% of image height) plus a safety margin, then re-verified clean with the same pixel-sampling technique. The top-left-packing bug was real and separate: `.install-body{height:100%}` only resolves correctly when its containing block (`.install-cta-panel`) has a *definite* height — true for the paired state (fixed `aspect-ratio:16/9`) but not for `.no-video`'s `min-height`-only box, where the percentage silently fell back to `auto`, collapsing the body to its own short content height stuck at the top instead of filling and centring in the card. Fixed by switching `.install-body` to `position:absolute;inset:0` (the same reliable stretch technique `.install-photo` already used, which doesn't depend on a definite parent height) — `justify-content:center` now actually has a full-height box to centre within, in both states. Per Brenton's explicit direction, the paired (half-size) state's text/icon sizing was left exactly as-is — only the image-crop fix applies there; the no-video (full-size) state got bigger text/icons/badge (heading 16px→21px, bullet text 12px→14.5px, badge 24px→32px, button 12.5px→14px, more generous gaps/padding) since it has the room and Brenton asked for it to feel less cramped. (2) Separately, Brenton supplied the real destination for this CTA (`https://www.roofracksgalore.com.au/roof-rack-installation-and-fitting-costs`) and asked for the fallback/default button copy to read **"See Fitting Options"** instead of the placeholder "More Information + Bookings" — updated on all 4 templates that show it statically (Simple, Config-Variant, Sibling-Color, Grouped-Bundle) plus `applyFitGalleryFlag()`'s fallback branch in shared.js (Vehicle-Specific, when "Fitment Gallery exists" is toggled off) — real external link, `target="_blank" rel="noopener noreferrer"` added/cleared correctly as the toggle flips between the in-page fitment-gallery link and this external one. Re-verified with Playwright on all 5 templates: zero height diff vs. the video on every template, no border visible (screenshot-confirmed on Simple paired/no-video and Vehicle-Specific mobile), correct CTA text/href/target in both of Vehicle-Specific's toggle states, zero console errors. Not yet pushed.
17. ✅ **Done 2026-09-11.** Fitment CTA copy — `applyFitGalleryFlag()` (shared.js) now branches on the gallery's real count instead of a flat "See N real fitments": 5+ reads "We've fitted this N times — view the gallery" (links to the gallery); 1–4 drops the number ("See real fitments," still links to the gallery); 0 (gallery exists, nothing to show) reads "Get It Installed" and falls back to the same external fitting-costs link the no-gallery-at-all state already used, kept as distinct copy since it's a different scenario (confirmed with Brenton — not the existing "See Fitting Options" text). "See Fitting Options" itself is unchanged for the "Fitment Gallery exists" checkbox being off. Confirmed region-agnostic with Brenton — this widget isn't in the Region Selector's cascade. Since the demo count was previously hardcoded to 283 with no way to preview other values, added a new "Fitment count" number input to the Demo State Panel (next to the existing "Fitment Gallery exists" checkbox, Vehicle-Specific only) that writes straight onto `#fitGallerySection`'s `data-count` and re-renders the CTA live. Verified with Playwright on Vehicle-Specific: default (283/on) shows the "We've fitted this 283 times" copy linking to the gallery anchor; count set to 3 shows "See real fitments" still linking to the gallery; count set to 0 shows "Get It Installed" linking externally with `target="_blank"`; checkbox off shows "See Fitting Options" unchanged; toggling back to 283/on restores the original state; zero console errors across all four states. Spot-checked Simple (no `#fitGallerySection`) — "See Fitting Options" default unaffected. `PAGE-GLOSSARY.md`'s "Get It Installed CTA" entry updated to match. Not yet pushed. **Flagged for `DEVELOPER-BRIEF.md`:** this widget now has 4 distinct visual/behavioural states driven purely by data (5+ fitments, 1–4, 0, and no gallery at all) — when this eventually gets written up (gated on the standing all-5-templates-complete screenshot policy), it needs a screenshot of each state, not just one representative shot, so the developer can see exactly what changes at each threshold.
18. ✅ **Done 2026-09-11.** Re-confirmed What's Included quantity-leading format ("1x ...") still holds after item 3's link change — confirmed, no behavior change. See Section 9 for detail.

### B. New tabs/sections
19. ✅ **Done 2026-09-11, upgraded to a real integration same day.** New "Reviews" tab — last tab in the Tabs widget on all 5 templates. First built as a hand-rolled placeholder (pill toggle + fake review cards); once Brenton supplied RRG's real REVIEWS.io Polaris widget snippet, replaced entirely with a live embed (`store: 'roof-racks-galore'`) per template, using `types: 'product_review,store_review'` so the widget's own native tabs provide the Product/Company split (the custom toggle and placeholder cards were removed as dead code). Each template passes every SKU in its family so reviews show regardless of selected variant/colour. The header star rating's "See reviews" link still jumps to the tab via the existing `initTabJumpLinks()` mechanism, though that header figure itself remains a static placeholder — see Section 7.
20. ✅ **Done 2026-09-11.** New FAQ accordion section (not a tab, per the original ask) below the Tabs widget, above Related Products, on all 5 templates — native `<details>`/`<summary>` so items open independently, no JS needed. 4 Q&As per product, tailored to that template rather than generic filler. Placeholder copy pending real client content — see Section 7.

### C. Larger builds — each needs its own short design pass when picked up, not just a markup change
21. ✅ **Done 2026-09-11.** Region selector (AU/NZ/UK) built as a working dropdown on the Utility Bar's existing "🇦🇺 Australia" item (previously decorative-only), same interaction pattern as the Products template-switcher (click-to-open, click-outside-to-close). `applyRegion()` in shared.js cascades the choice into: Click & Collect default tab (AU/UK → Collect, NZ → Delivery, via a real click on the existing tab handler, not duplicated logic); Showroom Finder heading (AU keeps its real per-template "N Stores Nationwide" copy, cached from markup so switching back restores it; NZ/UK read "On Display At the Auckland/London Store"); the Showroom Finder map (AU keeps the full 35-store clustered network from item 22's rework; NZ/UK zoom to one always-on-display pin at a fabricated placeholder location — `REGION_SINGLE_STORES`, flagged demo-only since RRG has no real stores there); both "View all stores" links (Showroom Finder's and the Click & Collect card's) and the card's postcode-driven "within 100km" search, since the real 35-store data is AU-only; and the Trust Row's "Trusted Since 1989"/"Australia's Largest" items, which swap to region-appropriate placeholder copy (confirmed with Brenton via AskUserQuestion: adapt both lines per region rather than leaving them AU-only or hiding them, e.g. NZ reads "Now serving New Zealand" / "Visit In Person — Check it out at our Auckland showroom"). Resets to AU on every fresh page load, no persistence, same convention as the rest of the Demo State Panel's toggles. Ships to all 5 templates in one pass, per the already-locked-in scope decision. A design call was confirmed with Brenton before building (single-store map treatment: keep the real Leaflet map zoomed to one pin, rather than a plain info card or hiding the section). Verified with Playwright on Simple (full AU↔NZ↔UK↔AU round trip: dropdown open/close, utility-bar flag/label, DC default tab, Showroom heading text, map pins/zoom, view-all links, Trust Row copy, all correctly restoring on return to AU) and spot-checked on Vehicle-Specific (no regressions against its fitment/persistent-bar logic) — zero console errors on either, and no mobile layout impact (Region Selector is desktop-only, same as the rest of the Utility Bar's right-hand items). Also added a "Dev Brief" link to the internal nav (opens `dev-brief-viewer.html`) on all 5 templates, alongside the existing internal-only Products template-switcher. **Now documented in `DEVELOPER-BRIEF.md` Section 4** (added 2026-09-11, see this file's own Section 11 log) — the widget-level cascade (DC tab, Showroom heading/map, Trust Row) is covered there in full; the header dropdown's own open/close mechanics are covered too since they're inseparable from explaining the cascade, a narrow exception to the usual "header is out of scope" rule for this one control.

**Follow-up same day: UK brand skin + currency.** RRG trades in the UK as a real sister brand, "The Roof Box Company" (roofbox.co.uk) — Brenton asked to scrape their live site for logo/colours before building, then confirmed scope via AskUserQuestion (4 questions): (1) rebrand depth — **logo + core colour only, keep Barlow Condensed/Lato typography** (not a full reskin, not logo-only); (2) Add to Cart colour — **their real green `#26B226`** (not RRG's gold); (3) NZ — **stays Roof Racks Galore-branded**, no rebrand (real scraped tab copy already treats AU+NZ as one network under one brand); (4) currency — **UK prices show £ instead of $** (same demo numeric values, symbol swap only, no FX conversion anywhere). Real logo SVG downloaded from `roofbox.co.uk/images/trbc_logo.svg` to `prototypes/_shared/brand-roofbox-uk-logo.svg` — its own fill values confirm their two core colours directly: navy `#2E41AE` and yellow `#FFFF19`; the green Add to Cart colour was read off their live "Click Here To Order" button on a real product page. Implementation: `body.region-uk` (toggled by `applyRegionBrand()`) overrides the `--rrg-red`/`--rrg-red-dark` custom properties to navy, which recolours every component already built against those tokens for free (utility bar, trust icons, links, tab underline, sale badges, etc.) with no per-component CSS needed; Add to Cart gets its own override since it was never on that token (RRG's separate locked-in gold default); the logo (desktop only — see the follow-up fix below) spans both header bars rather than just sitting taller within the Main Header, since their real logo is a tall vertical rectangle (0.82:1) rather than RRG's wide horizontal wordmark (6.2:1). Currency: a new `regionCurrencySymbol()` helper in shared.js drives both `fmtAud()` (shared.js) and the 3 per-template `fmtMoney()` duplicates (config-variant/sibling-color/vehicle-specific), so any price rendered *after* switching to UK picks up £ automatically (verified: switching a config-variant option mid-UK-session correctly re-rendered at `£780.00`); a companion `applyRegionCurrency()` text-node sweep (scoped away from `<script>`/`<style>`/the Demo State Panel) fixes whatever's already in the DOM at switch time — static per-SKU markup, Related Products' `.price` divs, payment-badge instalment text — without needing to touch every render path individually. Verified with Playwright on Simple (full brand+currency AU↔UK↔AU round trip: logo, `--rrg-red` value, CTA colour, price, all correctly restore) and config-variant (variant switch mid-UK-session, NZ confirmed unaffected by either the brand class or currency) — zero console errors, no mobile overflow at 390px. Now documented in `DEVELOPER-BRIEF.md` Section 4.3, same addition as the rest of item 21.

**Two more follow-up fixes, same day:**
1. **Search button background bug (all 5 templates, all regions, pre-existing — not caused by the region/UK work above, just caught while eyeballing the header for it).** `.rrg-search-btn`'s background only covered a ~16px sliver vertically centred inside the ~40px search pill, not the full button height. Root cause: `height:100%` had nothing definite to resolve against (`.rrg-search`, its flex container, has no explicit height of its own — it's sized by its tallest child, the input), so the browser fell back to the button's own content height (just the 16px icon). Fixed with `align-self:stretch`, which sizes the button against the flex line's actual cross-size instead. Confirmed: button height now matches the input's (~38.8px) in both red (AU/NZ) and navy (UK).
2. **UK logo spans both header bars — refined twice more after the first pass, now on mobile too, including the sticky header.** Round 1 (flush): Brenton supplied `UK header example.png` (project root) showing the logo flush against the very top of the navy Utility Bar and the very bottom of the white Main Header, "Your Nearest Store"/"Your Vehicle" starting clear of it. Built desktop-only at first. Round 2 (halfway, not flush): Brenton corrected the exact span — bottom flush with the base of the white bar as before, but the **top only reaches halfway up through the blue bar**, not all the way to its own top — and asked for the same treatment on **mobile too**, including the **sticky condensed header** once scrolled (which has no Utility Bar equivalent at all today, so it needed one invented). Final implementation, all via `shared.css`/`applyRegionBrand()`:
   - **Desktop (≥901px):** `.rrg-logo` reserves a fixed 90px width (flex item, so nothing else shifts); its `<img>` is pulled `position:absolute`, offset to put its top at the Utility Bar's vertical midpoint (18px down the page) and its bottom flush with the Main Header's base (109px down). `.rrg-utility-left` gets matching `margin-left` so the utility-bar text starts clear of the logo.
   - **Mobile (≤900px):** same halfway-to-base span, adapted for `.rrg-main-inner`'s 2-row grid layout (icon row + search row stacked) — "base of the white bar" means the base of the icon row specifically (86px down the page), not the full header including the search row below it, or the logo would run through the search input. Horizontal centring via `left:50%;transform:translateX(-50%)` since the logo is grid-centred on mobile, not left-aligned like desktop.
   - **Sticky header (mobile only, appears on scroll):** grows its own blue band via a `border-top:20px solid var(--rrg-red)` — a real invented "Utility Bar" for this context, since the fixed hamburger/logo/cart bar has nothing above it once the real Utility Bar has scrolled away. Border sits outside the padding/content box so it doesn't compress the existing hamburger/logo/cart row's own centring. Logo spans from the border's midpoint to the bar's own base, same halfway rule as everywhere else.
   - **Two real bugs caught by measuring against the reference with Playwright, not by eyeballing a screenshot:** (1) `.rrg-logo`'s own box collapses to ~0 size once its only child (the img) is pulled out of flow — for a *centre*-aligned item this silently shifts the apparent anchor point, so every instance above uses `align-self:flex-start`/`start` to anchor to the row's actual top instead. (2) Mobile/sticky specifically: once `.rrg-logo`'s box collapsed to 0 **width** too (no explicit width given there, unlike desktop's 90px), this project's own global `img{max-width:100%}` reset silently clamped the absolutely-positioned img's explicit `width:56px` down to `0px` (100% of a 0-width containing block) — the logo visually vanished entirely, confirmed via `getComputedStyle` showing `width:0px` despite the CSSOM plainly showing `width:56px` in the matching rule. Fixed by giving `.rrg-logo` itself the same explicit width as its img on mobile/sticky too, closing off the same collapse-to-0 root cause the flex-start fix only partially addressed.
   
   Verified with Playwright at 1280px and 390px on Simple and Vehicle-Specific: desktop span confirmed to sub-pixel precision (`logoTop`/`utilityMid` within 0.06px, `logoBottom`/`mainHeaderBottom` within 0.2px), mobile top-of-page span confirmed the same way (`logoTop`≈`utilityMid`, `logoBottom` clear of the search row by ~5.6px), sticky header confirmed exactly (`imgTop:10` = exact midpoint of a 20px border, `imgBottom:70` ≈ `stickyBottom:70.4`) — zero console errors on any of the 6 combinations (2 templates × desktop/mobile-top/mobile-sticky), AU round-trip confirmed clean throughout (no leftover width/position/margin).
22. ✅ **Done 2026-09-11.** Showroom Finder map rework: default view now `fitBounds()`s to show all 35 real stores at once (was a fixed zoom-10 view of 4 fabricated NSW stores), red divIcon pins (`--rrg-red`) for on-display stores per `ON_DISPLAY_STORES`, grey for the rest — not hidden, conveys real network size. Leaflet.markercluster (CDN, same unpkg pattern as Leaflet itself) groups pins when zoomed out and splits them apart on zoom in; cluster bubbles are grey by default and turn red the moment any store inside them is on-display, so that signal is visible before a viewer zooms in (confirmed with Brenton via AskUserQuestion before building — grey-default/red-if-contains-display was his pick over a single flat brand colour or Leaflet's stock green/yellow/orange cluster scheme). Also centralized the data: the 5 duplicated per-template `window.SHOWROOM_MAP_STORES` demo arrays (4 fabricated NSW stores each) are gone — `lat`/`lng` (approximate suburb-centre coordinates, demo precision) were added directly onto every store in the canonical `RRG_STORE_NETWORK` in shared.js, and map pins are now derived from that one real 35-store list + `ON_DISPLAY_STORES`, same source of truth already used by the store slide-out and Click & Collect widget. Popups show the real store name/address instead of a fabricated "status" string. Ships to all 5 templates in one pass, per the already-locked-in scope decision above. Verified with Playwright on Simple and Vehicle-Specific: default zoomed-out view renders real clustered pins across the country with one red NSW cluster (contains Moorebank + Castle Hill); clicking through to full zoom splits it into individual pins, 2 red (Moorebank, Castle Hill) + rest grey, matching `ON_DISPLAY_STORES` exactly (the old demo data had incorrectly also flagged Silverwater as on-display, now fixed by construction since there's only one source of truth); pin popup confirmed showing real address + "On Display"; mobile (390px) stacking and zoom controls unaffected; no console errors on either template. `node --check` clean.
23. ✅ **Done 2026-09-11.** Discontinued/no-longer-available product state — folded into the existing `STOCK_STATUS` state machine (shared.js) as a 5th mutually-exclusive value alongside in_stock/low_stock/not_in_stock/special_order, same principle item 25 already established for Special Order (a discontinued item can't simultaneously be "In Stock," so a layered checkbox would read as contradictory). Selecting "Discontinued" in the Demo State Panel's existing Stock status radio group: strikes through the price in place (`.price-block.discontinued .price-now`, reusing `.price-was`'s grey strikethrough look — keeps "the price it was sold at" on screen rather than hiding it); fully replaces the main decision-panel CTA — `.cta-col` (Add to Cart button + payment badges) hides entirely, with a new `.discontinued-banner` ("⛔ This product has been discontinued and is no longer available for purchase — see similar alternatives below.") in its place, same sibling-insertion pattern as the existing `.stock-banner`/`.special-order-banner`; and reveals a new full-width `.discontinued-alternates` section directly below the hero row (before the Tabs widget) with 3 real, currently-live, genuinely comparable RRG products as suggested alternatives — reuses the existing `.related-grid`/`.product-card` component, no new card CSS needed. The sticky-mobile and persistent-desktop CTA bars keep the existing generic disabled-button path (`[data-cta-label]`), now driven by a new per-state `cfg.ctaLabel` field ("Discontinued" vs. `not_in_stock`'s "Out Of Stock") instead of a hardcoded string. Confirmed with Brenton via AskUserQuestion before building: no region-specific differences (the "was" price just inherits whatever currency formatter is already active); the alternatives are a new section below the hero, not a repurposing of the existing bottom-of-page Related Products grid (which stays untouched); and the alternatives are curated real replacement picks per template, not the first 3 cards reused from each template's existing Related Products data. Real alternative-product data was sourced by live-browsing roofracksgalore.com.au (search + category pages) per template, verifying every image URL resolves (200) before use — not fabricated:
    - **Simple** (Front Runner Pro Water Tank 42L, FRWTAN063): Upright Water Tank/Flat (WTAN007, $346), Slanted Water Tank (WTAN008, $271), Water Tank 67L (WTAN023, $295) — other real Front Runner tank models, avoiding WTAN022 since it's already in this template's own Related Products grid.
    - **Config-variant** (Rhino Rack 6 Series Pioneer Platform 900x1430mm, RH62112): confirmed via its own live product page that it carries no site-native related-products module (same gap noted for this template's original Related Products build) — sourced 3 other real sizes from the same 6 Series Pioneer Platform family (universal trays, not vehicle-specific kits): 1300x1430mm (62111, $1,204.72), 1500x1430mm (62113, $1,371.92), 1300x1240mm (62109, $1,134.32).
    - **Sibling-color** (MAXTRAX MKII Recovery Boards): 3 other real MAXTRAX-brand recovery boards from different tiers — MAXTRAX LITE Black ($179, lighter/cheaper tier), MAXTRAX XTREME Stealth Black and Signature Orange ($519 each, premium tier).
    - **Vehicle-specific** (Rhino Rack Pioneer 6 Platform Kit, Hilux N80, GP01M1TZZ): per the backlog's "prioritise vehicle-fit matches where applicable" instruction — the one template where it's meaningfully applicable, since it's the only template with a fitment concept — 3 real roof rack kits from 3 different brands, each genuinely fitted to a Hilux N80 per the live site's own listings: Front Runner Slimline II Rack Kit (KRTH011T, $409), Yakima LockNLoad Platform 1390x1250mm (RuggedLine, $236.55), Wedgetail Core Platform Roof Rack (WC-THDC16-1412, $1,400).
    - **Grouped-bundle** (Yakima RoadShower 15L Complete Shower & Hose Bundle, 8004109PROMO): standalone-tank and different-brand alternatives — Yakima RoadShower SM 15L (8004109, $361.50, same tank without the bundle wrapper), Yakima RoadShower MD 26L (8004110, $411.50, bigger capacity), Rhino Rack Shower Awning (32149, $379.05, different brand/format).
    
    Ships to all 5 templates in one pass, per this project's convention for Demo State Panel features. Verified with Playwright on all 5 templates: toggling "Discontinued" correctly shows the struck-through price, red discontinued stock-status line, the new banner, and the alternatives section (3 real cards each) while hiding the CTA/payment badges; toggling back to "In Stock" fully restores every element with no leftover hidden/disabled state (spot-checked on Simple); sticky-mobile and persistent-desktop bars confirmed showing a disabled "Discontinued" button (checked on Vehicle-Specific, the template with the most surrounding CTA/fitment state machinery). Zero console errors on all 5 templates. Not yet pushed — working-tree only, per the usual push-after-approval workflow.
24. ✅ **Done 2026-09-11/12.** Compatibility feature — new "Cart contents" radio group in the Demo State Panel (Product State section, right after Stock status), 3 states: Empty (default, no persistence, same as every other toggle), Compatible item in cart (renders nothing — nothing to warn about), Incompatible item in cart (renders a new `.cart-conflict-banner` above Add to Cart via `applyCartConflict()` in shared.js, same `insertBefore(el, col)` insertion pattern as the existing stock/special-order/discontinued banners, styled identically to `.special-order-banner` — amber, black text, informational tier, never disables the CTA). Confirmed with Brenton via AskUserQuestion before building: region-agnostic (no `applyRegion()` hook needed), ships to all 5 templates (not vehicle-specific-only), and — a real clarification from Brenton after the first plan draft — "incompatible" is a **platform-vs-accessory** relationship (e.g. a roof rack platform already in the cart flagged against an accessory-type product being viewed, or vice versa), not a vehicle-fitment or brand-mismatch simulation; in a real Magento build this would be driven by a genuine per-product compatibility rule the team sets, the prototype just needs to surface the message. Per-template conflict item/reason are read generically off `data-conflict-item`/`data-conflict-reason` attributes on each template's own `.cta-col` (shared.js has no per-SKU branching): Simple (accessory) flags "a Rhino-Rack Pioneer Platform"; Config-variant (a platform) flags "a roof accessory"; Sibling-color (accessory) flags "a roof rack platform"; Vehicle-specific (a platform/kit) flags "a roof accessory"; Grouped-bundle (accessory) flags "a roof rack platform" — each with its own plausible reason text. `applyStockStatus()` now re-calls `applyCartConflict()` at the end of every run so the banner correctly disappears when the product is Discontinued (CTA hidden) and correctly restores when stock status returns to something that shows the CTA again. Skipped researching the UK reference site Tim flagged — Brenton confirmed proceeding from the spec's own description was fine. Verified with Playwright on all 5 templates: default Empty state shows no banner; Compatible shows no banner; Incompatible shows the correct per-template banner text with Add to Cart staying enabled; toggling back to Empty removes it cleanly; on Simple, confirmed it stacks correctly alongside an active Special Order banner (both render, correct order) and correctly disappears/restores across the Discontinued toggle; zero console errors on all 5 templates (aside from the pre-existing, unrelated favicon 404). Not yet pushed — working-tree only, per the usual push-after-approval workflow.

**Same-day follow-up, real bug caught by Brenton on review:** the banner's text rendered broken into odd multi-column chunks instead of wrapping as one paragraph. Root cause: `.cart-conflict-banner{display:flex;align-items:center;gap:8px}` — the banner's own content is a plain text run with an embedded `<strong>{item}</strong>`, and flex treats each text/element chunk (the text before `<strong>`, the `<strong>` itself, the text after) as its own anonymous flex item/column instead of one flowing paragraph. Fixed by dropping `display:flex`/`align-items`/`gap` entirely (plain block flow, `line-height:1.4` added) so it wraps as a single paragraph; also dropped the leading ⚠ icon character per Brenton's ask, since it wasn't adding anything once the layout bug was fixed. Re-verified with Playwright on Grouped-bundle (screenshot-confirmed clean single-paragraph wrap). **Note for a future pass:** `.special-order-banner` has the identical icon+`<strong>`+text shape and is likely subject to the same latent bug — not touched this session since Brenton only flagged the compatibility banner, but worth fixing the same way if it comes up.
25. ✅ **Done 2026-09-11.** Special Order status now fully replaces the stock-status label rather than layering a banner on top of whatever it said. Folded into the existing `STOCK_STATUS` state machine as a 4th mutually-exclusive value alongside In Stock/Low Stock/Not in Stock (was a separate, independent "Special order item" checkbox) — selecting it in the Demo State Panel's "Stock status" radio group swaps `.stock-status-line` to "⏱ Special Order — Ships in 5-7 Days" (amber, same token as Low Stock) and shows the existing `.special-order-banner` detail copy above Add to Cart; Add to Cart stays enabled per the standing "informational, not a purchase blocker" rule. `applySpecialOrderFlag()` was removed — `applyStockStatus()` now drives both banners (`.stock-banner` for Not in Stock, `.special-order-banner` for Special Order) off one `cfg` object, same pattern as the existing `blocksCta` field. Confirmed with Brenton: no region-specific differences for this item. Verified with Playwright on Simple (full In Stock → Special Order → In Stock → Not in Stock round trip, confirming the two banners never appear together), Vehicle-specific, and Sibling-color (confirmed its own colour-driven `renderStock()` correctly respects the existing `stockOverride` guard and doesn't clobber the Special Order state on a swatch switch) — zero console errors on all three.
26. ✅ Sticky Add-to-Cart bar (all 5 templates): `.sticky-cta-mobile` used to just show unconditionally at mobile widths regardless of scroll position (not actually scroll-aware at all). Replaced with `initStickyCta()` in shared.js — a single IntersectionObserver on the real Add to Cart button that shows the bar whenever that button isn't on-screen, covering both "starts below the fold on load" and "scrolled past it" with one rule. Removed the per-template inline `display:none`/`!important` overrides (simple, grouped-bundle, vehicle-specific) that were fighting the old width-only rule; `body.has-sticky-cta` now reserves the bottom padding only while the bar is actually visible, instead of a permanent gap.

**Not build items — process notes, carry forward but don't action as page work:**
- Font polish (sizing, 14pt body-text floor per the brand guidelines) is deliberately last, once every layout position above is finalized — same convention as earlier sessions.
- Product-attribute configuration (assembled/flat pack, colour picker vs. grid-select) needs to be modeled as attribute+values in Rackety — Salem is likely the right person for that conversation, not a prototype-side change.
- Recently Viewed reconfirmed out of the decision panel — if it surfaces at all, it's at the bottom of the mini-cart, not this page.
- Rackety bin-location setup (the real trigger for a genuine on-display flag) is Brenton's own follow-up with Chris — flagged here so a future session doesn't mistake it for front-end work.
- **Standing process change, 2026-09-11: `DEVELOPER-BRIEF.md` screenshots are held until every template is 100% complete, then captured in one single final pass — not per section, and not once an individual section looks stable.** Prompted by the Region Selector screenshot set going stale within the same session it was captured (Design Rationale visible in every shot, wrong UK store data) while Brenton was still reviewing and finding more corrections; tightened further the same day after Brenton clarified the first version ("once that section is stable") wasn't strict enough — a finished section can still sit next to an unfinished one in the same frame, or the page can shift again before the whole template settles, so per-section timing was never actually safe. **This is retroactive**, not just for new sections: the two sections that already had real screenshots (Section 2, REVIEWS.io; Section 3, Showroom map) are flagged in `DEVELOPER-BRIEF.md` itself to be recaptured in that same final pass too, even though nothing about either is expected to change — the goal is one consistent, same-date image set for Mark, not a patchwork of capture dates. Going forward: write a section's text/tables/code as soon as the decisions are confirmed (even ahead of the actual build, flagged as "not built yet" where relevant, same as Section 4's item 27–34 content), but hold every screenshot — using `dev-brief-assets/screenshot-pending.svg` placeholders in the meantime — until all 5 templates are done and ready to hand over. Also logged in `DEVELOPER-BRIEF.md` itself (Section 0, and Section 0.1 rule 2).

### D. Region Selector data corrections (2026-09-11, caught reviewing the item-21 screenshots)

Brenton spotted these live while reviewing the Region Selector screenshots just captured for `DEVELOPER-BRIEF.md` Section 4. **Deliberately captured here first and not yet built or re-screenshotted** — Brenton is still reading through the page looking for more before this batch gets actioned in one pass, same "agree the list, then build" approach as the rest of this backlog.

27. ✅ **Done 2026-09-11.** Removed the "Design Rationale" section entirely from all 5 templates (`<section class="rationale">` block + the now-dead `.rationale` CSS in shared.css) — it was never meant to ship, a build-time scratchpad documenting each template's design decisions for internal review. Also removed the stray "Each template page has its own 'Design Rationale' section..." line from `prototypes/index.html`'s overview copy, and reworded a stale JS comment in vehicle-specific that referenced it. No region-specific handling needed — confirmed with Brenton, identical removal across AU/NZ/UK.
28. ✅ **Done 2026-09-11.** Trust Row phone number is now region-aware — new `REGION_PHONE` map in shared.js (AU `1300 071 264`, NZ `09 481 1910`, UK `01204 899778`), applied in `applyRegion()` as its own pass separate from the `REGION_TRUST_COPY` founded/network loop, since this item's markup nests a `<a href="tel:...">` inside its `<p>` (`Contact us on: <a>...</a>`) — a plain `p.textContent` swap, as used for the other two trust items, would have destroyed the link. Added `data-trust="phone"` to the 4th trust-row item on all 5 templates so it's addressable the same way as the other two. Both the visible number and the `href` update together. Verified with Playwright on Simple and Vehicle-Specific: AU/NZ/UK all show the correct number+href, AU round-trips cleanly, zero console errors.
29. ✅ **Done 2026-09-11.** UK single-store region corrected from the placeholder "London" to the real store — `REGION_SINGLE_STORES.UK` in shared.js now carries real data: name **Bolton**, address (Unit B9, Edge Fold Industrial Estate, Plodder Lane, Farnworth, Bolton, BL4 0LR), phone (`01204 899778`), and coordinates (`53.5503, -2.3882`). Confirmed the predicted multi-spot fix: changing `.name` alone corrected both the Showroom Finder heading and the Click & Collect "On display at the ___ Store" line, no separate code changes needed for either. Also caught and fixed one more stale "London" reference the original item didn't list — the Trust Row's `REGION_TRUST_COPY.UK.network` copy ("Check it out at our London showroom") was a separate hardcoded string, not derived from `REGION_SINGLE_STORES`, so it needed its own one-line fix to say "Bolton." The Showroom map's single-pin popup (`renderShowroomMapPins()`) now shows the real street address instead of the old "TBD — placeholder for prototype" note text. Verified with Playwright on Simple (screenshot confirmed: heading, map popup, Trust Row copy, and B-Stock/Ex-Demo store-name copy — `exdemoStoreName()` — all correctly read "Bolton") and Vehicle-Specific — zero console errors on either.
30. ✅ **Done 2026-09-11.** NZ single-store region now carries real data (was already correctly labelled "Auckland," just with fabricated coordinates and no address/phone) — `REGION_SINGLE_STORES.NZ` in shared.js: address (195A Wairau Road, Wairau Valley, Auckland, 0627), phone (`09 481 1910`, shared with item 28's Trust Row fix), coordinates (`-36.7747, 174.7381`). Same verification pass as item 29 above (built and tested together, same source data).
31. ✅ **Done 2026-09-11.** Zip Pay hidden for NZ — the payment-badges markup on all 5 templates was rebuilt as 3 generic `.payment-badge` slots (no more `pb-afterpay`/`pb-paypal`/`pb-zip` classes or `data-pb-amount` identity attributes, since a slot's provider identity is no longer fixed once it can vary per region — see item 31a). `paymentBadgeSet(region)` in shared.js returns NZ's set as just 2 entries (Afterpay + PayPal, unchanged "Pay in 4" math); the rewritten `syncPaymentBadges()` hides whatever slot index has no matching entry, generically, so NZ's shorter array is all that's needed — no NZ-specific branch. The row stays centered with no gap since `.payment-badges{display:flex;justify-content:center}` + `.payment-badge{flex:1}` collapse a `hidden` 3rd child out of the layout on their own; no CSS changes were needed.
31a. ✅ **Done 2026-09-11.** UK payment badges — full 3-provider swap. `paymentBadgeSet('UK')` returns Clearpay (÷4, "4 payments of £X", same structure as Afterpay) + PayPal (÷3, "3 payments of £X" — genuinely different math from AU/NZ's ÷4) + Klarna (÷3, "3 payments of £X", confirmed with Brenton to use the same phrasing pattern as the other badges rather than a "spread over 3 months" framing — Klarna's own logo/label is what distinguishes it from PayPal, not different copy). The real `clearpay.svg`/`klarna.svg` logos staged in an earlier session are now referenced for the first time. Removed the now-dead `.payment-badge.pb-zip .pb-logo{height:13px}` CSS rule (Zip's own instance keeps that height via a per-entry `height` field in its `paymentBadgeSet()` config instead, applied as an inline style — the only provider that needed a size tweak from the default 15px). `applyRegion()` now re-syncs every `.price-block`'s badges on every region switch (not just on price-changing renders), since the provider **set itself** changes, not just the amount. Verified with Playwright on Simple and Vehicle-Specific: full AU→NZ→UK→AU round trip (AU/NZ/UK-restored all show the correct provider set, logos, and instalment math; NZ shows exactly 2 centered badges with no gap; UK's PayPal badge confirmed computing ÷3 not ÷4); a config-variant option switch mid-UK-session correctly recomputed all 3 UK badges off the new price (£780 → Clearpay "4 payments of £195.00", PayPal/Klarna "3 payments of £260.00"); no horizontal overflow at 390px across the full round trip; zero console errors on any template. **Follow-up same day, real bug caught by Brenton on review:** the Clearpay logo rendered visibly smaller/thinner than PayPal or Klarna alongside it — root cause was `clearpay.svg`'s own downloaded canvas, not the badge CSS: its `viewBox` was `0 0 1869.6 838.5`, but the actual wordmark's ink only occupies `x:341.5 y:308.9 w:1186.5 h:220.8` of that (confirmed via `getBBox()` in a scratch measurement page) — about 27% of the canvas height, with huge built-in brand clear-space padding baked in by whoever exported it. Since every payment logo renders at the same fixed pixel `height` and scales its width proportionally, a loosely-cropped canvas makes the actual glyph render much smaller than a tightly-cropped one at the identical nominal height. PayPal/Afterpay/Klarna were all confirmed already tightly cropped (their `getBBox()` nearly matches their full viewBox); Zip has some padding too, already compensated by its existing `height:'13px'` override in `paymentBadgeSet()`. Fixed by tightening `clearpay.svg`'s own `viewBox` to `320 288 1230 260` (content bbox + a small even margin) — no code or CSS changes needed, since the fix lives entirely in the asset. Re-verified with Playwright (server restarted on a fresh port first — browser image caching masked the fix on the first reload, same gotcha noted elsewhere in this file): Clearpay now renders at a comparable visual weight to PayPal/Klarna, AU's Afterpay/PayPal/Zip trio unaffected, no 390px overflow.
32. ✅ **Done 2026-09-11.** Header utility bar "Your Nearest Store" is now region-aware — new `applyRegionNearestStore()` in shared.js, called from `applyRegion()`, follows the same restore-on-AU caching pattern as `applyRegionShowroomHeading()` (item 21): caches each template's real AU value ("North Lakes") on first run so switching back restores it exactly, and reads NZ/UK's name straight from the existing `REGION_SINGLE_STORES` (Auckland/Bolton, already driving the Showroom heading and Click & Collect copy — no new data needed). A new `data-region-nearest-store` attribute was added to the existing `<a data-admin-trigger>North Lakes</a>` link on all 5 product templates, alongside (not replacing) `data-admin-trigger` — that click handler, which doubles as the mobile Demo-State-Panel trigger since the FAB is hidden on mobile, is untouched; only the visible text changes. `prototypes/index.html` (the internal template gallery page) has no Region Selector at all, so it's out of scope. Verified with Playwright on Simple and Vehicle-Specific: reads North Lakes/Auckland/Bolton correctly through a full AU→NZ→UK→AU round trip, restores the real per-template AU text on return, zero console errors.
33. ✅ **Done 2026-09-12 — scope expanded well beyond the original "redo the screenshots" ask.** With item 24 landing 2026-09-12, Brenton confirmed all 5 templates are hand-over-ready, unblocking the standing "screenshots wait for 100% complete" gate — but mid-session he widened this item into a full rebuild of `DEVELOPER-BRIEF.md` into a genuinely complete Magento handover document: every section/widget on every template (header excluded, per the existing standing rule), what each does, where every link goes, region differences, typography/CSS per element, and desktop/mobile layout — not just the 3 features (REVIEWS.io, Showroom map, Region Selector) it covered before. Confirmed via `AskUserQuestion` before building: structure = one shared **Component Library** (every widget documented once) + a **Page Layout** section per template (structure/order/general proportions, not exact pixel specs) — avoids duplicating shared-widget writeups 5 times, matches how a Magento dev would actually build (shared blocks + template overrides); build order = Page Layout sections first, then Component Library, in `PAGE-GLOSSARY.md`'s own order; dimensions = general proportions, not measured pixel specs. Two more requirements folded in after the plan was already approved: every entry documents its real typography/CSS (heading level, font, size, colour token — pulled from `shared.css`'s actual rules, not guessed), and a stricter screenshot process — the Demo State Panel/admin FAB is used to switch states but must never appear in a captured frame, and every "location" screenshot (showing where a widget sits) must be a **full page** capture with a red arrow, never just a cropped neighbourhood; only state/variant "detail" shots may be cropped.
    - **New Section 5 — Typography & colour reference**: the real shared type scale (headings, body, price, buttons, labels) and colour tokens (including the UK region's navy override and the Add to Cart gold's deliberate exemption from it), pulled from `shared.css`, so every later section can just say "uses the shared heading style" instead of repeating the same spec 20+ times.
    - **New Section 6 — Page Layout**, one subsection per template (6.1–6.5): section order top-to-bottom (starting at Breadcrumbs, since header stays out of scope), which widgets are present vs. absent and why, general desktop/mobile proportions (confirmed via the real CSS, e.g. the Hero's `1.15fr/1fr` ≈55/45 split is identical across all 5 templates), and a real desktop (1280px) + mobile (390px) full-page screenshot per template (10 screenshots total).
    - **Section 4's stale text fixed**: the status banner, cascade table rows 9–11, and Sections 4.4/4.5 all still said "not built yet" for items 28–32, which are actually done — corrected throughout, plus the `applyRegion()` reference comments and 4.6's placeholder explanation (rewritten past-tense now that real screenshots exist).
    - **Sections 2, 3, and 4's screenshots recaptured** (the original scope of this item): Reviews tab location/live-empty/populated-example (the "populated" shot turned out not to need mocking — RRG's real Company Reviews sub-tab has 1,878 real reviews), star-rating badge location/hidden-default/populated-example (mocked, since no demo SKU has real reviews yet), Showroom map location/split-pins-with-popup, and all 9 Region Selector shots (closed/open location shots redone as full-page+arrow per the new process; AU/NZ/UK full-page; UK/NZ header and Showroom close-ups) — all on the Simple template, all real interactions (typed postcodes, clicked map clusters/pins, switched regions), zero console errors throughout (aside from the pre-existing unrelated favicon 404).
    - **New Section 7 — Component Library, 29 entries**, one per widget named in `PAGE-GLOSSARY.md` (plus Discontinued/Cart-Conflict/Special-Order-as-exclusive-state and Short Description, which postdate `PAGE-GLOSSARY.md`'s own last update) — each with Name/Location/Purpose/Typography/Region differences/real-vs-placeholder Links/States, backed by real screenshots (a further ~55 captures: Delivery/Click & Collect + Store Slide-out + On Display pill together via a real typed postcode; Payment Badges; Brand Logo + SKU copy; Main Gallery; Get It Installed's paired and no-video states; Sale Tag; all 6 Tabs sub-panels; FAQ with one item expanded; Related Products; Fitment Status in all 3 states — fits/doesn't-fit/confirm-your-vehicle — plus Rack Fit Guarantee and Product Notes; Variant Picker; Colour/Swatch Grid; Package Contents on both Vehicle-Specific's placeholder-quantity and Grouped-Bundle's real-quantity versions; Special Order, Discontinued (both states), Cart-Conflict, and Ex-Demo/B-Stock's slide-in, all via real Demo State Panel toggles; Sticky Mobile Bar; Persistent Bar; Short description). Real gaps flagged honestly rather than glossed over — e.g. Related Products' cards don't link anywhere and its Add to Cart button isn't wired up (no real cart in this prototype), What's Included's component names are still `href="#"` placeholders, Gold Guarantee's tab content has no links at all since the fidelity pass restored full real copy (superseding `PAGE-GLOSSARY.md`'s now-stale "Learn more" description).
    - Verified with Playwright throughout: all ~78 total screenshots (10 Page Layout + 17 recaptured Sections 2–4 + ~55 Component Library, minus a couple of shared/reused images) confirmed non-broken via `naturalWidth` checks after loading `dev-brief-viewer.html` over a local server, zero stale "not built yet"/"Screenshot pending" text remaining anywhere in the rendered document, Demo State Panel confirmed absent from every captured frame, "location" shots spot-checked as genuine full-page captures with correctly-positioned arrows.
    - **Not pushed to git and `DEVELOPER-BRIEF.md`'s own build-status banners were not touched beyond what's described above** — per the standing gate, both still wait on Brenton's separate explicit sign-off.
34. ✅ **Done 2026-09-11.** Get It Installed panel's region differences, revisited now item 16's redesign has landed. Confirmed with Brenton: the two trust bullets ("Over 200,000 roof racks fitted"/"Nationwide network of trained fitters") stay **identical across all regions** — no `REGION_TRUST_COPY`-style copy swap. The CTA link (currently hardcoded to the full AU domain, `https://www.roofracksgalore.com.au/roof-rack-installation-and-fitting-costs`) is the one real fix: since this will be a genuine global URL in production (each region is its own Magento store view/domain), it's now a **relative path**, `/roof-rack-installation-and-fitting-costs`, instead of a hardcoded absolute AU URL — so it resolves correctly against whichever domain actually serves the page, with no per-region JS branching needed at all. Changed in the 4 templates with a static href (Simple, Config-Variant, Sibling-Color, Grouped-Bundle) plus `applyFitGalleryFlag()`'s `fallbackLink()` helper in shared.js, which drives Vehicle-Specific's CTA in its "0 fitments"/"gallery off" states (its "5+"/"1–4" states link in-page to `#fitGallerySection` and were already region-agnostic). `target="_blank" rel="noopener noreferrer"` left unchanged on all of them. No new `applyRegion()` hook was needed — this resolves as a structural fix rather than a new cascade branch, since a relative URL is inherently correct per-domain once each region is a real separate site. `.btn-outline` was reconfirmed to use `--rrg-black` (not `--rrg-red`), so the button's look was already unaffected by the UK brand-skin colour override. Verified with Playwright on all 5 templates: correct relative href + unchanged target/rel on the 4 static templates; Vehicle-Specific's 4 fitment-count states (5+, 1–4, 0, gallery off) each behave correctly, with only the 0/off states picking up the new relative URL; Simple's AU→NZ→UK→AU region round trip confirmed the CTA is unaffected by region switching (expected, was never wired into the cascade); zero console errors across all 5 templates (aside from a pre-existing, unrelated favicon 404).

**Confirmed by Brenton, 2026-09-11: items 27–34 above are the complete list of region-specific changes found on the PDP pages** (pending his final look before this batch gets actioned) — this isn't a partial list waiting on more items from unrelated areas of the page.

## 11. Developer Brief (handover doc)

**2026-09-13:** The header/utility-bar markup embedded in every template here is a placeholder only (inert nav links, dev tooling mixed into the visible nav) — it now has its own separate project, spec, and eventual dev brief: see `header-spec.md`. Don't extend or "fix" the current placeholder header as part of PDP work; header changes belong in that file.

`DEVELOPER-BRIEF.md` (project root) is the internal-developer-facing handover document for building the real Magento 2 templates from these prototypes — instructions, screenshots of where each widget sits on the page, and copy-paste-ready code for external integrations (starting with REVIEWS.io). It's separate from this spec: `spec.md` is the build log/backlog aimed at whoever is directing this prototyping work; `DEVELOPER-BRIEF.md` is aimed at the person who will actually build the production templates and doesn't need this file's full history to do that.

**Convention going forward:** whenever a section of `DEVELOPER-BRIEF.md` is added or updated, note it here with a one-line pointer (date + what changed), rather than duplicating its content into this file.

- **2026-09-11:** Created `DEVELOPER-BRIEF.md`. First section covers the REVIEWS.io integration in full — both the Reviews tab (Polaris widget embed, exact config, per-template SKU table) and the Decision Panel star-rating badge (custom-built, since REVIEWS.io has no widget for that compact placement) — including the no-fallback-to-store-wide-rating behavior (Section 5/9). Screenshots live in `dev-brief-assets/`.
- **2026-09-11:** Added Section 0.1 — documentation rules for every section in the brief going forward: plain-English writing (no jargon), a screenshot for every visual state/variant of a widget, at least one whole-page screenshot per widget with a red arrow marking its location, and a mandatory Name/Location/Purpose structure. A section template is included.
- **2026-09-11 (same day):** Retrofitted Section 2 (REVIEWS.io) onto the new 0.1 template — both the Reviews tab and the star-rating badge now have explicit Name/Location/Purpose lines and a new whole-page "location" screenshot with a red arrow, in addition to their existing per-state screenshots. New screenshots added to `dev-brief-assets/`: `reviews-tab-location.png`, `star-rating-location.png`.
- **2026-09-11 (same day):** Added `dev-brief-viewer.html` (project root) — a standalone page that fetches `DEVELOPER-BRIEF.md` live and renders it (headings, images, tables, syntax-highlighted code blocks, a sticky table-of-contents with scroll-spy) using marked.js + highlight.js from CDN, styled in RRG brand colours/fonts. No build step and nothing to keep in sync by hand — `DEVELOPER-BRIEF.md` stays the only file anyone edits; this page always reflects whatever is currently in it. Must be viewed over a local web server (e.g. `python -m http.server` from the project root), not opened directly as a file, because browsers block a page from reading local files via `fetch` under the `file://` protocol — the page detects this and shows an on-screen instruction if so.
- **2026-09-11 (same day):** Added Section 3 — Showroom Finder interactive map, covering the backlog-item-22 rework (default zoomed-out view, red/grey divIcon pins, Leaflet.markercluster grouping). CDN tags, the pin/cluster-icon JS, and the colour CSS are all copy-paste ready; also flags the two real data gaps that still need a production source before this leaves prototype stage (geocoded store coordinates, and a real per-store/per-product on-display boolean). New screenshots in `dev-brief-assets/`: `showroom-map-location.png`, `showroom-map-pins-split.png`.
- **2026-09-11 (new session):** Added a "Region-specific heading copy" note to Section 3, flagging that the Showroom Finder's `<h3>` heading is AU-only today ("On Display At N Stores Nationwide") and needs per-region variants once the Section 10 item 21 region selector is built — NZ: "On Display At the Auckland Store", UK: "On Display At the [X] Store" (real store name TBD). Prompted by the AU copy itself being changed the same session (see below).
- **2026-09-11 (same day, per-region breakdown session):** Added Section 4 — Region Selector & Multi-Region Support, covering backlog item 21 in full: dropdown mechanics, the 8-row per-widget region cascade table (utility bar label, DC default tab + view-all links, Showroom Finder heading + map, Trust Row copy, currency, brand skin), and the UK Brand Skin/currency implementation (colour token override, logo swap + its positioning CSS, currency symbol sweep). Updated the "Region-specific heading copy" note in Section 3 to point at the new section now that it's built rather than still-pending. New screenshots in `dev-brief-assets/`: `region-selector-location.png`, `region-selector-dropdown-open.png`, `region-au-fullpage.png`, `region-nz-fullpage.png`, `region-uk-fullpage.png`, `region-uk-header-closeup.png`, `region-uk-showroom-closeup.png`, `region-nz-header-closeup.png`, `region-nz-showroom-closeup.png` (all captured live via Playwright against `prototypes/simple/index.html`, one representative template across all 3 regions per the agreed screenshot-coverage scope). Flagged explicitly in the new section: this whole feature previews a hypothetical future capability (RRG doesn't currently sell into NZ/UK), not a shipped one — worth the business confirming intent before this is treated as a real build spec.
- **2026-09-11 (same day, later in the review session):** Reworked Section 4 after Brenton's screenshot review surfaced real problems in the set above (Design Rationale visible in every shot, plus the UK store data itself was wrong — see Section 10.D items 27–34). **Deleted the 9 screenshots listed above entirely** — they no longer match reality and won't until items 27–33 are built — and replaced every image reference in Section 4 with a new placeholder, `dev-brief-assets/screenshot-pending.svg`. Updated the section's written content (the cascade table, the `REGION_SINGLE_STORES`/`REGION_TRUST_COPY` reference code, plus two new subsections — 4.4 Region-specific store & contact data, 4.5 UK payment provider differences) to the corrected, client-confirmed values from items 27–34, clearly flagged throughout as "confirmed, not yet built" rather than presented as live prototype behaviour. Added a status banner at the top of Section 4 making the built-vs-not-built split explicit. This is also a standing process change going forward, not a one-off for this section — see Section 0.1 rule 2 in `DEVELOPER-BRIEF.md` itself and the matching process note in this file's Section 10 "Not build items" list.
- **2026-09-11 (same day, tightened once more):** Brenton corrected the policy above — "once that section is stable" wasn't strict enough, since a finished section's screenshot can still frame an unfinished neighbour, or shift again before the whole template settles. **Tightened Section 0's overview and 0.1 rule 2 to: every screenshot in the entire document waits for all 5 templates to be 100% complete and hand-over-ready, captured in one single final pass — not per section.** Added explicit notes to the top of Section 2 and Section 3 flagging that their existing real screenshots (still accurate today) will be recaptured in that same final pass too, for date-consistency across the document, not because anything in either section is wrong. Section 4's status banner and its 4.6 explanation were updated to match the tightened wording.
- **2026-09-12: the final screenshot pass (Section 10 item 33) — expanded mid-session into a full rebuild, not just a recapture.** Section 4's stale "not built yet" text fixed throughout (status banner, cascade table, 4.4/4.5, `applyRegion()` reference comments, 4.6 rewritten past-tense); Sections 2–4's screenshots recaptured for real (Reviews tab, star-rating badge, Showroom map, all 9 Region Selector shots). Then, per Brenton's expanded ask: **new Section 5** (Typography & colour reference — the real shared type scale and colour tokens, so later sections don't repeat the same spec 20+ times) and **new Section 6** (Page Layout, one subsection per template — section order, absent widgets, general desktop/mobile proportions, a real full-page desktop+mobile screenshot each). **New Section 7 — Component Library, 29 entries**, one per widget named in `PAGE-GLOSSARY.md` (plus a few newer ones it doesn't cover yet — Discontinued, Cart-Conflict, Short Description), each with real screenshots, typography, region differences, and honest real-vs-placeholder link status. Full item-33 write-up in Section 10 above has the complete detail (screenshot counts, process notes, real gaps flagged). ~78 screenshots total in `dev-brief-assets/` now. Not pushed; `DEVELOPER-BRIEF.md`'s build-status framing otherwise untouched beyond what's described — still gated on Brenton's separate sign-off before anything further changes.
- **2026-09-12 (Section 12 bulk batch):** Text corrections across the brief from the dev-brief review backlog (Section 12/Section 9 above have the complete detail) — §6.1/6.2/6.3/6.5/6.4/§7.27 updated for the Persistent Bar's rollout to all 5 templates; §6.2/6.3/§7.5/§7.6 reworded so variant/colour switching correctly describes real production navigation rather than the prototype's in-page demo swap; §6.4's "kits" → "Vehicle Rack Sets"; §7.3 badge location + new click-to-Gold-Guarantee-tab Links line; §7.4/§7.18 gained Rackit data-source notes; §7.17 gained a Gold Guarantee content-source note and a Reviews-tab cross-reference; §7.9 and §7.7 corrected for the real on-display/DC-tab region behaviour; §7.15 corrected to match actual (fully-hidden) video-slot behaviour; §7.19/§7.22 now document intended Related-Products/Discontinued-alternates click-through; §7.20 rewritten to drop an incorrect claim and an unnecessary named reference; §7.22 notes the stock-status-line is now hidden (not duplicated) in the Discontinued state. A global pass (0.1, 4.6, Section 3, §7.20, and elsewhere) removed backlog-item-number citations, meeting references, and named confirmations, restating each as current, correct behaviour — this was itself one of the batch's items (spec.md Section 12 item 14). Not pushed; brief's own build-status framing otherwise untouched, still gated on Brenton's separate sign-off.
- **2026-09-12 (Section 12 item 31 scoped via `/backlog`):** Added new **Section 8 — SEO & Structured Data**, scoping the previously-unscoped "full-page SEO + AEO pass" into 7 concrete findings: missing meta description, missing canonical URL, missing `BreadcrumbList` schema, prototype-style `<title>` tags, a few empty `alt=""` attributes, `Product` schema's `priceCurrency` hardcoded to AUD and not following the Region Selector (flagged separately as region-specific), and `aggregateRating`/`Review` schema — not actually data-blocked, since the real REVIEWS.io rating/review-count data the star-rating badge (§2.2) already fetches live could drive it. Documentation only, written directly per Brenton's explicit sign-off for this item — nothing built in `prototypes/`. Not pushed; still gated on Brenton's separate sign-off before anything further changes.
- **2026-09-12 (post-items-17/20/24/38 re-sync, Brenton's explicit ask — "replace everything we've worked on since," not just the 4 backlog screenshot gaps):** Refreshed every part of the brief left stale by features built after the last mass-capture pass but before their own screenshots/text were revisited:
  - **New §7.30 — Price Block**, documenting the Now/RRP/Save redesign (Section 12 item 23) in full, which had a `PAGE-GLOSSARY.md` entry but no Component Library writeup at all. 3 new screenshots (on-sale/off-sale desktop, mobile with the gallery-mounted Sale Tag).
  - **All 10 §6 Page Layout screenshots recaptured** (desktop + mobile × 5 templates) — every one predated the pricing redesign and still showed the old single-line price.
  - **§7.16 Fitted Photos Gallery rewritten** — text still described the old flat in-page grid; didn't mention the two-view (grid → detail, Prev/Next, live Rack Components list) slide-out drawer built the same day (item 26). 2 new screenshots added.
  - **§7.2/§7.3 Fitment Status / Rack Fit Guarantee screenshots recaptured** — predated the badge's icon swap (item 12). **Real bug found and fixed while recapturing, not previously caught:** `applyFitmentState()` (`shared.js`) hid the badge via `badge.hidden = state !== 'fits'`, but the badge is an `<svg>` and this browser doesn't reflect the boolean `hidden` IDL property to the actual `hidden` content attribute on SVG elements — so the setter silently no-opped and the badge stayed visible in every fitment state (Doesn't Fit, Confirm Your Vehicle), contradicting §7.3's own documented behaviour and every prior session's "verified — badge correctly hides" Playwright checks (which had only asserted the IDL property, not the resulting computed `display`). Fixed with `badge.toggleAttribute('hidden', state !== 'fits')`, which works correctly regardless of namespace; re-verified via `getComputedStyle` (not just the IDL property this time) across all 3 states on all 3 badge instances (Decision Panel, Sticky Mobile Bar, Persistent Bar). §7.3's stale "region differences: none confirmed yet" line also corrected to state plainly that the badge's colour is tied to the `--rrg-fits` token, not region.
  - **§7.28 Short description rewritten** — text still described the old "Read more jumps to the Details tab" behaviour; it's actually been an animated in-place expand/collapse (`toggleShortDesc()`) since item 40. New "expanded" state screenshot added.
  - **§7.26/§7.27 Sticky Mobile Bar / Persistent Bar gained a second screenshot each** — both widgets now have two visually distinct variants across templates (Vehicle-Specific's fitment-box version vs. the other 4's plain thumbnail+name+price+CTA version, the latter added by items 1/39) but only the Vehicle-Specific variant had ever been captured.
  - **§7.20 Package Contents (Grouped-Bundle)** — real screenshot captured, replacing the `screenshot-pending.svg` placeholder that had been held since a Demo State Panel framing bug (Section 12 item 34).
  - All new captures verified with the Demo State Panel FAB hidden from frame; `dev-brief-viewer.html` reloaded and checked (`naturalWidth`) — 84 images, zero broken. Not pushed; `DEVELOPER-BRIEF.md`'s own build-status framing otherwise untouched, still gated on Brenton's separate sign-off for anything beyond this kind of screenshot/text sync.
- **2026-09-12 (same-day follow-up — full-brief audit against `PAGE-GLOSSARY.md`):** Brenton asked whether any built feature was missing from the brief entirely, not just stale. Cross-checked every Section/Widget named in `PAGE-GLOSSARY.md` against §7's Component Library — found 2 real, built-since-day-one features with **no dedicated entry anywhere** (only mentioned in passing, inside other sections' text): **Trust Row** (new §7.31 — the 4-icon strip, region-aware copy/phone number via `REGION_TRUST_COPY`/`REGION_PHONE`) and **Breadcrumbs** (new §7.32 — 3-level vs. Vehicle-Specific's 4-level trail, `.rrg-crumbs` naming-gotcha note, placeholder `href="#"` links). Both added with real screenshots. Not pushed yet.
- **2026-09-12 (same-day follow-up — full Component Library reorder, Brenton's request):** Every widget in the brief was renumbered/reordered top-to-bottom by real on-page position (previously ordered by build history — REVIEWS.io/Showroom map/Region Selector were separate, differently-numbered Sections 2–4, and §7's own 32 entries were in whatever order they'd been added). Confirmed scope via `/backlog`-style `AskUserQuestion` before touching the file: fold Sections 2–4 into one continuous Component Library (now Section 4, 36 entries), with the Region Selector placed first (its cascade affects many widgets below it, even though its own dropdown lives in the out-of-scope header) and the Demo State Panel placed last (reviewer tooling, not part of the real page). Top-level sections renumbered: Typography 5→2, Page Layout 6→3, Component Library 7→4 (Reviews tab and Showroom Finder map folded in as their own entries at their real page positions, Region Selector's old 4.1–4.7 sub-headings converted to plain bold sub-headings within the new single entry), SEO 8→5. Every internal cross-reference (`§7.x`, `Section 2/3/4.x`, etc.) updated to match — done as one full-file rewrite (Write, not sequential Edits) given the scale, then verified via `dev-brief-viewer.html`: 86 images load, zero broken, heading sequence 4.1→4.36 confirmed unbroken. **Two stale-content bugs caught and fixed while moving entries, unrelated to the reorder itself:** Sale Tag's own entry (formerly §7.12) still only described the old price-block-only desktop position and had never been updated for the mobile gallery-image variant built during the earlier Price Block re-sync pass this same day — fixed. Package Contents' (formerly §7.20) "Location" line said "below the price/stock line," but the real DOM order (checked directly in `vehicle-specific/index.html`) has it after the Delivery/Click & Collect widget, the actual last item in the Decision Panel — fixed. Historical dated log entries elsewhere in this file (this section included) were **not** retroactively renumbered to match — they're a point-in-time record of what was true when written, same convention as a git commit message. Not pushed yet.

## 12. Dev-brief review backlog — 2026-09-12 (Brenton's first pass over `DEVELOPER-BRIEF.md`)

Logged during a read-through of the newly-rebuilt `DEVELOPER-BRIEF.md` (Section 10 item 33). **Capture-only pass — nothing below has been built yet.** Status key: ⬜ not started · 🤔 open decision needed before it can be built · 💬 flagged for a larger, separate discussion, not scoped yet.

Screenshot process note (reconfirmed, not new): as each item below gets actually built, update the written brief content (text/tables/code/data-source notes) immediately, but swap the affected screenshot(s) to `dev-brief-assets/screenshot-pending.svg` rather than leaving a stale/incorrect real image in place. Hold all real screenshots for one single final mass-capture session once this whole backlog is done — same approach used for Section 4 previously. That final pass must also include an explicit check of every existing screenshot in the document for the Demo State Panel being visible in frame (see item 12 below — one already slipped through the last "final" pass).

1. ✅ **Done 2026-09-12.** **Persistent Bar (desktop) should ship on all 5 templates, not just Vehicle-Specific** — only the fitment-status portion is conditional (present on Vehicle-Specific, omitted elsewhere), same pattern as the Sticky Mobile Bar already uses on mobile. Currently wrong in four places in `DEVELOPER-BRIEF.md`: §6.1/6.2/6.3/6.5's "Absent widgets" lists, §6.4's Vehicle-Specific section-order/desktop-layout text ("this is the only template with a desktop condensed bar..."), and §7.27's Location/Purpose lines. Fix the prototype (`shared.js`/`shared.css` + all 5 templates' markup) first, then a single find-and-fix pass across all four doc locations.

**Real bug found and fixed same day, caught by Brenton on review:** the bar wasn't appearing on the 4 new templates at all. Root cause: `.persistent-bar` in `shared.css` never had a `top` value — Vehicle-Specific's original markup carried an inline `style="top:0;"` that made it work, which wasn't obvious was load-bearing and wasn't copied onto the 4 new templates' bars. Without it, `position:fixed` with `top:auto` resolves to the element's static (in-flow) document position instead of the viewport top, so the bar was technically present and toggling `.visible` correctly, just rendering thousands of pixels down the page instead of pinned to the top — invisible in practice. Fixed by moving `top:0` into the shared `.persistent-bar` CSS rule itself (removing the now-redundant inline style from Vehicle-Specific) so this can't recur per-template. Verified with Playwright on all 5 templates, fresh page loads (no cached CSS): each bar now reports `top:0` once `.visible`, at the correct scroll threshold for that template's own decision-panel height.
2. ✅ **Done 2026-09-12.** **Variant/colour-switching wording is misleading — describe it as a page navigation, not a live in-page swap.** Every variant/colour is its own separate product listing/URL (spec.md §1.1) — the prototype's JS simulates an in-page live swap for demo convenience only. `DEVELOPER-BRIEF.md`'s "live-swap" language (§6.2 Config-Variant purpose, §6.3, §6.4, §7.5 Variant Picker, §7.6 Colour/Swatch Grid) needs rewording to say the real build navigates to the sibling SKU's own page. Wording-only fix, no prototype/interaction change.
3. ✅ **Investigated 2026-09-12 — confirmed capture-process issue, not a code bug.** **Showroom Finder map renders as blank/black space on mobile whole-page screenshots**, across all 5 templates' `page-layout-*-mobile.png`. Confirmed live with Playwright at 390px on Simple: an immediate full-page screenshot right after load reproduces the bug exactly (solid black where the map should be); scrolling the map into view and waiting ~2.5s for tiles to load, then re-capturing, shows the map rendering correctly with pins and tiles. So this is a capture-timing issue (the mass-capture process needs to scroll the map into view and wait for tile-load before shooting mobile full-page screenshots), not a genuine render bug — no code change needed. Noted as a capture-process requirement for the eventual final mass-capture pass (held per the standing screenshot policy).
4. ✅ **Done 2026-09-12.** **§6.4 terminology fix:** "for kits sold against a specific vehicle fitment" → "for Vehicle Rack Sets sold against a specific vehicle fitment." Check whether "kit(s)" is used elsewhere in the brief/spec for this product type and should be swapped consistently, or if this is a one-off line.
5. ✅ **Done 2026-09-12.** **Fitted Photos Gallery — finalized placement + colour, removed the Demo State Panel toggles for both.** Brenton's decision: full-width above the Trust Row, non-red background (the existing default) — permanent. Removed `applyFitGalleryPlacement()`/`applyFitGalleryRedFlag()`, the `#fitGalleryNestedSlot` slot, the `.fit-gallery-section.nested`/`.fit-gallery-panel.red` CSS, and both Demo State Panel controls (radio group + checkbox) from `shared.js`/`shared.css`/`vehicle-specific/index.html`. `PAGE-GLOSSARY.md` updated to match; `DEVELOPER-BRIEF.md` §7.16/§6.4 still describe the old toggle-pending state and are owed a fix whenever Brenton's next dev-brief sign-off round happens (not touched this session, per the standing gate). Verified with Playwright: gallery renders full-width/non-red with no visual regression from the previous default, Demo State Panel no longer shows either control, zero console errors.
6. ✅ **Done 2026-09-12.** Full heading-hierarchy (h1–h6) audit/fix across all 5 templates, tags/semantics only — no visual change. Confirmed with Brenton before building: region-agnostic; Demo State Panel headings (`.admin-section h5` — "Session Vehicle," "Product State," etc.) excluded, reviewer-only tooling stripped before production, same treatment as the removed Design Rationale section; the two overlay drawer headings (Ex-Demo/B-Stock slide-in, Store Slide-out) promoted to `h2` since each is the primary heading of its own panel when open; Trust Row's 4 item labels stay `h4` with no new wrapping heading (a deliberate, informed trade-off — see below). New map: **h1** = product title (already correct, unchanged) · **h2** = top-level sections/panels (`.related-heading` — both Related Products and the Discontinued-alternates heading — `.showroom-heading`, `.tabs-heading`, `.faq-heading`, `.fit-gallery-title h2` [already correct, unchanged], plus the two drawer headings, promoted from h3) · **h3** = sub-content (Showroom Finder's real inner headline and Get It Installed, both already h3 and unchanged; Variant Picker "Build Option" and Colour Swatch "Colour," promoted from h4; Package Contents "What's Included," every `.details-sub` tab-panel sub-heading [Details/Gold Guarantee/Shipping Info content], and Related Products/Discontinued-alternates card titles, all promoted from h5) · **h4** = Trust Row item labels, unchanged.

    **Real complication found mid-build, not anticipated in the original plan:** each template has its own per-page inline `<style>` block in `<head>` (separate from `shared.css`) carrying page-specific rules for some of these exact selectors — `.variant-label-row h4`/`.swatch-label-row h4` (Vehicle-Specific, Config-Variant, Sibling-Color) and `.details-sub h5` (Simple, Grouped-Bundle only — the other 3 templates have no such inline rule for this selector and were relying purely on browser-default h5 styling, which is why a new `.details-sub h3{font-family:'Lato',Arial,Helvetica,sans-serif;font-weight:700;font-size:.83em;text-transform:none;letter-spacing:normal;margin:1.67em 0;color:inherit;}` rule was added to `shared.css` to reproduce that default look exactly for those 3). Retagging the markup without also renaming these per-page inline selectors left them orphaned (matching nothing), causing 3 real visual regressions caught by Playwright computed-style diffing before/after — "Build Option"/"Colour" briefly rendered black/18.72px instead of grey/13px, and Simple/Grouped-Bundle's tab sub-headings briefly fell through to the new generic `.details-sub h3` rule instead of their own distinct Barlow-Condensed-uppercase inline look. Fixed by renaming the tag in each affected inline `<style>` block to match. Also caught: `.product-card h5` never had an explicit `color` (it inherited the body's `#232323`) — promoting it to h3 swept it into the shared `h1,h2,h3,h4{color:var(--rrg-black)}` base rule, silently turning card titles black; fixed by adding `color:#232323` explicitly to `.product-card h3` in `shared.css`. All three fixes verified via `getComputedStyle` before/after diffing (font-family/size/weight/transform/letter-spacing/margin/color all pixel-identical to the pre-change baseline).

    **Two residual single-level skips, both deliberate, not oversights:** (1) the page's h1 title is followed by Get It Installed/Build Option's h3 with no h2 in between — there's no natural top-level section there to insert, and forcing these small in-panel widget labels up to h2 would blur them with real page sections like Related Products/Tabs/FAQ; (2) Fitment Gallery's h2 is followed by Trust Row's h4 with no h3 in between — direct consequence of Brenton's own confirmed choice to leave Trust Row's item labels at h4 with no new wrapping heading, not something a tag-only fix could avoid without either demoting Fitment Gallery or promoting Trust Row (both rejected). Both are minor, common single-level skips, a large improvement over the original h1→h4→h5→h2→h3→h5 chaos, and were accepted as reasonable trade-offs rather than "fixed" by adding invisible headings or reshuffling section weight.

    Verified with Playwright on all 5 templates: exactly one real `h1` each, zero heading-level skips (walked the full `h1`–`h6` sequence programmatically), every retagged element's computed style (font-family/size/weight/transform/letter-spacing/margin/color) pixel-identical to its pre-change baseline, both drawer headings confirmed at `h2` with unchanged styling when opened, mobile (390px) spot-checked on Vehicle-Specific and Grouped-Bundle (the two templates with the most heading-CSS complexity), zero console errors throughout.

    **Not done this session, per the standing `DEVELOPER-BRIEF.md` sign-off gate:** §6/§7's tag-per-widget documentation (which the original ask also ­requested) and §7.2's misleading "heading uses the page's h1-adjacent styling" wording (which is about a `<strong>` tag, not a real heading, so out of scope for this tag-only fix anyway) remain stale/unwritten — owed whenever Brenton's next separate dev-brief sign-off round happens. Not pushed to git.
7. ✅ **Done 2026-09-12.** Fitment Status widget wording standardized to the full slug down to roof type across all three states. `FITMENT_COPY.unknown.detail` (`shared.js`) changed from `(${PRODUCT_FITMENT.vehicle_years})` to `(${PRODUCT_FITMENT.body_style}, ${PRODUCT_FITMENT.roof_type})`, matching the "fits" state's existing format exactly (both describe the same static `PRODUCT_FITMENT` data, not a customer's own vehicle). `no_fit`'s detail — a function of the *customer's* saved vehicle, already including body style/roof type/year — was left unchanged. Verified with Playwright on Vehicle-Specific: cycled all 3 states (`fits`/`unknown`/`no_fit`) and confirmed "unknown" now reads "This product suits Toyota Hilux N80 (4dr Ute, Bare Roof)," zero console errors.
8. ✅ **Decided 2026-09-12 — documented, not built.** Fitment widget's action buttons ("Select your vehicle," "Change vehicle," "Find the right fit") stay decorative/dead in this prototype — confirmed with Brenton via AskUserQuestion, no click handlers built. Real destinations for the developer handover: **"Select your vehicle"/"Change vehicle"** open the real Magento vehicle-selector/garage flow (out of scope here). **"Find the right fit"** navigates to the equivalent Vehicle Rack Set product for the customer's actual saved vehicle where one exists, falling back to that vehicle's Rack Set category page if no exact product match exists — Brenton can walk Mark through the fallback logic in more detail live if needed. Owed a write-up in `DEVELOPER-BRIEF.md` §7.2's Links line once Brenton signs off on dev-brief edits (standing gate, not touched this session).
9. ✅ **Done 2026-09-12, then corrected further same day per Brenton's follow-up.** **Real bug: Add to Cart is hard-disabled on "Doesn't fit."** `shared.js:116` — `applyFitmentState()`: `btn.disabled = state === 'no_fit';`. Contradicts the required behaviour: purchase should never hard-stop regardless of fitment state. First pass removed the disable but kept the existing `.btn-outline` relabel/restyle (matching this item's original wording) — Brenton reviewed and asked for the button to be genuinely identical in every fitment state, not just clickable: same "Add To Cart" text, same primary style, always enabled, in Fits/Doesn't Fit/Confirm Your Vehicle alike. `applyFitmentState()` now sets a fixed `'Add To Cart'`/`.btn-primary` on `[data-cta-label]` regardless of state; the now-unused `cta` field was removed from `FITMENT_COPY`'s three state objects (the fitment card's own verdict text/actions, rendered separately by `renderFitmentHTML()`, are unaffected). Added an explicit line to §7.2 in `DEVELOPER-BRIEF.md` stating Add to Cart must look and behave identically across all three fitment states. Verified with Playwright on Vehicle-Specific: all 3 CTA instances (Decision Panel, Sticky Mobile Bar, Persistent Bar) read "Add To Cart"/primary/enabled through a full none→match→mismatch round trip; fitment card's own verdict text unaffected; zero console errors (aside from the pre-existing unrelated favicon 404).
10. ✅ **Done 2026-09-12.** **§7.3 Rack Fit Guarantee badge — location wording is stale.** Says "top-right corner of the Fitment Status card"; should say right-aligned, vertically centred against the card (already fixed in the actual CSS on 2026-09-10 — `top:50%;transform:translateY(-50%)` — the doc just never caught up). Doc-only fix.
11. ✅ **Done 2026-09-12.** **Rack Fit Guarantee badge needs a real click action** — jump to and open the Gold Guarantee tab (all 3 instances: main card, Sticky Mobile Bar, Persistent Bar), same tab-jump pattern as `initTabJumpLinks()`/the star-rating badge's `data-jump-tab`. Currently no click handler at all. Update §7.3's Links line once built; `title` tooltip stays as-is.
12. ✅ **Done 2026-09-12.** Rack Fit Guarantee badge — `fitgaurantee.svg` (confirmed, by reading the file, to be a baked-in raster image wrapped in an `<svg>` element, not a real vector — it couldn't be recoloured via CSS at all) replaced across all 3 instances (`prototypes/vehicle-specific/index.html` lines 192/760/774) with a hand-drawn placeholder vector icon (shield outline + a checkmark cut out of the fill via `fill-rule="evenodd"`, same technique as a classic "verified" badge — single flat colour, the checkmark reads as clean negative space against whatever's behind it). Confirmed with Brenton: **not** region-aware after all — ties directly to the existing `--rrg-fits` green token (#1E7A34, no new token) since the badge only ever shows in the "Fits your vehicle" state, which doesn't vary by region. New rule `.rack-fit-badge,.rack-fit-badge-icon{color:var(--rrg-fits);}` in `shared.css` (added directly after the existing sizing rules); `data-jump-tab="t3"` kept on the `<svg>` root so the existing generic `initTabJumpLinks()` needed zero changes. Verified with Playwright: colour resolves to `rgb(30,122,52)` on all 3 instances (Decision Panel 32px, Sticky Mobile Bar 20px, Persistent Bar 20px), badge correctly shows only in the "fits" state and hides in "confirm your vehicle"/"doesn't fit" (unchanged behaviour), click still opens the Gold Guarantee tab, zero console errors on desktop (1280px) and mobile (390px). `PAGE-GLOSSARY.md` updated to match. This is an explicit **hand-drawn placeholder**, swappable later for real client-approved artwork. Owed a `DEVELOPER-BRIEF.md` §7.3 "Region differences" line fix (colour is tied to fitment state, not region) once Brenton signs off on dev-brief edits (standing gate, not touched this session).
13. ✅ **Done 2026-09-12.** **§7.4 Product Notes — add the Rackit data-source note.** State that "Product Notes" (on-page name) maps to Rackit's "Important Vehicle Fit Notes" field — the real backend source, and why it used to carry that name before the 2026-09-11 rename.
14. ✅ **Done 2026-09-12.** **Global cleanup: strip backlog-item/meeting/internal-process references out of `DEVELOPER-BRIEF.md`.** 31 occurrences found (e.g. "(backlog item 11)", "(Graham Sowerby meeting)", "renamed 2026-09-11", named confirmations like "per Brenton's confirmation"). This history belongs in `spec.md`, not the developer-facing doc. Full pass needed across the entire document, rewriting each to state only the current, correct behaviour. Confirmed specific instances so far: §7.4 (renamed-backlog-item mention), §7.6 (mobile-readiness-audit mention), §7.20 (names Brenton directly, and is also factually backwards — see item 22).
15. ⬜ **Every widget/section needs a Data Source line: Magento (template/layout) vs. Rackit (which exact field).** None of §6 or §7's entries currently say where their content comes from. Add a "Data Source" line to every Component Library entry (§7.1–7.29) and relevant Page Layout mentions (§6), stating "Magento — template-level" or "Rackit — field: `[name]`." Confirmed concrete examples needing this: product title/price/SKU/breadcrumbs/description/specs/stock/images/video/fitting-instructions/fitment data/package-contents/related products (likely Rackit), Gold Guarantee tab content (Magento CMS content block, same as the current live site), FAQ (Rackit, per item 20 below), Tabs accordion mechanism/Sticky bar/Region Selector (Magento/template, no Rackit field). **Needs input from whoever owns/knows the Rackit schema before it can be completed** — not to be guessed from the scraped JSON shape alone.
16. ✅ **Done 2026-09-12.** **Real bug: UK's Delivery/Click & Collect widget should default to the Delivery tab, not Click & Collect.** `shared.js:1146` — `REGION_DEFAULT_DC_TAB = { AU: 'collect', NZ: 'delivery', UK: 'collect' }` — UK's value is wrong, should be `'delivery'` (same as NZ, since both are single-store regions). Fix code, then §4.2 cascade table row 2 and §7.7's region-differences line in both `DEVELOPER-BRIEF.md` and `spec.md`.
17. ✅ **Done 2026-09-12.** **§7.7 missing screenshots of the widget's actual populated states.** Replaced the single generic "postcode entered (AU)" shot with 3 distinct captures on Simple: `dc-widget-results-delivery.png` (Delivery tab, Standard/Express Freight rates shown), `dc-widget-results-collect.png` (Click & Collect tab, store results + "View all stores"), plus a bonus `dc-widget-single-store.png` showing the NZ/UK fixed single-store line (no postcode search UI) — added while scoping this item via `/backlog`, since the widget's region difference had text but no screenshot. Verified live with Playwright (AU postcode 2170, both tabs; UK region switch showing "On display at the Bolton Store"), Demo State Panel confirmed out of frame in all 3. `DEVELOPER-BRIEF.md` §7.7 updated to reference all 3; old `dc-widget-results.png` deleted (no longer referenced anywhere).
18. ✅ **Done 2026-09-12, together with item 19.** No invalid-postcode handling existed at all. Resolved as part of the v2 widget rebuild below — an invalid postcode (anything not exactly 4 digits) now shows a red-bordered input plus an inline error banner ("That doesn't look like a valid Australian postcode. Please check and try again."), matching Brenton's Figma design exactly.
19. ✅ **Done 2026-09-12 — built as a Demo State Panel preview, not yet the shipped default.** Brenton supplied Figma exports (`Delivery and Click and Collect Widget Design/`, project root) redesigning the widget's interaction model: an explicit postcode **Check** button (not live-as-you-type) → a "Checking…" loading state → one of three outcomes — a radio-selectable options list (Standard/Express Shipping + Click & Collect on the Delivery tab, nearest real stores with Closest/In-Stock/Need-to-Order-In badges on the Click & Collect tab), a "No Stores Nearby" state, or a "Remote" quote-request state (email + Request Quote). Selecting Click & Collect from within Delivery's own options switches the widget to the real Click & Collect tab.
    - **New toggle:** Demo State Panel → Widget Previews → "New Delivery/Click & Collect design (preview, AU only)" (`dcWidgetV2` flag, `applyDcWidgetV2Flag()`/`dcV2*()` functions in shared.js). Off by default — the current live widget stays the shipped default until Brenton signs off on the preview.
    - **AU-only by design, confirmed with Brenton:** NZ/UK keep today's single-store widget completely unchanged regardless of the toggle — `dcV2SyncForRegion()` (called from `applyRegion()`) reverts any widget to its real original markup the moment the region isn't AU, and re-applies the preview from that same cached original on returning to AU.
    - **"Remote"/"No Stores Nearby" = Northern Territory postcodes (0800–0999).** Not fabricated — RRG genuinely has zero NT stores in `RRG_STORE_NETWORK` today, so this reuses a real gap in the existing store network rather than inventing a fake unserviceable area. Every other valid postcode resolves to a real state with real stores.
    - **Nearest-store ranking and distances are a numeric-proximity heuristic** (`dcNearestStores()`: ranks a state's stores by `|store postcode − typed postcode|`, top 3 shown), not real geocoding — this prototype has never had geocoding (same caveat as `postcodeToState()`/the Showroom map). Flagged as demo precision.
    - **New `ORDER_IN_STORES` demo set** (alongside the existing `ON_DISPLAY_STORES`) drives which stores show "Need to Order In" vs "In Stock" (`rrgStorePillsHTML()`, also reused by the Store Slide-out) — fabricated, needs a real per-store inventory feed before production.
    - **Shipping prices are flat demo values taken directly from the Figma** ($19 Standard / $45 Express), not postcode-dependent — same flat-rate convention the original widget's prices already used.
    - Zero changes to any of the 5 templates' HTML — fully JS/CSS-only (`dcV2Render()` caches each `.dc-widget`'s real `innerHTML` on first toggle and restores it exactly when the preview is off), so this is completely reversible with no risk to the shipped default.
    - Verified with Playwright on Simple (valid AU postcode → options on both tabs, instant tab-switch with no second loading flash, Click & Collect radio switches tabs; NT postcode → Remote on Delivery + No Stores Nearby on Collect, Switch To Delivery jumps tabs correctly; invalid formats → immediate error, no loading flash; second `.dc-widget` in the Shipping Info tab mirrors every state instantly; toggle off reverts both widgets to exactly their original markup/behaviour with no duplicate event listeners; full AU→NZ→UK→AU region round trip with the preview left on throughout confirmed NZ/UK totally unaffected) and spot-checked on Vehicle-Specific (no regressions against its fitment/persistent-bar logic, mobile 390px clean) — zero console errors throughout (aside from the pre-existing unrelated favicon 404). Not pushed — holding for Brenton's review/sign-off before this becomes the shipped default, per the usual workflow. `DEVELOPER-BRIEF.md` not touched, per the standing gate.
20. ✅ **Done 2026-09-12.** **§7.8 Store Slide-out screenshots.** Confirmed live with Playwright before recapturing, per the item's own instruction: `renderStoreSlideoutBody()`'s grouping logic is correct — entering NSW postcode 2170 genuinely promotes a "Within 100km" group to the top (open by default, Moorebank's On Display pill visible), so this was a stale/bad capture, not a code bug. Re-captured `store-slideout-open.png` (NSW promoted) and added a new `store-slideout-closed.png` for the no-postcode state — which actually renders every state group **open** (not closed, as this item's own text assumed), matching `renderStoreSlideoutBody(null)`'s real behaviour. `DEVELOPER-BRIEF.md` §7.8 updated to reference both, Demo State Panel confirmed out of frame in both.
21. ✅ **Done 2026-09-12.** **§7.9 doc fix: On Display isn't inherently AU-only** — it's the component it currently lives in (AU-only store-row list) that's AU-only, not the on-display concept itself. Reword.
22. ✅ **Done 2026-09-12.** **Real bug: NZ/UK's Click & Collect summary line always claims "On display," with no check against any actual on-display flag.** `shared.js:220` — the NZ/UK branch hardcodes `` `On display at the ${...} Store` `` unconditionally, unlike AU's (already imperfect) postcode-gated branch. Needs to actually check the real/placeholder on-display flag for that single store before asserting it's on display.
23. ✅ **Done 2026-09-12.** Whole pricing section redesigned off Brenton's Figma reference (`Price Layout Design.png`, project root) — a stacked "Now $X" (red, Barlow Condensed, bumped 34px→42px) / "Was $Y  [Save Z%]" (dark grey `#434343`, badge corner-radius softened from a full pill to a `8px` rectangle, badge text-transform changed from uppercase to the reference's mixed case) layout, on all 5 templates' Decision Panel. Pixel-sampled the reference directly (Windows `System.Drawing`) rather than eyeballing: the red is `#BB0200`, already matching this project's `--rrg-red` token almost exactly (no new token needed); the grey is `#434343`, notably darker than the previous `#8a8a8a`. Scoped via AskUserQuestion before building: region-agnostic (colours are token-driven, so the existing UK navy-skin/currency plumbing applies automatically with no new work); Sale Tag unchanged (stays, just wasn't in the reference crop — its position is already relative to `.price-block`'s real height, so it re-centers against the taller block with no code change); off-sale state drops the "Now"/"Was" labels entirely and falls back to a plain price, same as before this redesign; the condensed Sticky Mobile Bar/Persistent Bar price is adapted (same red/dark-grey colours on the existing single-line price) rather than getting the full labelled layout, since there's no room for labels/badge there. One judgement call flagged at plan review and built as recommended: the plain (non-sale) price stays neutral black rather than always-red, since the reference is explicitly an on-sale example and standard practice reserves red for the sale contrast itself, not every regular price — `.price-block.on-sale .price-now` is the only red-price rule.

    `.price-now`/`.price-was`/`.badge-save` kept their exact class names so every existing JS hook (`reapplySaleFlag()`/`setSaleFlag()`/`detectInitialSaleState()`/`syncSaleTag()`, plus `vehicle-specific`/`config-variant`/`sibling-color`'s own `renderPrice()`-equivalents) kept working unchanged against the restructured markup. New `syncPriceLabels()` in shared.js drives the "Now"/"Was" labels and the whole "Was" line generically off `.price-was`'s own hidden state — the same signal already used for the old single-row layout — so a per-variant/colour discount resolves correctly too, not just the demo sale toggle (verified on sibling-color's Titanium Grey, which has no real discount: correctly shows a plain black price with no labels even while the page's overall "sale" flag is on). `.price-block.discontinued .price-now`'s strikethrough grey was updated to the same `#434343` for visual consistency with its own code comment ("same visual language as `.price-was`"). Condensed-price recolouring needed two different fixes: a shared CSS rule covers every instance still using the real `.price-now`/`.price-was` classes (Persistent Bar on all 5 templates, `config-variant`/`sibling-color`'s persistent price spans) for free, while the 4 spots using inline-styled markup for the Sticky Mobile Bar's price (`config-variant`/`sibling-color`'s JS-built `#mobilePrice`, `simple`/`grouped-bundle`'s hardcoded static markup) needed direct colour edits since inline styles don't inherit a class-level change.

    **Real bug found and fixed while building this:** `setSaleFlag(true)`'s manual-restore branch (the path static templates without a `renderAll()` take when the Demo State Panel's "on sale" toggle is switched back on) never called `reapplySaleFlag()` — so the Sale Tag graphic (and, now, the new price labels) stayed hidden after being toggled off, even once the price itself was restored. Fixed by calling `reapplySaleFlag()` unconditionally at the end of that branch too (safe — its own price-swap logic is a no-op once `adminState.sale` is true again, so this only runs the sync-the-rest-of-the-UI half).

    Verified with Playwright on all 5 templates: on-sale layout matches the reference's proportions (desktop 1280px); toggling "Product is on sale" off/on in the Demo State Panel round-trips correctly on config-variant (confirmed via computed style, not just visually — `on-sale` class, label/line `hidden` state, Sale Tag `hidden`, and `.price-now`'s resolved colour all flip correctly both directions, including after switching variants mid-toggle); sibling-color's Titanium Grey confirmed as a plain black price with no labels; Discontinued state's struck price confirmed still reading correctly with the updated grey; mobile (390px) spot-checked on vehicle-specific and grouped-bundle — no horizontal overflow, Sticky Mobile Bar price shows the red/dark-grey treatment cleanly. Zero console errors throughout (aside from the pre-existing unrelated favicon 404). `PAGE-GLOSSARY.md` updated (new "Price Block" entry, Sale Tag entry cross-referenced). Not pushed — holding for Brenton's sign-off per the usual workflow. **`DEVELOPER-BRIEF.md` not touched** — owed whenever Brenton's next separate dev-brief sign-off round happens, same standing-gate treatment as items 5/8/12 before it.

    **Follow-up same day, 5 small direct fixes from Brenton's review (no new plan needed — explicit, concrete asks):** (1) "Was" renamed to "RRP" in the label span, all 5 templates. (2) Confirmed the whole "RRP $X" already read as one struck-through phrase (both the label and the price span carried `text-decoration:line-through` from the original build) — no change needed here beyond the rename. (3) The RRP price's font-size bumped 16px→19px so its rendered line-height (measured via Playwright: 27.55px) matches `.badge-save`'s real rendered height (27.4px) almost exactly, rather than sitting visibly smaller. (4) Real root cause found for "the RRP label looks like a different colour than the price even though the hex matched": the label used the shared `.price-label` treatment (Barlow Condensed, weight 700) while `.price-was` was plain Lato/400 — same `#434343` hex, but bold ink coverage at a different font reads visually heavier/darker at the identical colour. Fixed by making `.price-line-was .price-label` match `.price-was`'s actual computed font exactly (family/weight/size/colour), confirmed identical via `getComputedStyle` on both spans, rather than only matching colour as the first pass did. (5) "Now" label now shares `.price-now`'s red via `.price-block.on-sale .price-line-now .price-label` (previously stayed black even when on sale — an oversight in the original build, only the price itself had the on-sale red rule). Re-verified with Playwright on Simple and Config-Variant (including a live variant switch): "Now"/price colours match exactly, "RRP"/price font family/weight/size/colour are pixel-identical, RRP height ≈ badge height, zero console errors, no mobile (390px) overflow. Not pushed.

    **Second follow-up same day, 2 more direct fixes:** (1) **Gap between the Now and RRP lines reduced** — the explicit margin between the two lines was already only 2px, but `.price-now`'s 42px font-size at the inherited body line-height (1.45) produced a 60.9px-tall line box around text that's really only ~29px tall, so most of the "large gap" complaint was actually invisible line-height padding below the Now line, not the margin. Fixed by setting `line-height:1` on `.price-now` and the Now line's own `.price-label` only — the RRP line's line-height was deliberately left at the inherited 1.45 since `.price-was`'s font-size was tuned against exactly that line-height to match `.badge-save`'s height in the previous follow-up, and touching it would have undone that. Row height dropped from 60.9px to 42px, closing the visual gap to just the 2px margin. (2) **RRP price and the Save badge centre-aligned** — `.price-line-was` changed from the shared `.price-line` default of `align-items:baseline` to `align-items:center`; confirmed via `getBoundingClientRect()` that the RRP price's, badge's, and label's vertical centres now land within 0.01px of each other (previously baseline-aligned, which put the badge's own centred padding visibly out of true vertical alignment with the RRP text). Re-verified with Playwright on Simple and Config-Variant: gap tight and consistent, three elements centre-aligned, zero console errors, no mobile (390px) overflow. Not pushed.

    **Third follow-up same day — mobile-only: pricing widget centred, Sale Tag repositioned.** Confirmed the two previous follow-ups' fixes are live on all 5 templates first (shared.css, no per-page divergence). Brenton's ask: on mobile, centre the pricing widget (both price lines + stock-status line) as a unit instead of staying left-aligned, and fix the Sale Tag's mobile position, which read as disconnected — floating at the block's far-right edge, vertically centred against the whole (now off-centre) block, with a large dead gap to the actual price text. `@media (max-width:900px)` (this project's real mobile-vs-desktop breakpoint, not the narrower 640px used elsewhere for small-phone-only tweaks): `.price-block` becomes a flex column with `align-items:center` — each price line is already a flex row that shrink-wraps to its own content width, so this centres each one as a whole block with no per-line change needed. Sale Tag moved from "vertically centred against the whole block" (correct on desktop, where price content runs the block's full left-aligned width) to a fixed top-right corner (`top:4px;right:4px`, `width:64px`, no more `translateY`) — a corner ribbon in a fixed corner is both the simpler fix and the more conventional treatment once the price content no longer runs the block's full width.

    **Real overlap caught and fixed while verifying, not assumed safe:** checked the longest price on the site (vehicle-specific's `$1,893.09`) specifically, since a wider centred price sits closer to the fixed top-right tag than a short one does — confirmed a genuine bounding-box and visual overlap between the tag and the "Now" line on first pass. Fixed with `.price-block.on-sale .price-line-now{margin-top:44px}` inside the same media query — scoped to `.on-sale` (kept in sync with the tag's own visibility by the existing `syncPriceLabels()`) so the extra top space isn't wasted when there's no tag to clear, and targeting the line's own margin rather than `.price-block`'s padding to avoid a cascade fight with each template's inline `<style>` `.price-block{padding:16px 0}` override, which loads later in the document than shared.css and would otherwise win at equal specificity.

    Verified with Playwright at 390px on all 5 templates: pricing widget centres correctly on Simple/Config-Variant/Sibling-Color/Grouped-Bundle/Vehicle-Specific; vehicle-specific's long price no longer overlaps the tag; off-sale state (toggled via Demo State Panel on Simple) correctly shows no tag and no wasted top margin, restoring cleanly back to on-sale; desktop (1280px) confirmed completely unaffected (`.price-block` stays `display:block`, tag keeps its original `top:50%`/`translateY` centred treatment) — computed styles checked directly, not just visually. Zero console errors, no horizontal overflow at 390px on any template. Not pushed.

    **Fourth follow-up same day — mobile: Sale Tag moved off the price block entirely, onto the main product image.** Brenton reviewed the top-right-corner-of-the-price-block fix above and said it still looked wrong. Rather than a third position on `.price-block`, moved the mobile tag onto the gallery instead — added a second `.sale-tag.gallery-sale-tag` image inside `.gallery-main` (the shared main-image container, already `position:relative` + `overflow:hidden` on all 5 templates) on all 5 templates, positioned `top:8px;right:8px` and shown only at `max-width:900px`; the original `.price-block .sale-tag` instance is now `display:none` on mobile entirely rather than repositioned, and stays exactly as it was (vertically centred against the block) on desktop. `.gallery-main`'s existing `overflow:hidden` conveniently clips the new tag flush to the image's rounded corner for free, and its existing `img{width:100%;height:100%;object-fit:cover}` rule needed an explicit two-class override (`.gallery-main .gallery-sale-tag`) so the tag isn't stretched to fill the whole image box the way the real product photo is.

    `syncSaleTag()` in shared.js (previously scoped to find one `.sale-tag` within the price block's own `.decision-panel`) was broadened to toggle every `.sale-tag` on the page together — a page only ever has one product/one sale state, so this is simpler and correct regardless of how many tag instances currently exist, and means the gallery instance automatically inherits every existing behaviour (Titanium Grey's no-discount state, the Demo State Panel's on/off toggle, variant switches) with no separate wiring. Verified with Playwright on all 5 templates: gallery tag renders correctly over both static and JS-rendered gallery images (config-variant/sibling-color), legible against both a plain white product photo and a real busy lifestyle/product photo (vehicle-specific); sibling-color's Titanium Grey correctly hides it, Black correctly restores it; the Demo State Panel's "Product is on sale" toggle hides/shows both tag instances together; desktop (1280px) confirmed completely unaffected via computed style (`.price-block .sale-tag` still `display:block`, `.gallery-sale-tag` still `display:none`); no horizontal overflow at 390px on any template; zero console errors. Not pushed.
24. ✅ **Done 2026-09-12.** **§7.14 Get It Installed CTA — 4 screenshots**, replacing the old 2. Captured on Vehicle-Specific (the only template with the Fitment Gallery toggle), all 4 combinations of "Has video" × "Fitment Gallery exists": `install-cta-paired-gallery.png`, `install-cta-paired-no-gallery.png`, `install-cta-fullwidth-gallery.png`, `install-cta-fullwidth-no-gallery.png`. **Real latent bug found while capturing, not fixed here (out of scope for this item):** the page's static HTML default (before any Demo State Panel toggle is touched) hardcodes the CTA text as "See 283 real fitments," which doesn't match `applyFitGalleryFlag()`'s own current 5+ copy ("We've fitted this 283 times — view the gallery") — nothing calls that function on page load to sync the two, so the static markup is stale relative to the JS logic. Screenshots use the correct, function-driven copy (toggling gallery off/on once syncs it); flagging the stale static default as a separate follow-up, not folded into this item. `DEVELOPER-BRIEF.md` §7.14 updated to reference all 4; old `install-cta-paired.png`/`install-cta-no-video.png` deleted.
25. ✅ **Done 2026-09-12.** **§7.15 Product Video — description is stale.** Says "a placeholder box shows for SKUs with no real scraped video" — checked the CSS (`shared.css:585`, `.install-media-row.no-video .video-placeholder{display:none!important;}`), the video slot is actually fully hidden, not shown as a placeholder, and Get It Installed expands to full width instead. Doc-only fix — code is already correct.
26. ✅ **Done 2026-09-12, after item 5; reworked same day per Brenton's live-site screenshot.** **§7.16 Fitted Photos Gallery — "View All In-store Fitments" link now works, as a two-view slide-out.** First pass opened a flat photo grid only; Brenton then shared a screenshot of the live site's own "Browse Fitment" popup and asked for that fuller experience — clicking a grid photo opens a per-fitment detail view (`renderFitGallerySlideoutDetail()`), with a "‹ Back to Grid View" link replacing the grid's heading. Detail view: a light "Browse Fitment N of 283 / Fit #NNNNNN" bar (the Fit # is each photo's own real Rackit `product_id` from the scrape, not fabricated), Prev/Next (disabled at the ends), a large main photo + 4 thumbnails, the page's own real product title (read from `<h1>`) and vehicle line (`VEHICLE_PRODUCT.shared.specifications_shared['Vehicle']`), a "Rack Components" list, a disclaimer note, and a "Back to Product" button. Confirmed with Brenton: every fitment shows the *same* real vehicle/components (no fabricated variety across vehicles, unlike the live reference which spans multiple products) — the components list is read live from the page's own What's Included rows (`rackComponentsFromPage()`) so the two can't drift apart; the detail view's 3 "other angle" thumbnails reuse other real photos from the same 16-photo set rather than inventing new ones (flagged in Section 7); and "Back to Product" (replacing the live site's "View Rack & Buy", which doesn't apply on a page that already is that product) closes the drawer and scrolls to the Decision Panel with a brief highlight instead of navigating anywhere. Grid view unchanged from the first pass otherwise (`buildFitGallerySlideout()`/`openFitGallerySlideout()`/`closeFitGallerySlideout()`/`renderFitGallerySlideoutGrid()` in `shared.js`, `.fit-gallery-slideout*`/`.fgs-*` CSS in `shared.css`; `FIT_GALLERY_PHOTOS` in `vehicle-specific/index.html` now `{thumb, id}` objects instead of plain URLs, still all 16 real scraped photos). Verified with Playwright: grid → detail → Prev/Next (Fit # and browse count update correctly, Prev disabled at fitment 1, Next disabled at the last tile) → Back to Grid View → reopen a different photo → Back to Product (drawer closes, Decision Panel receives the highlight class) — zero console errors throughout. Screenshot deferred to the standing final mass-capture pass, same as the rest of this backlog.
27. ✅ **Done 2026-09-12.** **§7.17 Gold Guarantee tab — add a content-source note: Magento CMS content, not Rackit** (same as the current live site). Concrete example feeding into item 15.
28. ✅ **Done 2026-09-12.** **§7.17 Tabs widget — Reviews tab missing from the States list** even though it's named in the location line. Add a line cross-referencing Section 2.
29. ✅ **Done 2026-09-12.** **§7.18 FAQ — add a Rackit data-source note** (per-product FAQ content, not hardcoded placeholder copy).
30. ✅ **Done 2026-09-12.** **Real gap: no `FAQPage` schema exists anywhere** — checked the JSON-LD on Simple, only `Product`/`Offer` schema exists (matches the known gap in §5). Once real FAQ content is sourced from Rackit (item 29), it should also drive `FAQPage` structured data for AEO.
31. ✅ **Done 2026-09-12, scoped via `/backlog`.** Full-page SEO + AEO optimization pass — heading hierarchy (item 6) and baseline schema (item 30) were already done, so this pass covered what was still missing: no meta description, no canonical URL, no `BreadcrumbList` schema, prototype-style `<title>` tags, a few empty `alt=""` attributes, `Product` schema's `priceCurrency` hardcoded to AUD (region-specific, doesn't follow the Region Selector), and `aggregateRating`/`Review` schema (not data-blocked — the real REVIEWS.io data already exists via §2.2's endpoint, just not wired in). Written up as `DEVELOPER-BRIEF.md` **Section 8 — SEO & Structured Data** for the Magento build, not built into the prototype — Brenton's explicit call for this item.
32. ✅ **Done 2026-09-12 — documented, not built.** **§7.19 Related Products — specify card click-through behaviour.** Clicking anywhere on a card except the Add to Cart button should navigate to that product's own PDP; Add to Cart stays a real add-to-cart action and must not also trigger navigation. Also applies to §7.22 Discontinued's "alternatives" section, which reuses this card component.
33. ✅ **Done 2026-09-12.** **§7.20 Package Contents — Links section is wrong and wrongly attributed.** Currently says the placeholder links don't need to resolve "per Brenton's confirmation" — this both names Brenton unnecessarily (see item 14) and directly contradicts `spec.md` §7's own note that real Magento links should go to each component's own PDP. Rewrite to state the correct instruction plainly, no names.
34. ✅ **Done 2026-09-12.** **Screenshot bug: Grouped-Bundle's Package Contents screenshot (`package-contents-bundle.png`) shows the Demo State Panel visible in frame** — violates the standing "panel never in frame" rule, and slipped through the 2026-09-12 "final" pass. Feeds into the general re-audit noted in the section intro above.
35. ✅ **Done 2026-09-12.** **§7.22 Discontinued state — remove the duplicate stock-status-line text**, keep only the banner card (both currently say the product is discontinued; redundant). Only for the discontinued state — other stock states keep their status line.
36. ✅ **Done 2026-09-12.** **Real bug: Discontinued banner has red text on a red-tinted box, should be black text on the box.** `shared.css:719` — `.discontinued-banner` sets `color:var(--rrg-nofit)`. This contradicts §7.22's own claim that it "follows the same black-on-warning-colour rule as 7.4/7.21" — the code doesn't actually match. Fix the CSS; keep the red background/border.
37. ✅ **Done 2026-09-12.** **Real bug: Ex-Demo/B-Stock pill tags wrap to two lines** (e.g. "Sellable Return") instead of staying on one line, breaking the pill shape. `shared.css:816` — `.exdemo-option .eo-tag` has no `white-space:nowrap`. Fix, then redo the §7.24 screenshot.
38. ✅ **Done 2026-09-12.** **§7.24 region-specific screenshots** — captured on Simple: `exdemo-slideout-au.png` (AU/NZ wording, "Ex-Demo & Factory Seconds" heading) and `exdemo-slideout-uk.png` (UK "Graded Stock" heading, "Graded" inline text, £ currency, Bolton store name) side by side. Confirmed the 3 option tags (Ex-Demo/Sellable Return/Factory Second) are unchanged across both, per `exdemoCopy()`. `DEVELOPER-BRIEF.md` §7.24 updated; old generic `exdemo-slideout.png` deleted (superseded by the AU-labelled version).
39. ✅ **Done 2026-09-12.** **§7.26 Sticky Mobile Bar — truncated product name added.** Confirmed with Brenton via AskUserQuestion before building: region-agnostic (price/currency/CTA colour already flow through the existing generic render/`applyRegion()` paths, no new hook needed); **name only, no thumbnail** (not matching Vehicle-Specific's thumbnail+name treatment); **keep price visible** on these 4 bars (Vehicle-Specific's own bar drops price to make room for its fitment box + Rack Fit Guarantee badge, but these 4 have no fitment box so there's more room). Added a `.persistent-name` div (the same class already used for the thumbnail-paired name in `.persistent-product` elsewhere — reused here as a plain block child of `.info`, no wrapper needed, since `.info` has no base rule of its own and sizes to the flex item's full width) above the existing price line on Simple, Config-Variant, Sibling-Color, and Grouped-Bundle — each reusing the exact same static product-name string already shipped in that template's own Persistent Bar (desktop), same source of truth, no new copy. No `shared.js`/`shared.css` changes needed — the existing `.sticky-cta-mobile .persistent-name{font-size:12px}` descendant rule already applied. Config-Variant and Sibling-Color's price stays live-synced via the existing `#mobilePrice` id (untouched); their name is static, matching the Persistent Bar's own existing behaviour of not live-updating name on variant/colour switch. Verified with Playwright at 390px on all 4 templates: name truncates cleanly with ellipsis where needed (Simple/Grouped-Bundle's longer names), no horizontal overflow, bar still only appears once the real Add to Cart button scrolls out of view; Config-Variant's Assembled→Flat Pack switch and Sibling-Color's swatch switch both correctly update price live while name stays static; Vehicle-Specific spot-checked unaffected (still thumbnail+name); zero console errors across all 5 templates (aside from the pre-existing unrelated favicon 404). **Flagged for `DEVELOPER-BRIEF.md` §7.26:** owed a text/screenshot update reflecting the name addition whenever Brenton's next separate dev-brief sign-off round happens — not touched this session, per the standing gate. Not pushed — holding for Brenton's sign-off per the usual workflow.

**Same-day follow-up — real pre-existing bug found and fixed, not caused by the item above.** Brenton reported the sticky bar "not at the bottom" on mobile. Root cause: the site's real mobile/desktop breakpoint (used for the Hero single-column reflow, header hamburger, and horizontal-vs-vertical Tabs) is **900px**, but the Sticky Mobile Bar / Persistent Bar pair switched at a different, narrower **640px** breakpoint (`shared.css` lines ~996 and ~1018). Between 641–900px wide, the page already looked and behaved "mobile" everywhere else, but the CTA bar logic still treated it as desktop — showing the top Persistent Bar instead of the bottom Sticky Mobile Bar. Fixed by changing both breakpoints to `900px` to match the rest of the site; the other, unrelated uses of `640px` elsewhere in `shared.css` (touch-nav-arrow hiding, small-phone font-size floor, admin FAB hiding) were deliberately left untouched, since those are genuine small-phone/touch concerns, not the mobile-vs-desktop layout split. Verified with Playwright: at 750px width (the gap that exposed the bug), the bottom bar now shows correctly with the top bar hidden once the real Add to Cart button scrolls out of view; 390px and 1280px both re-confirmed unaffected (bottom bar only below 901px, top bar only at/above it); zero console errors. Not pushed — holding for Brenton's sign-off per the usual workflow.
40. ✅ **Done 2026-09-12.** Short description "Read more" changed from a Details-tab jump to an animated in-place expand/collapse, on all 5 templates. `shared.js`: the existing word-trim-to-2-lines algorithm was factored into `renderShortDescCollapsed()`; a new `toggleShortDesc()` swaps in the full `data-full-text` and relabels the link "See less" on expand, and reverses it on collapse — both directions animate via `el.style.maxHeight` set on the next frame so the CSS transition (`shared.css`: `.short-desc{overflow:hidden;transition:max-height .3s ease;}`) does the reveal/close; collapsing swaps the DOM text back to the trimmed version only after the transition ends (`transitionend`), so the text swap itself never causes a jump. `layoutShortDesc()` now binds the click handler once per element and skips re-trimming `.expanded` elements on resize (just re-measures their height). Markup: removed the now-stale `data-jump-tab="t1"` attribute from all 5 templates' `.short-desc-readmore` links (it no longer jumps tabs, and leaving it would have double-bound `initTabJumpLinks()`'s generic handler). `PAGE-GLOSSARY.md`'s "Short description 'Read more'" entry updated to match; the separate stale "Short Description" glossary row (pre-2026-09-11 layout) was also corrected while in the area. Confirmed with Brenton: region-agnostic, and expand/collapse animates (smooth height transition) rather than snapping instantly. Verified with Playwright on all 5 templates: full expand→collapse round trip restores the exact original trimmed text/height, Details tab radio (`#t1`) confirmed untouched by the click (no more tab-jump), mobile (390px) spot-checked on Simple with no horizontal overflow, zero console errors throughout.

**Same-day follow-up (2026-09-12): status-pill style unified site-wide.** Brenton preferred the new Delivery/Click & Collect v2 preview's pill style (`.stock-chip` family — soft tinted background, coloured 1px border, `2px 8px` padding, 11px/900-weight uppercase text) and asked for every status-style pill on the page to match it, not just this new widget. Two real inconsistencies found and aligned: `.exdemo-option .eo-tag` (Ex-Demo/Sellable Return/Factory Second tags) had slightly different padding (`3px 9px`); `.variant-option .v-badge` (the "Save X%" pill on Config-Variant's and Vehicle-Specific's variant-picker cards, defined in each template's own inline `<style>`) had both a smaller font-size (10px) and different padding (`2px 7px`). Both now byte-identical to `.stock-chip`'s sizing. Explicitly confirmed with Brenton as **out of scope**: the "SAVE 14%" discount badge next to price (`.badge-save`) keeps its current solid-red-fill treatment — it's a promotional callout, not a status label, and the whole pricing section already has its own separate flagged redesign discussion (item 23 above).

**Real pre-existing bug caught during the follow-up full-template check (2026-09-12):** `.stock-chip` had no `white-space:nowrap` (unlike `.eo-tag`, which already had this fix from item 37) — invisible before this session since `rrgStorePillsHTML()`'s "order" status was hardcoded dead code (see `ORDER_IN_STORES` above, item 19), so "Order In — 1-2 Days" had never actually rendered anywhere until now. With Malaga (WA) now genuinely flagged as order-in, the pill wrapped to two lines in both the new DC v2 widget and the Store Slide-out. Fixed by adding `white-space:nowrap` to the shared `.stock-chip` base rule.

Verified with Playwright across **all 5 templates** (not just Config-Variant): DC v2 widget checked with a real postcode on each — Simple (QLD), Config-Variant (VIC), Sibling-Color (WA, including the Malaga "Order In" pill fix and the Store Slide-out sharing the same fix), Vehicle-Specific (SA, plus its variant-picker "Save %" pill alongside the untouched solid-red price badge), Grouped-Bundle (TAS, the single-store edge case) — every pill renders correctly on one line, zero wrapping/overflow at both 1280px and 390px, zero console errors on any template. Ex-Demo/Factory Seconds tags also spot-checked on Simple and Grouped-Bundle.

**Corrected same day — the pill recipe itself was wrong, not just its rollout.** Brenton's actual reference (screenshots of the widget above) has less-rounded corners, no border/stroke, and dark grey text — not the fully-rounded, coloured-border-and-text look every pill (including the new ones above) still had. Fixed across the whole family in one pass: `.stock-chip` base rule now `border-radius:6px` (was `999px`) and `color:#333` (dark grey, was per-state colour) with the `border` removed entirely from all 4 state modifiers (`.in`/`.order`/`.none`/`.display`) — only the tinted `background` stays per-state. Same fix applied to `.exdemo-option .eo-tag` and both templates' `.variant-option .v-badge` (Config-Variant, Vehicle-Specific) for full consistency, per the "any pill on the page" ask. The one context-specific override, `.showroom-widget .stock-chip.display` (gold pill on the dark Showroom Finder card), kept its existing black text — already dark/neutral, just dropped its now-redundant `border-color`. Re-verified with Playwright on all 5 templates (same postcode checks as above) plus a `getComputedStyle` check confirming the showroom-widget gold pill (`background:rgb(255,202,72)`, `color:rgb(0,0,0)`, `border:0px none`, `border-radius:6px`) — zero console errors throughout. Not pushed.
