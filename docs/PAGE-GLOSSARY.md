# PDP Page Glossary

Reference for naming the parts of the product page (PDP) templates, so "gallery," "widget," "panel," etc. all mean one specific thing in conversation. Split into **Sections** (structural blocks that lay out the page) and **Widgets** (interactive components that live inside sections). Where a name has caused confusion before, that's called out explicitly.

Applies across the 5 PDP templates in `prototypes/`: **Simple**, **Sibling-Color**, **Config-Variant**, **Vehicle-Specific**, **Grouped-Bundle**. The **Vehicle Landing Page** (`prototypes/vehicle-landing/`, added 2026-09-15) is a different page type — one page per make/model rather than per product — and gets its own short section near the end of this file rather than being folded into the tables below, which are PDP-specific.

---

## ⚠️ Naming clash to know about: "Gallery"

There are **three different things** that have all been called "gallery" at some point. Use the specific name:

| Specific name | What it is | Where it lives |
|---|---|---|
| **Main Product Gallery** | The primary product photos — big image + thumbnail strip at the top of the page, beside the Decision Panel. Always stays put beside the Decision Panel; nothing moves it. | `.gallery-main`, `.gallery-thumbs`, `.gallery-col` — all templates |
| **Fitted Photos Gallery** | Real customer installation photos, presented as the "Gallery" panel widget (icon badge, "View All In-store Fitments (N)" link, "Explore Fitting Costs" button, photo carousel with nav arrows + page dots) — separate from the Main Product Gallery entirely. Final design locked in 2026-09-12: full width above the Trust Row, non-red background, permanently (the placement/red-background Demo State Panel toggles that previously existed to help decide this have been removed). | `.fit-gallery-section` / `.fit-gallery-panel` — **Vehicle-Specific only** |

When you say "the gallery" going forward, say which of the three you mean, or use the bold name above.

---

## Sections

Top-to-bottom structural blocks. Not every section appears on every template — noted where limited.

| Section name | Description |
|---|---|
| **Utility Bar** | Thin top strip above the main header (nearest store, vehicle, call us, region, account) — `.rrg-utility-bar` |
| **Region Selector** | The Utility Bar's "🇦🇺 Australia ▾" item — previously decorative-only, now a working dropdown (2026-09-11, backlog item 21). Behaves like the Demo State Panel toggles: a live client-side region swap (AU/NZ/UK), no page reload, resets to AU on every page load. Desktop only (same as the rest of the Utility Bar's right-hand items). See Widgets below for what it cascades into. — `.region-switcher` / `applyRegion()` |
| **Main Header** | Logo, nav links, search bar, cart icon — `.rrg-main-header` |
| **Breadcrumbs** | Trail above the hero — `.rrg-crumbs`. 3 levels (Home → Category → Product) on Simple/Config-variant/Sibling-color/Grouped-bundle; 4 levels on Vehicle-specific (Home → Vehicle → Category → Product, e.g. Home → Toyota Hilux N80 → Platforms & Trays → product) per Graham's requested structure. Note: this class name doesn't contain the word "breadcrumb" — a session grepped for that literal word 2026-09-10 and wrongly concluded breadcrumbs didn't exist. Search for `.rrg-crumbs` instead. |
| **Hero** | The main above-the-fold row: Main Product Gallery on the left, Decision Panel on the right — `section.hero` |
| **Decision Panel** | The right-hand sidebar column in the Hero: brand logo, SKU, price, fitment status, variant/colour picker, Add to Cart, payment badges, Delivery/Click & Collect widget — `.decision-panel`. **Mobile only (≤900px, 2026-09-10):** renders above the video/Get It Installed row, not below it — `.gallery-col`'s children become independent `.hero` grid items via `display:contents` so gallery images/thumbs, Decision Panel, and the video/install row can each be ordered independently without moving any markup. |
| **Trust Row** | 4-column strip of trust points (Trusted Since 1989, Trained Professionals, etc.) below the hero — `.trust-row` |
| **Showroom Finder section** | Full-width black CTA block ("See It In Person — On Display At N Stores") — its own section below the hero, not inside the Decision Panel. **Now on all 5 templates** (client ask, 2026-09-10; previously Vehicle-Specific only), toggleable per-SKU via the Demo State Panel. See Widgets below. |
| **Fitted Photos Gallery section** | "See it fitted to a [vehicle] just like yours" — grid of real installation photos. **Vehicle-Specific only.** See naming clash above. |
| **Short Description** | The real Rackit short-description field, under the title in the Decision Panel, on all 5 templates — see the "Short description 'Read more'" widget entry below (consolidated here 2026-09-11; this row previously described the pre-consolidation layout). |
| **Body Tabs section** | Full-width area containing the Tabs widget (Details/Specifications/Gold Guarantee/Shipping/Fitting Instructions) — `.body-cols` |
| **Related Products section** | "Related Products" heading + grid of cross-sell product cards — `.related-heading` / `.related-grid` |
| **Design Rationale section** | Bottom-of-page internal/reviewer section explaining build decisions and trade-offs — not part of the shipped design — `.rationale` |
| **Sticky Mobile Bar** | Condensed product name + price + Add to Cart bar fixed to the bottom of the screen, mobile only — `.sticky-cta-mobile`. Vehicle-Specific carries a thumbnail + name (`.persistent-product`, added 2026-09-11 alongside its fitment box); the other 4 templates carry name only, no thumbnail (`.persistent-name`, added 2026-09-12 — Brenton's explicit choice over matching Vehicle-Specific's thumbnail treatment). |
| **Persistent Bar** | Condensed fitment/price + Add to Cart bar fixed to the top of the screen on desktop, fades in once the Decision Panel scrolls out of view — `.persistent-bar` |

---

## Widgets

Interactive or reusable components that live inside the sections above.

| Widget name | Description |
|---|---|
| **Main Product Gallery** | Big image + horizontal-scroll thumbnail carousel. See naming clash above. |
| **Fitted Photos Gallery** | "Fitment Gallery" panel: icon badge + heading, right-aligned "View All In-store Fitments (N)" link, and a landscape-photo carousel (nav arrows + page dots) of real customer installation photos, 7 per view. Clicking any carousel photo opens that same fitment's detail view directly in the slide-out below (`openFitGallerySlideoutAt()`), skipping the grid. Clicking "View All In-store Fitments" opens a right-edge slide-out (`buildFitGallerySlideout()` in shared.js, same drawer convention as the Store Slide-out) with two views: a photo grid (default — real photos are `FIT_GALLERY_PHOTOS`, 16 scraped `{thumb, id}` objects, cycled to fill the real count; see spec.md Section 7 for the 16-vs-283 data gap) and, on clicking a photo, a per-fitment detail view (`renderFitGallerySlideoutDetail()`) matching the live site's own "Browse Fitment" popup — Prev/Next, the photo's real Rackit `product_id` as its "Fit #," the page's own real vehicle + Rack Components (read live from the What's Included rows, same on every fitment — no fabricated per-vehicle variety), and a "Back to Product" button (closes the drawer, scrolls to the Decision Panel) in place of the live site's "View Rack & Buy." Final gallery placement/colour locked in 2026-09-12 (full-width, non-red, permanent). **Vehicle-Specific only.** See naming clash above. |
| **Variant Picker** | Comparison-card selector for Assembled vs Flat Pack — **Config-Variant & Vehicle-Specific** — `.variant-picker` |
| **Colour/Swatch Grid** | Grid of colour swatches for sibling-colour products (e.g. MaxTrax) — **Sibling-Color only** — `.swatch-grid` |
| **Fitment Status widget** | The "fits your vehicle / doesn't fit / no vehicle set" box, white card with a coloured left accent — appears in the Decision Panel, the Sticky Mobile Bar, and the Persistent Bar — `.fitment`. Copy no longer says "kit" (now "this product," 2026-09-10); the "doesn't fit" message now names the customer's full saved vehicle spec (make/model/generation, body style, roof type, year), not just its name — `FITMENT_COPY.no_fit.detail` in shared.js is a function of the session vehicle rather than a static string. |
| **Rack Fit Guarantee** | Hand-drawn placeholder vector icon (shield + checkmark, single-colour via `fill-rule="evenodd"`), icon-only with a `title` tooltip ("Peace of mind with our FREE Nationwide Rack Guarantee") on all 3 Fitment Status instances — `.rack-fit-badge` (Decision Panel corner) and `.rack-fit-badge-icon` (Sticky Mobile Bar, Persistent Bar). Colour tied to `--rrg-fits` (green), not the region skin — confirmed with Brenton, since the badge only ever shows in the "Fits your vehicle" state, which doesn't vary by region. **Replaced 2026-09-12** — the original real client asset (`fitgaurantee.svg`) turned out to be a baked-in raster image wrapped in an `<svg>`, not a true vector, so it couldn't be recoloured; this is an explicit placeholder pending real client-approved artwork. Vehicle-Specific only, no demo toggle — visible only when the session vehicle is a confirmed match, auto-hidden by `applyFitmentState()` for "Confirm your vehicle"/"Doesn't fit"/no vehicle set. Clicking any instance jumps to and opens the Gold Guarantee tab. |
| **Important Vehicle Fit Notes** | Amber-accented content block directly below the Fitment Status widget in the Decision Panel, above the variant picker — `.vehicle-fit-notes` / `#vehicleFitNotes`. Vehicle-Specific only, starts hidden (Demo State Panel toggle), placeholder copy pending real client content. Added 2026-09-10. |
| **Region Selector cascade** | What switching the header Region Selector (see Sections above) actually changes on the page, all via `applyRegion()` in shared.js: (1) Click & Collect widget's default tab — AU/UK → Click & Collect, NZ → Delivery; (2) Showroom Finder heading — AU keeps its real per-template "N Stores Nationwide" copy (cached from the page's own markup so switching back restores it), NZ/UK switch to "On Display At the [City] Store"; (3) Showroom Finder map — AU shows the full 35-store clustered network, NZ/UK zoom to one always-on-display pin at a fabricated placeholder location (`REGION_SINGLE_STORES`, flagged demo-only — RRG has no real NZ/UK stores); (4) both "View all stores" links (Showroom Finder's and the Click & Collect card's) hide for NZ/UK, since the real 35-store slide-out is AU-only data, and the Click & Collect card's postcode-driven "Showing stores within 100km..." line/results are suppressed the same way; (5) Trust Row's "Trusted Since 1989"/"Australia's Largest" items (`[data-trust="founded"/"network"]`) swap to region-appropriate placeholder copy (`REGION_TRUST_COPY`), flagged for real client wording later; (6) **UK only** — brand skin (`applyRegionBrand()`) and currency (`applyRegionCurrency()` + `regionCurrencySymbol()`), see the UK Brand Skin entry below; (7) Trust Row's "Need Help" phone number/`tel:` link (`REGION_PHONE`, added 2026-09-11 — AU `1300 071 264`, NZ `09 481 1910`, UK `01204 899778`); (8) Payment-Plan Badges' provider set (see that entry above — NZ drops Zip, UK is a full 3-provider swap); (9) header Utility Bar's "Your Nearest Store" text (`applyRegionNearestStore()`, added 2026-09-11 — caches each template's real AU value so switching back restores it, reads NZ/UK from `REGION_SINGLE_STORES`). Resets to AU on every fresh page load, no persistence — same convention as the rest of this prototype's demo-state toggles. |
| **UK Brand Skin** | RRG's real UK sister brand, "The Roof Box Company" (roofbox.co.uk) — applies only when the Region Selector is set to UK, toggled via a `body.region-uk` class. Colour: `body.region-uk` overrides the `--rrg-red`/`--rrg-red-dark` custom properties to their real navy (`#2E41AE`/`#1F2E82`, read straight off their logo SVG's own fill values), which recolours every component already built against those tokens for free — utility bar, trust icons, links, tab underline, sale badges — no per-component CSS needed. Add to Cart gets its own override to their real green `#26B226` (read off their live "Click Here To Order" button) since it was never on the `--rrg-red` token (RRG's own locked-in gold default). Deliberately **not** a typography change — Barlow Condensed/Lato stay, confirmed scope with Brenton. Currency: prices show `£` instead of `$` for UK (same demo numeric values, symbol swap only) via `regionCurrencySymbol()`, which drives both `fmtAud()` (shared.js) and the 3 per-template `fmtMoney()` duplicates so any future price render picks it up automatically, plus a one-time `applyRegionCurrency()` DOM sweep that fixes whatever's already on the page at the moment the region switches (static per-SKU prices, Related Products, payment badges). NZ is unaffected by any of this — it stays Roof Racks Galore-branded, confirmed with Brenton since RRG's own real copy already treats AU+NZ as one brand/network. **Logo (real SVG, `prototypes/_shared/brand-roofbox-uk-logo.svg`, scraped from their live site)** swaps in via `applyRegionBrand()` on every `.rrg-logo img` on the page, and — per Brenton's explicit ask, refined twice — spans both header bars rather than just sitting inline: bottom flush with the base of the white bar, top reaching only halfway up through the blue bar (not all the way to its own top). Applies in all three header contexts, not desktop-only: the top-of-page header (desktop: `.rrg-logo` reserves 90px, `<img>` absolutely positioned against it; mobile: same halfway-to-base rule adapted for `.rrg-main-inner`'s 2-row grid, where "base of the white bar" means the base of the icon row specifically, not the full header including the search row underneath) and the **sticky condensed mobile header**, which had no blue-bar equivalent at all until this — it grows one via `border-top:20px solid var(--rrg-red)` so the logo has something to span into even after the real Utility Bar has scrolled away. Two real bugs surfaced building this (see [[feedback-pdp-design-review]]'s reusable-gotcha note for the second one): a *centre*-aligned `.rrg-logo` box collapses to ~0 size once its only child is pulled `position:absolute` out of flow, so every instance uses `align-self:flex-start`/`start` to anchor to the row's actual top instead of a collapsed box's centre; and on mobile/sticky specifically, `.rrg-logo` needs an explicit width (matching its img) or the project's own global `img{max-width:100%}` reset silently clamps the img's width to `0px` against the collapsed 0-width container. |
| **Payment-Plan Badges** | Instalment amounts shown under Add to Cart, real logos + live-computed amounts — `.payment-badges` / `paymentBadgeSet(region)`. **Region-aware (2026-09-11):** AU/default = Afterpay + PayPal + Zip ("Pay in 4" + Zip's own weekly copy); NZ drops Zip (2 badges); **UK is a full 3-provider swap**, not a hide/show — Clearpay (Afterpay's UK brand, "Pay in 4") + PayPal ("Pay in 3," not 4) + Klarna ("Pay in 3" over 3 months, a distinct provider kept as its own badge). 3 generic `.payment-badge` slots (no more fixed `pb-afterpay`/`pb-paypal`/`pb-zip` identity), filled per-region. |
| **Delivery / Click & Collect widget** | Tabbed widget (Delivery | Click & Collect) sitting directly under Add to Cart in the Decision Panel — `.dc-widget`. Reworked 2026-09-10: one shared postcode field (`.dc-postcode-row`, sits above both tab panels, not one input per tab, starts **empty** — a follow-up fix same day, an earlier version prefilled a demo postcode which read as real customer data), default active tab is now **Click & Collect** (was Delivery), small tab icons. **Both panels' results are hidden until a postcode is entered** (Delivery's Standard/Express rates and Collect's named store rows) — Delivery was initially missed in this fix (left showing regardless) and corrected in a later pass same day; while hidden, Delivery shows a `.dc-postcode-prompt` ("Enter your postcode above to see delivery options.") and Collect's results area is replaced by its `.dc-viewall-line` (store count + "View all stores", copy changes once a postcode is entered). See `initDcPostcode()`. |
| **Store Slide-out** | Right-edge drawer listing the full real store network — opened by "View all stores," present on the Click & Collect widget (all 5 templates) and the Showroom Finder widget (Vehicle-Specific only so far). Rebuilt 2026-09-10 (follow-up same day) on real data: scraped from the live site's own Store Inventory slide-out (a real product's Click & Collect card, "View all Stock") via Playwright — 35 real stores, real names/addresses/phone numbers/Google Maps links, grouped by state to match the live site's presentation. See `RRG_STORE_NETWORK` below. — `.store-slideout` / `.store-slideout-backdrop` / `buildStoreSlideout()` |
| **RRG_STORE_NETWORK** | The canonical real store dataset backing the Store Slide-out, the Click & Collect widget's inline rows, and (as of 2026-09-11) the Showroom Finder map's pins — one shared list in shared.js (store identity doesn't vary by product), not per-template fabricated data. Each store now also carries `lat`/`lng` (added 2026-09-11 for the map rework, demo-precision approximate suburb centres). Per-store In Stock/Order In status and which stores are flagged "On Display" (`ON_DISPLAY_STORES`) are still demo/placeholder — no real per-SKU per-store stock feed exists. New 2026-09-10. |
| **On Display pill** | Pill shown on individual store rows (Click & Collect's inline rows and the Store Slide-out) alongside the existing In Stock/Order In pill, wherever that specific store is flagged as carrying this product on display (`ON_DISPLAY_STORES`) — e.g. Moorebank shows both "In Stock" and "On Display." **Corrected 2026-09-10 (same day, client feedback):** an earlier version placed this pill next to the price/stock-status line instead — wrong, since "on display" is a per-store fact, not a blanket product-level one; that placement was removed entirely. Distinct gold/bronze colour from the green "In Stock" pill — `.stock-chip.display` / `rrgStorePillsHTML()` |
| **Showroom Finder widget** | High-contrast black CTA block, "on display at N stores," postcode search, paired with the Interactive Map (below) in a split view — sits in its own full-width section below the hero (not the Decision Panel). Headed by an `<h3 class="showroom-heading">Showroom Finder</h3>` (2026-09-11, same shared heading style as Related Products/Fitment Gallery/Tabs/FAQ) sitting inside `#showroom` itself, above `.showroom-widget` — so it's already hidden along with the rest of the section by the existing `applyShowroomFlag()` toggle, no separate wiring needed. **All 5 templates**, with a Demo State Panel toggle ("On display in-store") to preview a SKU that isn't on display anywhere — `.showroom-widget` / `#showroom` / `applyShowroomFlag()`. Its own inline store-row pills use the same gold "On Display" colour as the On Display pill above. Its "View all stores" link is white (`.showroom-widget .dc-viewall`, scoped to this widget only — the same link in the Click & Collect card stays blue), since white reads better against the black background. |
| **Interactive Map** | Leaflet + OpenStreetMap tiles, real pins for all 35 stores in `RRG_STORE_NETWORK` — runs alongside the Showroom Finder widget's black block in a **split view** (`.showroom-widget.split-view`, `#showroomDefaultView` : `#showroomMap` at a 1:2 width ratio, map flush to the widget's edges with no padding). **Piloted on Vehicle-Specific only, then locked in as the default and rolled out to all 5 templates same day (2026-09-10) after client review** — no longer a Demo State Panel toggle; `applyShowroomMapFlag(true)` runs unconditionally wherever `#showroomMap` exists. Fixed along the way: a z-index bug where the map rendered on top of the Demo State Panel (`.showroom-map{position:relative;z-index:0}` contains Leaflet's internal panes, which otherwise default up to z-index 1000) and a layout bug where the first version replaced the black block entirely instead of running alongside it. **Reworked 2026-09-11 (backlog item 22):** default view now `fitBounds()`s to show every store at once (was a fixed zoom on 4 fabricated NSW stores); pins are colour-coded red (`.rrg-map-pin.on-display`, per `ON_DISPLAY_STORES`) vs. grey (custom `L.divIcon`s, no image assets); Leaflet.markercluster groups pins into numbered bubbles when zoomed out (grey by default, red if any store inside is on display — `rrgClusterIcon()`) and splits them apart on zoom in. The 5 per-template `window.SHOWROOM_MAP_STORES` demo arrays are gone — pins now derive from the same canonical `RRG_STORE_NETWORK` (with `lat`/`lng` added) the Store Slide-out and Click & Collect widget already use, so all of a page's store data comes from one source. Real postcode-driven radius search still isn't implemented (no geocoding service) — flagged as the gap before this leaves prototype stage. — `#showroomMap` / `applyShowroomMapFlag()` / `rrgPinIcon()` / `rrgClusterIcon()` |
| **Package Contents / "What's Included"** | List of individual kit components, quantity leading each row (`.pi-qty`, e.g. "1x Rhino-Rack 6 Series Pioneer Platform"), SKU text itself the click-to-copy target (`.sku-copy`) — **Vehicle-Specific & Grouped-Bundle both** — `.package-items`. Vehicle-Specific's quantities (all ×1) are a placeholder default, no real per-component data exists. Grouped-Bundle's quantities (all ×1) are real — taken verbatim from that page's own "Bundle Includes" list in the Details tab. Both templates match as of 2026-09-10 (Grouped-Bundle was missed on the first SKU-copy pass, corrected same day). |
| **Get It Installed CTA** | Full-bleed installer photo with an icon badge + "Get It Installed" heading, 2 real trust bullets ("Over 200,000 roof racks fitted" / "Nationwide network of trained fitters," drawn from Trust Row's real copy), and a subdued `.btn-outline` button, all overlaid on the photo via a dark gradient scrim — paired with the product video under the gallery, locked to the same `aspect-ratio:16/9` so the two are exactly the same height side by side. **Redesigned 2026-09-11 (spec.md item 16), refined twice same day**: a first pass used a framed photo + white body below it, but that made the card taller than the video it sits beside and left a visible border around the photo — reverted to a full-bleed absolutely-positioned photo with no border. A second pass fixed two more real bugs: `installer.png` itself has a ~10–25px black border baked into the source file (confirmed by pixel-sampling, not CSS) — `.install-photo{transform:scale(1.16)}` crops it outside the visible frame; and the no-video full-width state's content was stuck top-left instead of centred, since `.install-body{height:100%}` only resolves against a *definite* parent height (true for the paired 16/9 state, not the no-video min-height-only box) — switched to `position:absolute;inset:0` (same technique the photo already used) so centring works in both states. The no-video state also got larger text/icon sizing since it has more room; the paired state's sizing was left untouched. The CTA is deliberately outline-style rather than red/gold so it doesn't compete with the one purchase CTA. **On all 5 templates** — `.install-media-row` / `.install-cta-panel`. On Vehicle-Specific, the button copy is fitment-count-aware (added 2026-09-10, threshold logic added 2026-09-11 per backlog item 17) and branches on the gallery's real fitment count (`#fitGallerySection`'s `data-count`, previewable via the Demo State Panel's new "Fitment count" number input): **5 or more** reads "We've fitted this N times — view the gallery" and links to the Fitment Gallery; **1–4** drops the number ("See real fitments," still links to the gallery, since the number itself isn't impressive enough to lead with); **0** (gallery exists but nothing to show) reads "Get It Installed" and falls back to the same external link as the no-gallery state below, kept as separate copy since it's a distinct scenario. When the "Fitment Gallery exists for this product" checkbox is off entirely, it falls back to **"See Fitting Options"** (real link, `roofracksgalore.com.au/roof-rack-installation-and-fitting-costs`, opens in a new tab). The other 4 templates (no fitment-gallery concept) show "See Fitting Options" as their permanent default. |
| **Product Video** | Embedded product video or placeholder, appears under the Main Product Gallery — `.video-wrap` / `.video-placeholder` |
| **Price Block** | The price + stock-status area of the Decision Panel — `.price-block`. **Redesigned 2026-09-12** (spec.md Section 12 item 23, built off Brenton's Figma reference "Price Layout Design.png") to a stacked two-line layout: `.price-line-now` ("Now" label + `.price-now`, both red when on sale) above `.price-line-was` ("RRP" label + struck-through `.price-was` + `.badge-save`, both the label and price sharing identical dark-grey `#434343`/Lato-400 styling so the whole "RRP $X" reads as one continuous struck-through phrase — the RRP price is sized (19px) to match `.badge-save`'s own rendered height). Off-sale, both the "Now"/"RRP" labels and the RRP line disappear entirely — a plain black price, same as before this redesign. `.price-now`/`.price-was`/`.badge-save` kept their exact class names/JS hooks; `syncPriceLabels()` in shared.js drives the labels/line generically off `.price-was`'s own hidden state (same signal `reapplySaleFlag()`/each template's `renderPrice()`-equivalent already used), so per-variant/colour discounts (e.g. sibling-color's Titanium Grey, which has no real discount) resolve correctly too. The condensed Sticky Mobile Bar/Persistent Bar price is adapted, not the full stacked treatment — same red/dark-grey colour language on the existing single-line price, no labels/badge (no room). |
| **Sale Tag** | Corner ribbon graphic shown when a product is on sale — `.sale-tag`. **Mobile-only change, 2026-09-12:** a second instance (`.gallery-sale-tag`) now sits over the top-right corner of the Main Product Gallery's main image at ≤900px width instead of the Decision Panel — two mobile-only attempts to keep it near the price block (vertically centred against the block, then a fixed top-right corner of the block) both still read as visually disconnected once the price content itself centres on mobile, so it moved off the price block entirely. The original Decision Panel instance stays exactly as-is on desktop (≥901px). Both instances toggle together off one shared signal — `syncSaleTag()` in shared.js updates every `.sale-tag` on the page at once, not just one scoped to a single panel — so Titanium Grey (sibling-color's no-discount swatch), the Demo State Panel's on/off toggle, and variant switches all stay correct for whichever instance is currently visible. |
| **Brand Logo** | Manufacturer logo at the top of the Decision Panel — `.brand-logo`. Sized against a bounding box (`max-width:160px;max-height:32px`, 2026-09-10) rather than a fixed height, so brands with different source aspect ratios (MAXTRAX/Front Runner's squatter canvas vs. Rhino Rack/Yakima's thin wordmark) render at a similar visual footprint. |
| **Special Order banner** | **Now a 4th mutually-exclusive `STOCK_STATUS` value** (2026-09-11, alongside In Stock/Low Stock/Not in Stock/Discontinued), not an independent add-on checkbox as originally built — selecting it fully replaces the stock-status line ("⏱ Special Order — Ships in 5-7 Days," amber) as well as showing the `.special-order-banner` detail copy above Add to Cart. Add to Cart stays enabled — informational, not a purchase blocker. `applyStockStatus()` drives it (`applySpecialOrderFlag()` was removed). — `.special-order-banner` |
| **Discontinued state** | The 5th `STOCK_STATUS` value (2026-09-11, spec.md item 23) — a discontinued item can't also be "In Stock," so it's mutually exclusive with the other 4. Strikes through the price in place (reuses the "was" price's grey strikethrough), fully hides `.cta-col` (Add to Cart + payment badges) and replaces it with a `.discontinued-banner`, and reveals a new full-width `.discontinued-alternates` section below the Hero with 3 real, curated, currently-live alternative products (reuses the `.related-grid`/`.product-card` component). Sticky/Persistent bars show a disabled "Discontinued" button via the generic `cfg.ctaLabel` field. **All 5 templates.** — `.discontinued-banner` / `.discontinued-alternates` / `applyStockStatus()` |
| **Compatibility / Cart-Conflict banner** | New Demo State Panel toggle (2026-09-11, spec.md item 24), "Cart contents": Empty (default) / Compatible item in cart (no banner) / Incompatible item in cart (`.cart-conflict-banner` above Add to Cart, same amber/black informational tier as Special Order, never disables the CTA). "Incompatible" is specifically a **platform-vs-accessory** relationship (a roof rack platform in the cart flagged against an accessory product, or vice versa) — not a vehicle-fitment or brand-mismatch check. Per-template conflict item/reason text read off `data-conflict-item`/`data-conflict-reason` attributes on each template's own `.cta-col`. **All 5 templates**, region-agnostic. — `.cart-conflict-banner` / `applyCartConflict()` |
| **Ex-Demo / B-Stock CTA + slide-out** | Inline text appended to the stock-status line itself (`In Stock — Ex-Demo/Factory Seconds from $X`), price portion a clickable link, opening a **right-edge slide-in drawer** (changed 2026-09-11 from a centred modal, matching the Store Slide-out's pattern) listing Ex-Demo/Sellable Return/Factory Second discount options. **UK calls this "Graded"** instead of "Ex-Demo/Factory Seconds" (drawer heading + inline stock-line text only — the 3 individual option tags stay the same wording in every region). Option-card store names are real, derived from `RRG_STORE_NETWORK`/`REGION_SINGLE_STORES` rather than a fabricated list. — `.exdemo-inline-link` / `.exdemo-slideout*` / `buildExdemoSlideout()` / `openExdemoSlideout()` / `renderStockLine()` |
| **Paid "Fitted" Option** | Demo-preview-only upsell — either a third variant card or a checkbox above Add to Cart, offering paid fitting — **Config-Variant & Vehicle-Specific only** — `.fitted-upsell-checkbox` etc. |
| **Tabs widget** | Accordion/tab-bar of Details, Specifications, Gold Guarantee, Shipping Info, Fitting Instructions, Reviews — `.tabs`, headed by an `<h3 class="tabs-heading">Product Information</h3>` (2026-09-11) sharing the same shared heading style as Related Products/Showroom Finder/Fitment Gallery/FAQ. Specifications renders as a real 2-column table (`.specs-table`, `<th>` attribute / `<td>` value), not dot points, on all 5 templates (2026-09-10). **Gold Guarantee shows full real guarantee copy** (Standard + Gold Rack Guarantee, Rack Check, Key Cover, Free Basic Accessory Fitting, 90-Day Exchanges) — a 2026-09-10 fidelity pass reverted an earlier condensed "2 USP lines + Learn more link" version once Brenton confirmed the client wanted the real content back; there is no link anywhere in this tab now (the old `.guarantee-usps`/`.guarantee-learn-more` classes are gone). Pure CSS (radio + label + sibling `.content`, no JS) — see "Read more" below for what that means for jump-links into it. |
| **Short description "Read more"** | `.short-desc` — the real Rackit short-description field, under the title in the Decision Panel — clamps to 2 lines via `layoutShortDesc()`/`renderShortDescCollapsed()` (shared.js), trimming word-by-word so "`<text>`… Read more" fits exactly. **Changed 2026-09-12 (Section 12 item 40):** clicking "Read more" no longer jumps to the Details tab — it expands the full text in place with an animated height transition (`toggleShortDesc()`, `.short-desc{transition:max-height .3s ease}`), and the link relabels to "See less" to re-collapse. (Consolidated 2026-09-11: this used to be split across two elements — this real field plus a separate fabricated `.value-prop` micro-differentiator line occupying the same slot, and an unclamped duplicate of `.short-desc` sitting lower on the page near the USP row. `.value-prop` was never a real data field and was removed; the lower-page duplicate was deleted once its content moved up here.) |
| **Related Products carousel** | Grid of cross-sell product cards below the main content — `.product-card` |
| **Copy-to-clipboard buttons** | The SKU text itself is the click target everywhere now (`.sku-copy`, 2026-09-10) — main SKU row on all 5 templates, plus every Package Contents row on both Vehicle-Specific and Grouped-Bundle. No separate "⧉ Copy" icon button remains anywhere in the prototype — the old `.copy-btn` CSS was removed from shared.css as dead code once the last usage (Grouped-Bundle) was converted. Wired up by `initCopyButtons()` in shared.js. |
| **Demo State Panel** | Floating "Demo State" button (bottom-right) that expands into a reviewer-only control panel for previewing product states (video/sale/stock/shipping/collect/special order/ex-demo/showroom/fitted option/gallery placement/session vehicle, plus the Vehicle-Specific-only interactive map pilot). Not part of the shipped design. Add to Cart colour is no longer a toggle here — gold is now the locked real default. — `.admin-fab` / `.admin-panel` |
| **Reviews tab** | New tab (2026-09-11), last in the Tabs widget on all 5 templates (`t6` where Fitting Instructions exists, `t5` where it doesn't). Originally built as a hand-rolled `.review-summary`/`.rvw-tabs` pill toggle over placeholder `.review-card`s — **replaced same day** with a real REVIEWS.io Polaris widget embed (`store: 'roof-racks-galore'`, `#ReviewsWidget`, one inline `<script>` per template) once Brenton supplied the real widget snippet. `options.types: 'product_review,store_review'` makes the widget render its own native Product/Company sub-tabs, so the custom pill toggle, `.review-card` CSS, and `initReviewTabs()` in shared.js were all removed as dead code. Each template passes every SKU in its family (semicolon-separated, e.g. sibling-color passes all 13 MaxTrax SKUs) so reviews show regardless of which variant/colour is selected — the widget is not re-initialized on variant switch. `hide_if_no_results` is deliberately set to `false` (the vendor's own pasted default was `true`) since the real `roof-racks-galore` account currently has zero reviews against every demo SKU here — with it `true` the tab would render nothing at all, which reads as broken; `false` shows the widget's real "No reviews collected for this product yet — Be the first to write a review" state instead, which is honest and correctly branded. `styles` colours were lightly retinted from the vendor's neutral black defaults to RRG's red/gold/Barlow Condensed to match the rest of the page. The star rating in the Decision Panel's `.reviews-strip` ("See reviews") still jumps here via the existing generic `initTabJumpLinks()` mechanism (`data-jump-tab`). |
| **Decision Panel star-rating badge** | The small `★★★★★ 4.3 (128 reviews) · See reviews` line above the price — `.reviews-strip[data-review-sku]`. No suitable off-the-shelf REVIEWS.io widget exists for this compact above-the-fold placement, so it's custom-built against the same real data source as the Reviews tab: `initReviewSummary()` in shared.js calls the same `api.reviews.io/timeline/data` endpoint the Polaris widget itself calls (confirmed CORS-open via network inspection), passing each template's real SKU list via `data-review-sku`. **Deliberately does not fall back to the store-wide rating** when a SKU has zero product reviews (an earlier version did, corrected 2026-09-11 same day per client feedback — a company-wide figure next to one specific product reads as a per-product rating and is misleading on an individual PDP) — the strip just hides itself in that case, same as on fetch failure. Since none of this prototype's demo SKUs have real reviews yet, the badge is currently hidden on all 5 templates; it will appear automatically once real reviews exist against a SKU. Stars render as filled/dimmed (`.stars-empty{opacity:.35}`) based on the rounded real average, not a fixed 5-star decorative default. |
| **FAQ section** | New accordion section (2026-09-11), below the Tabs widget and above Related Products on all 5 templates — `.faq-section` / `.faq-list` / `.faq-item`. Deliberately **not** a tab (unlike Reviews) since FAQ items should open independently rather than being mutually exclusive — built on native `<details>`/`<summary>`, no JS. Heading (`.faq-heading`) shares the same Barlow Condensed Bold Italic style as Related Products/Showroom Finder/Fitment Gallery headings. 4 Q&As per product, tailored per template (fitment/warranty for Vehicle-Specific, assembled-vs-flat-pack for Config-Variant, colour/mounting for Sibling-Color, bundle-contents for Grouped-Bundle) — placeholder copy pending real client-supplied FAQ content, flagged in spec.md Section 7. |

---

## Vehicle Landing Page (`prototypes/vehicle-landing/`)

Not a PDP — a make/model landing page (2026-09-15), one per make/model rather than per exact fitment, built to capture proven search terms ("Toyota Hilux Roof Racks"). Worked example re-themed same day from an initial Ford Ranger build to Toyota Hilux — see [[project_pdp_vehicle_landing_page]]. Reuses the global Header/Footer and the shared `.faq-section`/`.faq-item` FAQ component as-is; everything else below is new to this page.

| Name | Description |
|---|---|
| **Fit Finder** | Dark widget below the hero — 5 selects (Make/Model/Year/Body/Roof Type) + "View Results". Make/Model are locked to the page's own vehicle; "View Results" stays disabled until Year/Body/Roof are all chosen, then stubs out (`data-vlp-cta`) since no category/PLP page exists yet for it to hand off to. Year options cover N90/N80/N70/Pre-2005, matching the Generation Table below. — `.fit-finder-widget` |
| **Fitment Gallery** | **The exact same widget as Vehicle-Specific's Fitment Gallery** (2026-09-15 correction — a first version built a bespoke "Recent Fits" carousel instead, replaced per Brenton: same widget, same function, same look everywhere). Markup, CSS, and JS (`initFitGalleryCarousel()`/`buildFitGallerySlideout()` in shared.js) are all unchanged from the PDP version — the only real difference is what the backend would populate `FIT_GALLERY_PHOTOS` with (a whole vehicle's fitments here, one SKU's on a PDP). Uses the real **Toyota Hilux N80** fitment-centre photos, genuine content for this page since the Hilux re-theme. The slide-out's per-fitment detail view still reads `<h1>`/`.package-items`/`.decision-panel` off the page (PDP-only concepts) — on this page those degrade gracefully to the page's own H1, an empty Rack Components list, and a "Back to Product" button that just closes the drawer, rather than erroring. — `.fit-gallery-section` / `.fit-gallery-panel` |
| **Fitment education content** | Plain-content section ("What Roof Racks Fit My Toyota Hilux?") explaining why fitment varies by generation/roof type — real copy, not placeholder. — `.vlp-content` |
| **Generation Table** | Added 2026-09-15. Multi-column data table ("Which Hilux Generation Do I Have?") covering the Hilux's full production history, 1st Gen (1968) through N90 (2024–present) — generation/years/common search names/real Toyota model codes/factory roof fitment, plus a footnote that N70/N80/N90 are aftermarket shorthand, not official Toyota terminology. Backs the same SEO intent as the Fit Finder's Year select and the page's `Vehicle` structured data (Section 5 of the VLP dev brief) — one real table of facts, three surfaces. — `.vlp-gen-table` |
| **Trust banner** | Full-bleed installer-photo banner adapting the PDP's `.install-cta-panel` pattern to two CTAs (Book An Installation / Store Finder) instead of one. — `.vlp-trust-banner` |
| **Shop The Best Brands** | Full-width logo strip below the FAQ. Only brands with real assets already in `_shared/` are shown (Rhino Rack, Yakima, Front Runner, MAXTRAX) — Thule/Wedgetail/Cruiz from the Figma reference are omitted, not fabricated. — `.brands-section` / `.brands-track` |

---

*Generated 2026-09-10, updated 2026-09-11 (Reviews tab, FAQ section, Showroom Finder map rework, Region Selector, UK Brand Skin), updated 2026-09-12 (Discontinued state, Compatibility/Cart-Conflict banner added; Special Order, Ex-Demo/B-Stock, Gold Guarantee, Payment-Plan Badges, and the Region Selector cascade corrected to match current behaviour — see `DEVELOPER-BRIEF.md` Section 7 for the fuller writeup of every widget), updated 2026-09-15 (Vehicle Landing Page section added; same day, re-themed Ford Ranger references to Toyota Hilux and added the Generation Table entry). If a new section or widget gets added, add it here too so this stays the source of truth for naming.*
